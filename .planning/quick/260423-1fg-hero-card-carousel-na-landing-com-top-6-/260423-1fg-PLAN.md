---
phase: 260423-1fg-hero-carousel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/public_route_service.py
  - app/routes/dashboard.py
  - app/templates/landing.html
  - tests/test_hero_carousel.py
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
must_haves:
  truths:
    - "Landing (/) renderiza ate 6 cards de rota vindos do route_cache com preco > 0"
    - "Carousel auto-rotaciona a cada 5s com fade via opacity transition"
    - "Setas prev/next e bolinhas de pagina permitem navegacao manual"
    - "Hover no wrapper pausa a auto-rotacao"
    - "Sem JS o primeiro card ja aparece (SSR fallback funcional)"
    - "Zero nova dependencia JS ou npm (vanilla JS puro, ~80 LOC)"
  artifacts:
    - path: "app/services/public_route_service.py"
      provides: "get_hero_routes(db, limit=6) retornando top 6 rotas do route_cache com preco > 0"
      contains: "def get_hero_routes"
    - path: "app/routes/dashboard.py"
      provides: "Injeta hero_routes no contexto do landing.html; mantem featured_route como fallback"
      contains: "hero_routes"
    - path: "app/templates/landing.html"
      provides: "Wrapper .hero-carousel com N cards, controles prev/next, dots, inline JS de rotacao"
      contains: "hero-carousel"
    - path: "tests/test_hero_carousel.py"
      provides: "Cobertura RED do service e render do template"
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/services/public_route_service.py::get_hero_routes"
      via: "import + call dentro de dashboard_index quando user is None"
      pattern: "get_hero_routes\\(db"
    - from: "app/templates/landing.html"
      to: "hero_routes context var"
      via: "{% for r in hero_routes %}"
      pattern: "for r in hero_routes"
---

<objective>
Substituir o card estatico unico (featured_route) por um carousel rotacionando as top 6 rotas do route_cache, com auto-play 5s, fade, setas, dots e pause-on-hover. SSR puro, vanilla JS inline, zero nova dependencia.

Purpose: Landing hoje mostra so 1 rota em observacao. Expor as top 6 rotas mais frescas do route_cache aumenta densidade informacional do hero sem mudar layout nem endpoint /rotas/.

Output:
- Service `get_hero_routes(db, limit=6)` em `public_route_service.py`
- Route handler `/` passando `hero_routes` pro template
- Template com `.hero-carousel` + inline JS ~80 LOC
- Suite de testes RED-first cobrindo service, rota e markup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/rules/python-coding-style.md
@$HOME/.claude/rules/python-patterns.md
@$HOME/.claude/rules/python-testing.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/templates/landing.html
@app/routes/dashboard.py
@app/services/public_route_service.py
@app/models.py
@tests/test_public_route_discovery.py

<interfaces>
Modelo (app/models.py linhas 156-178):
```python
class RouteCache(Base):
    __tablename__ = "route_cache"
    id: int
    origin: str          # 3 chars
    destination: str     # 3 chars
    departure_date: date
    return_date: date | None
    min_price: float
    currency: str        # default "BRL"
    cached_at: datetime  # server_default=func.now()
    source: str          # default "travelpayouts"
```

Nota: **nao existe campo `updated_at`** em RouteCache. O spec menciona "updated_at DESC" mas o campo real e `cached_at`. Usar `cached_at DESC` como criterio de "mais recente".

Route handler existente (app/routes/dashboard.py:118-139):
```python
@router.get("/", response_class=HTMLResponse)
def dashboard_index(request, msg, db, user):
    if user is None:
        popular_public_routes = get_top_public_routes(db, limit=5)
        featured_route = get_featured_route_for_hero(db)
        return templates.TemplateResponse(
            request=request, name="landing.html",
            context={"user": None, "flash_message": ...,
                     "popular_public_routes": popular_public_routes,
                     "featured_route": featured_route,
                     "format_price_brl": format_price_brl},
        )
```

Services existentes em `app/services/public_route_service.py`:
- `get_featured_route_for_hero(db) -> dict | None` (linhas 28-76) — MANTER como fallback quando `hero_routes` vazio
- `get_top_public_routes(db, limit)` — NAO usar para carousel (filtra por FlightSnapshot, nao por route_cache)
- `iata_to_city(code)` disponivel via `from app.services.iata_cities import iata_to_city`

Helper de formatacao (ja importado em dashboard.py):
```python
from app.services.dashboard_service import format_price_brl
```

Template atual do hero card (app/templates/landing.html:751-808): bloco `{% if featured_route %}<a class="landing-preview landing-preview-link" ...>`. **Substituir por wrapper `.hero-carousel` contendo N slides no mesmo estilo visual do card atual**. Quando `hero_routes` vazio, cair no `featured_route` existente ou num card hardcoded de exemplo.

Test fixtures (tests/conftest.py — inferido de test_public_route_discovery.py:62):
- `unauthenticated_client` — TestClient FastAPI sem sessao
- `db` — Session de banco em memoria
- `test_user` — user de teste
</interfaces>

Regras aplicaveis:
- SSR Jinja2 puro (sem framework JS)
- CSS tokens em base.html — reutilizar `--bg-1`, `--border-1`, `--brand-500`, `--r-xl`, `--sp-*`
- pt-BR com acentuacao correta em texto visivel, sem travessao, sem emojis
- SDD ja aprovado (spec acima), TDD obrigatorio (testes RED antes de cada implementacao)
- Atomic commits com Conventional Commits
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service get_hero_routes com testes RED</name>
  <files>tests/test_hero_carousel.py, app/services/public_route_service.py</files>
  <behavior>
    Testes (RED primeiro, criar ANTES da implementacao, confirmar falha com pytest):

    1. `test_get_hero_routes_returns_top_6_by_cached_at_desc`:
       Arrange: inserir 8 RouteCache com cached_at escalonado (now, now-1h, now-2h...), min_price=1000 em todos.
       Act: `get_hero_routes(db, limit=6)`.
       Assert: retorna 6 dicts, ordenados por cached_at DESC (o mais recente primeiro).

    2. `test_get_hero_routes_filters_price_gt_zero`:
       Arrange: 3 RouteCache com min_price=0, 0.0, -5; 2 com min_price=1500, 2000.
       Act: `get_hero_routes(db, limit=6)`.
       Assert: retorna apenas 2 itens (ambos com preco valido).

    3. `test_get_hero_routes_empty_returns_empty_list`:
       Act/Assert: banco vazio retorna `[]` (nao None).

    4. `test_get_hero_routes_respects_limit`:
       Arrange: 10 rotas validas. Act: `get_hero_routes(db, limit=3)`. Assert: len == 3.

    5. `test_get_hero_routes_shape`:
       Cada dict deve conter chaves exatas: `origin`, `destination`, `origin_city`, `destination_city`, `min_price`, `departure_date`, `return_date`, `cached_at`. `origin_city` e `destination_city` populados via `iata_to_city`.

    Rodar `pytest tests/test_hero_carousel.py::test_get_hero_routes -x` e confirmar falha (AttributeError: module has no attribute 'get_hero_routes') ANTES de implementar.
  </behavior>
  <action>
    RED:
    1. Criar `tests/test_hero_carousel.py` com os 5 testes acima usando o fixture `db` (mesmo padrao de `tests/test_public_route_discovery.py`).
    2. Rodar `rtk pytest tests/test_hero_carousel.py -x -k get_hero_routes` — DEVE FALHAR (attribute missing).

    GREEN (em `app/services/public_route_service.py`):
    3. Adicionar no topo (apos imports existentes, sem duplicar): garantir `from app.models import RouteCache` ja importado (esta).
    4. Implementar:
       ```python
       def get_hero_routes(db: Session, limit: int = 6) -> list[dict]:
           """Top N rotas mais recentes do route_cache com min_price > 0.

           Ordenado por cached_at DESC. Retorna list vazia se nenhuma qualifica.
           Nao chama API externa; leitura direta de route_cache.
           """
           rows = (
               db.query(RouteCache)
               .filter(RouteCache.min_price > 0)
               .order_by(RouteCache.cached_at.desc())
               .limit(limit)
               .all()
           )
           return [
               {
                   "origin": r.origin,
                   "destination": r.destination,
                   "origin_city": iata_to_city(r.origin),
                   "destination_city": iata_to_city(r.destination),
                   "min_price": r.min_price,
                   "departure_date": r.departure_date,
                   "return_date": r.return_date,
                   "cached_at": r.cached_at,
               }
               for r in rows
           ]
       ```
    5. Rodar os testes novamente — DEVEM PASSAR.

    REFACTOR: nada a refatorar (funcao curta, query direta, sem duplicacao).

    Commit atomico: `feat(landing): get_hero_routes retornando top N do route_cache`.
  </action>
  <verify>
    <automated>rtk pytest tests/test_hero_carousel.py -x -k get_hero_routes -v</automated>
  </verify>
  <done>Os 5 testes passam. `get_hero_routes` existe em `public_route_service.py`, filtra min_price > 0, ordena por cached_at DESC, respeita limit, retorna lista com a shape especificada. Commit criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler / injeta hero_routes + teste de integracao</name>
  <files>tests/test_hero_carousel.py, app/routes/dashboard.py</files>
  <behavior>
    Novos testes em `tests/test_hero_carousel.py` (RED antes da alteracao do handler):

    6. `test_landing_injects_hero_routes_context`:
       Arrange: seed 3 RouteCache validas (min_price > 0).
       Act: `unauthenticated_client.get("/")`.
       Assert: status 200, cada par `origin-destination` aparece ao menos uma vez no HTML (ex: "GRU", "SDU"), e `featured_route` nao e mais o unico bloco renderizado (assert marker do carousel: `hero-carousel` no HTML).

    7. `test_landing_falls_back_when_route_cache_empty`:
       Arrange: zero registros em route_cache.
       Act: GET `/`.
       Assert: status 200, pagina renderiza sem erro. Se `featured_route` existir (FlightSnapshot populado), usa ele; senao, mostra pelo menos um card exemplo hardcoded (string marker: `hero-carousel-fallback`).

    Rodar e confirmar falha antes de alterar o handler.
  </behavior>
  <action>
    RED:
    1. Adicionar os 2 testes em `tests/test_hero_carousel.py`.
    2. Rodar `rtk pytest tests/test_hero_carousel.py -x -k landing` — DEVE FALHAR (marker `hero-carousel` ausente).

    GREEN (em `app/routes/dashboard.py`):
    3. No import no topo, adicionar `get_hero_routes` ao bloco ja existente que importa de `public_route_service`. Procurar a linha que ja importa `get_featured_route_for_hero` / `get_top_public_routes` e acrescentar.
    4. Dentro de `dashboard_index`, no ramo `if user is None:` (linhas 125-139), ADICIONAR antes do `return`:
       ```python
       hero_routes = get_hero_routes(db, limit=6)
       ```
    5. Acrescentar `"hero_routes": hero_routes,` no dict `context`. Manter `featured_route` como esta (fallback).

    REFACTOR: verificar ausencia de imports quebrados, rodar suite completa dos testes novos.

    Commit atomico: `feat(landing): injeta hero_routes no contexto da rota /`.
  </action>
  <verify>
    <automated>rtk pytest tests/test_hero_carousel.py -x -v</automated>
  </verify>
  <done>Handler `/` passa `hero_routes` ao template. Quando route_cache vazio, pagina ainda renderiza 200. Os dois testes de landing passam. Commit criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Template hero-carousel com JS vanilla + testes de markup</name>
  <files>tests/test_hero_carousel.py, app/templates/landing.html</files>
  <behavior>
    Testes de markup (RED antes de editar o template):

    8. `test_landing_renders_six_carousel_slides`:
       Arrange: 6 RouteCache validas com origens diferenciadas (GRU, CGH, BSB, GIG, REC, SSA).
       Act: GET `/`.
       Assert: HTML contem 6 ocorrencias de `class="hero-carousel-slide` (ou equivalente), cada IATA aparece no HTML, presenca de `.hero-carousel-prev`, `.hero-carousel-next`, `.hero-carousel-dots`, atributo `data-hero-carousel` no wrapper.

    9. `test_landing_hero_carousel_has_accessible_controls`:
       Assert: controles tem `aria-label` em pt-BR (ex: `aria-label="Proxima rota"`, `aria-label="Rota anterior"`); wrapper tem `role="region"` e `aria-roledescription="carrossel"`.

    Rodar e confirmar falha.
  </behavior>
  <action>
    RED:
    1. Adicionar os 2 testes em `tests/test_hero_carousel.py`.
    2. Rodar `rtk pytest tests/test_hero_carousel.py -x -k carousel` — DEVE FALHAR.

    GREEN (em `app/templates/landing.html`):
    3. SUBSTITUIR o bloco `{% if featured_route %}...{% endif %}` (linhas 751-808) por:

       ```jinja
       {% if hero_routes %}
       <div class="hero-carousel" data-hero-carousel role="region" aria-roledescription="carrossel" aria-label="Rotas em observacao">
           {% for r in hero_routes %}
           <a class="landing-preview landing-preview-link hero-carousel-slide {% if loop.first %}is-active{% endif %}"
              href="/rotas/{{ r.origin }}-{{ r.destination }}"
              data-slide-index="{{ loop.index0 }}"
              aria-hidden="{% if not loop.first %}true{% else %}false{% endif %}"
              aria-label="Ver detalhes da rota {{ r.origin_city }} para {{ r.destination_city }}">
               <div class="preview-header">
                   <span class="preview-header-label">Rota em observacao</span>
                   <span class="preview-days-badge">{{ loop.index }} / {{ hero_routes|length }}</span>
               </div>
               <div class="preview-route">{{ r.origin }} &rarr; {{ r.destination }}</div>
               <div class="preview-price-block">
                   <div class="preview-price-label">Menor preco cacheado <span class="preview-price-hint">ida e volta</span></div>
                   <div class="preview-price-row">
                       <div class="preview-price-value">{{ format_price_brl(r.min_price) }}</div>
                   </div>
                   <div class="preview-metrics">
                       <div>
                           <div class="preview-metric-label">Ida</div>
                           <div class="preview-metric-value">{{ r.departure_date.strftime('%d/%m') }}</div>
                       </div>
                       {% if r.return_date %}
                       <div>
                           <div class="preview-metric-label">Volta</div>
                           <div class="preview-metric-value">{{ r.return_date.strftime('%d/%m') }}</div>
                       </div>
                       {% endif %}
                       <div>
                           <div class="preview-metric-label">Cidades</div>
                           <div class="preview-metric-value">{{ r.origin_city }} / {{ r.destination_city }}</div>
                       </div>
                   </div>
               </div>
               <span class="preview-cta-hint">Ver rota completa &rarr;</span>
           </a>
           {% endfor %}
           <button type="button" class="hero-carousel-prev" aria-label="Rota anterior">&larr;</button>
           <button type="button" class="hero-carousel-next" aria-label="Proxima rota">&rarr;</button>
           <div class="hero-carousel-dots" role="tablist" aria-label="Navegacao de rotas">
               {% for r in hero_routes %}
               <button type="button"
                       class="hero-carousel-dot {% if loop.first %}is-active{% endif %}"
                       data-slide-target="{{ loop.index0 }}"
                       role="tab"
                       aria-selected="{% if loop.first %}true{% else %}false{% endif %}"
                       aria-label="Ir para rota {{ loop.index }}"></button>
               {% endfor %}
           </div>
       </div>
       {% elif featured_route %}
           {# Fallback legado: bloco original preservado #}
           {# ...manter o <a class="landing-preview"> original aqui... #}
       {% else %}
       <div class="hero-carousel hero-carousel-fallback landing-preview">
           <div class="preview-header"><span class="preview-header-label">Exemplo</span></div>
           <div class="preview-route">GRU &rarr; SDU</div>
           <div class="preview-price-block">
               <div class="preview-price-label">Exemplo de rota monitorada</div>
               <div class="preview-price-value">R$ 489,00</div>
           </div>
       </div>
       {% endif %}
       ```

       IMPORTANTE: no ramo `{% elif featured_route %}` reproduzir EXATAMENTE o bloco `<a class="landing-preview landing-preview-link" ...>` original (linhas 752-807). Nao perder o markup existente; o comportamento de fallback depende dele.

    4. Adicionar dentro da tag `<style>` existente (ao final, antes de `</style>`), CSS usando tokens existentes:

       ```css
       .hero-carousel { position: relative; }
       .hero-carousel-slide {
           position: absolute; inset: 0;
           opacity: 0; pointer-events: none;
           transition: opacity 0.3s ease;
       }
       .hero-carousel-slide.is-active {
           position: relative;
           opacity: 1; pointer-events: auto;
       }
       .hero-carousel-prev, .hero-carousel-next {
           position: absolute; top: 50%; transform: translateY(-50%);
           background: var(--bg-2); border: 1px solid var(--border-2);
           color: var(--text-1); width: 36px; height: 36px;
           border-radius: var(--r-full); cursor: pointer; z-index: 2;
           font-size: 18px; line-height: 1;
       }
       .hero-carousel-prev { left: -16px; }
       .hero-carousel-next { right: -16px; }
       .hero-carousel-prev:hover, .hero-carousel-next:hover {
           border-color: var(--brand-500); color: var(--text-0);
       }
       .hero-carousel-dots {
           display: flex; gap: var(--sp-2); justify-content: center;
           margin-top: var(--sp-3);
       }
       .hero-carousel-dot {
           width: 8px; height: 8px; padding: 0;
           border-radius: var(--r-full);
           background: var(--border-2); border: none; cursor: pointer;
           transition: background 180ms ease, transform 180ms ease;
       }
       .hero-carousel-dot.is-active {
           background: var(--brand-500); transform: scale(1.3);
       }
       ```

    5. Adicionar JS inline no final do template (dentro do `<script>` existente OU em novo `<script>` logo apos o existente no bloco content), ~80 LOC:

       ```javascript
       (function() {
           var wrapper = document.querySelector('[data-hero-carousel]');
           if (!wrapper) return;
           var slides = wrapper.querySelectorAll('.hero-carousel-slide');
           var dots = wrapper.querySelectorAll('.hero-carousel-dot');
           var prev = wrapper.querySelector('.hero-carousel-prev');
           var next = wrapper.querySelector('.hero-carousel-next');
           if (slides.length <= 1) return;

           var idx = 0;
           var intervalMs = 5000;
           var timer = null;

           function show(newIdx) {
               newIdx = (newIdx + slides.length) % slides.length;
               slides[idx].classList.remove('is-active');
               slides[idx].setAttribute('aria-hidden', 'true');
               dots[idx].classList.remove('is-active');
               dots[idx].setAttribute('aria-selected', 'false');
               idx = newIdx;
               slides[idx].classList.add('is-active');
               slides[idx].setAttribute('aria-hidden', 'false');
               dots[idx].classList.add('is-active');
               dots[idx].setAttribute('aria-selected', 'true');
           }
           function start() { stop(); timer = setInterval(function() { show(idx + 1); }, intervalMs); }
           function stop() { if (timer) { clearInterval(timer); timer = null; } }

           prev.addEventListener('click', function() { show(idx - 1); start(); });
           next.addEventListener('click', function() { show(idx + 1); start(); });
           dots.forEach(function(dot) {
               dot.addEventListener('click', function() {
                   show(parseInt(dot.getAttribute('data-slide-target'), 10));
                   start();
               });
           });
           wrapper.addEventListener('mouseenter', stop);
           wrapper.addEventListener('mouseleave', start);

           var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
           if (!reduce) start();
       })();
       ```

    6. Rodar `rtk pytest tests/test_hero_carousel.py -x -v` — todos os 9 testes DEVEM PASSAR.

    REFACTOR:
    - Conferir que CSS so usa tokens (nao hex hardcoded)
    - Verificar que nenhuma nova dependencia foi adicionada (`rtk git diff package.json` se existir; nao deve existir nada de frontend)
    - Conferir pt-BR com acentuacao nas strings visiveis (aria-labels, textos)
    - Rodar suite completa `rtk pytest -x` pra garantir zero regressao

    Commit atomico: `feat(landing): hero-carousel com auto-play 5s, setas, dots e pause-on-hover`.
  </action>
  <verify>
    <automated>rtk pytest tests/test_hero_carousel.py -x -v && rtk pytest tests/test_public_route_discovery.py tests/test_dashboard.py -x</automated>
  </verify>
  <done>
    - Template renderiza 6 slides dentro de `.hero-carousel` com controles acessiveis
    - CSS usa apenas tokens de base.html
    - JS vanilla ~80 LOC, sem dependencia nova
    - Todos os 9 testes de `test_hero_carousel.py` passam
    - Suite de landing/dashboard nao quebra
    - Fallback preserva o bloco `featured_route` original
    - Commit criado
  </done>
</task>

</tasks>

<verification>
Ao final da execucao:

1. `rtk pytest tests/test_hero_carousel.py -v` — 9 testes verdes
2. `rtk pytest -x` — suite inteira passa (zero regressao)
3. Inspecao manual opcional: subir servidor local e confirmar:
   - `/` mostra carousel com ate 6 cards
   - Rotacao automatica de ~5s com fade
   - Hover pausa
   - Setas e dots funcionam
   - Com JS desabilitado (devtools), primeiro card ainda visivel (SSR ok)
4. `rtk git log --oneline -5` — 3 commits atomicos com prefixo `feat(landing):`
</verification>

<success_criteria>
- [ ] `get_hero_routes(db, limit=6)` existe, filtra `min_price > 0`, ordena por `cached_at DESC`, retorna lista (nunca None)
- [ ] Rota `/` (dashboard_index, ramo `user is None`) passa `hero_routes` no contexto
- [ ] `landing.html` renderiza `<div class="hero-carousel" data-hero-carousel>` com N slides
- [ ] Primeiro slide tem classe `is-active` no SSR (fallback sem JS)
- [ ] Auto-rotacao a cada 5s com fade via `opacity + transition 0.3s ease`
- [ ] `mouseenter` pausa, `mouseleave` retoma
- [ ] Setas prev/next e dots funcionam
- [ ] Zero npm/pip dependencia nova
- [ ] 9 testes em `tests/test_hero_carousel.py` passam
- [ ] 3 commits atomicos com Conventional Commits
- [ ] Nenhum emoji, travessao ou hex hardcoded em codigo novo
- [ ] pt-BR com acentuacao correta em strings visiveis
</success_criteria>

<output>
Nenhum SUMMARY.md formal (modo quick). Ao final, reportar ao humano:
- Feature entregue: hero carousel top 6 rotas
- Testes: 9 novos, todos passando
- Arquivos modificados: `app/services/public_route_service.py`, `app/routes/dashboard.py`, `app/templates/landing.html`, `tests/test_hero_carousel.py`
- Commits criados (lista)
- Pendencias: opcional conferencia visual via `uvicorn app.main:app --reload`
</output>
