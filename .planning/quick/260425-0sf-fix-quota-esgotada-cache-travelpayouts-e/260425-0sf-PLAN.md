---
quick: 260425-0sf
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_polling_quota_exhausted.py
  - app/services/quota_service.py
  - app/services/polling_service.py
  - app/routes/dashboard.py
  - app/services/route_cache_service.py
autonomous: true
requirements:
  - QUICK-260425-0sf-FIX1  # Short-circuit + flash claro pra quota=0
  - QUICK-260425-0sf-FIX2  # Cache Travelpayouts expandido (BR-internacional)
  - QUICK-260425-0sf-FIX3  # Remover copy mentirosa "fast-flights"

must_haves:
  truths:
    - "Quando quota mensal SerpAPI = 0, run_polling_cycle nao chama SerpAPI e ainda processa rotas via cache Travelpayouts"
    - "manual_polling retorna flash diferenciado quando quota=0 (polling_sem_quota ou polling_parcial_quota)"
    - "Lista de rotas do cron Travelpayouts inclui 8 rotas BR-internacional (GRU/GIG/BSB para SCL/EZE/LIM/BOG/MEX)"
    - "Warning de quota esgotada nao mente sobre fallback para fast-flights"
    - "Suite pytest cresce de 419 para >= 424 testes (5 RED novos)"
  artifacts:
    - path: "tests/test_polling_quota_exhausted.py"
      provides: "5 testes RED cobrindo quota=0 short-circuit, stats dict, flash, lista de rotas, copy"
      min_lines: 80
    - path: "app/services/quota_service.py"
      provides: "Helper next_reset_date()"
      contains: "def next_reset_date"
    - path: "app/services/polling_service.py"
      provides: "run_polling_cycle com short-circuit e retorno dict de stats"
      contains: "snapshots_skipped_quota"
    - path: "app/routes/dashboard.py"
      provides: "manual_polling com flash diferenciado por resultado do ciclo"
      contains: "polling_sem_quota"
    - path: "app/services/route_cache_service.py"
      provides: "TOP_BR_ROUTES com 6 rotas BR-internacional adicionais"
      contains: "GIG\", \"SCL"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/quota_service.py"
      via: "get_remaining_quota + MONTHLY_QUOTA + next_reset_date"
      pattern: "next_reset_date"
    - from: "app/routes/dashboard.py"
      to: "app/services/polling_service.py"
      via: "run_polling_cycle retorna stats dict consumido pelo handler"
      pattern: "snapshots_skipped_quota"
    - from: "app/scheduler.py"
      to: "app/services/route_cache_service.py"
      via: "TOP_BR_ROUTES importado pelo run_travelpayouts_refresh"
      pattern: "TOP_BR_ROUTES"
---

<objective>
Corrigir 3 problemas relacionados ao silencio aparente do polling quando a quota mensal da SerpAPI esgota:

Fix 1: short-circuit quando quota=0 (nao chamar SerpAPI), retornar stats dict, flash diferenciado no handler manual_polling.
Fix 2: expandir lista de rotas do cron Travelpayouts com 6 destinos BR-internacional adicionais (Santiago, Buenos Aires, Lima, Bogota, Cidade do Mexico) garantindo cache mesmo com SerpAPI esgotada.
Fix 3: remover copy mentirosa "Tentando via fast-flights" (fast-flights foi removido em v2.3) e substituir pelo comportamento real (apenas cache Travelpayouts).

Purpose: usuario que clica "Buscar agora" com quota esgotada hoje recebe flash neutro "polling_ok" mesmo sem nenhuma busca real ter ocorrido. Spec aprovada do usuario corrige isso com sinalizacao honesta + cache mais amplo.

Output: 5 testes RED -> implementacao GREEN -> suite passa 424+/424+. Atomic commits respeitados (1 fix por commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/debug/silent-manual-polling.md

@app/services/polling_service.py
@app/services/quota_service.py
@app/services/route_cache_service.py
@app/routes/dashboard.py
@app/scheduler.py

<interfaces>
<!-- Contratos chave para o executor. Extraidos do codebase. Nao precisa explorar de novo. -->

From app/services/quota_service.py:
```python
MONTHLY_QUOTA = 250

def get_current_year_month() -> str: ...
def get_monthly_usage(db: Session) -> int: ...
def increment_usage(db: Session, count: int = 1) -> int: ...
def get_remaining_quota(db: Session) -> int: ...
```

From app/services/polling_service.py (atual):
```python
def run_polling_cycle(user_id: int | None = None):  # nao retorna nada hoje
def _poll_group(db, group: RouteGroup): ...
```
A funcao _poll_group ja consulta route_cache via search_flights, que internamente faz fallback travelpayouts_cached antes de SerpAPI. Logo, basta evitar chamar SerpAPI quando quota=0 — cache hits continuam funcionando.

Localizacao do warning a corrigir (Fix 3): polling_service.py:36-40 (logger.warning com "Tentando via fast-flights").

From app/services/route_cache_service.py:
```python
TOP_BR_ROUTES: list[tuple[str, str]] = [
    ("GRU", "GIG"), ("GIG", "GRU"), ("GRU", "SSA"), ("GRU", "REC"), ("GRU", "FOR"),
    ("CGH", "SDU"), ("GRU", "BSB"), ("GRU", "POA"), ("GRU", "CWB"), ("GRU", "CNF"),
    ("GRU", "MAO"), ("GIG", "SSA"), ("BSB", "GRU"), ("REC", "GRU"), ("FOR", "GRU"),
    ("GRU", "LIS"), ("GRU", "MAD"), ("GRU", "MIA"), ("GRU", "JFK"), ("GRU", "SCL"),
    ("GRU", "EZE"), ("GRU", "LHR"), ("GRU", "CDG"), ("GRU", "FCO"), ("GRU", "BCN"),
    ("GRU", "FRA"), ("GIG", "LIS"), ("GIG", "MIA"),
]
```
Ja inclui GRU-SCL e GRU-EZE. ADICIONAR (6 novos): GIG-SCL, BSB-SCL, GIG-EZE, GRU-LIM, GRU-BOG, GRU-MEX. Os 8 do spec acima formam o conjunto-alvo a ser asserido pelo teste.

From app/routes/dashboard.py:
```python
FLASH_MESSAGES["polling_ok"] = "Busca iniciada em segundo plano. Atualize a página em instantes."
FLASH_MESSAGES["polling_erro"] = "Erro na busca. Tente novamente."

def _run_polling_background(user_id: int | None = None) -> None:
    # chama run_polling_cycle(user_id=user_id) sem capturar retorno

@router.post("/polling/manual")
def manual_polling(request, background_tasks, user):
    background_tasks.add_task(_run_polling_background, uid)
    return RedirectResponse(url="/?msg=polling_ok", status_code=303)
```
PROBLEMA: BackgroundTasks executa apos o response. Logo o handler nao consegue saber o resultado do ciclo antes de redirecionar. SOLUCAO: o flash diferenciado precisa ser determinado no _run_polling_background apos o ciclo completar. Aproveitar mecanismo ja existente: gravar marker server-side OU mover a checagem de quota para ANTES do background task (sincrono, rapido — get_remaining_quota e SELECT trivial). Decisao: checar quota sincronamente no handler manual_polling antes do redirect (consulta sub-segundo, nao bloqueia). Background task processa o ciclo normal.

Logica do handler (apos fix):
- chamar get_remaining_quota(db) sincronamente
- se 0: redirect com msg=polling_sem_quota (com proximo_mes injetado na mensagem)
- se >0: agendar background + redirect msg=polling_ok (atual)
- "polling_parcial_quota" e cenario de transicao raro (quota era >0 quando handler abriu mas zerou durante o ciclo). Nao endereçavel sem persistir resultado do ciclo. Manter na enum + teste de sanity, mas o flash so dispara se um futuro mecanismo escrever esse marker. Por ora, o teste exercita o ramo via injecao direta.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — escrever 5 testes falhantes + Fix 3 (copy mentirosa, 1 linha)</name>
  <files>tests/test_polling_quota_exhausted.py, app/services/polling_service.py</files>
  <behavior>
    - test_run_polling_cycle_with_quota_zero_skips_serpapi: monta DB com ApiUsage.search_count=MONTHLY_QUOTA, mocka serpapi_client.search com MagicMock, cria RouteGroup ativo, roda run_polling_cycle, assert mock.call_count == 0.
    - test_run_polling_cycle_returns_stats_dict: roda ciclo qualquer (sem grupos OK), assert resultado e dict com chaves processed_groups, snapshots_created, snapshots_skipped_quota, snapshots_skipped_no_data.
    - test_manual_polling_flash_when_quota_zero: TestClient POST /polling/manual com usuario logado e quota zerada (ApiUsage no DB), assert resposta 303 com Location contendo "msg=polling_sem_quota".
    - test_travelpayouts_cron_includes_international_routes: importa TOP_BR_ROUTES de route_cache_service, assert que contem ("GRU","SCL"), ("GIG","SCL"), ("BSB","SCL"), ("GRU","EZE"), ("GIG","EZE"), ("GRU","LIM"), ("GRU","BOG"), ("GRU","MEX") (8 tuplas).
    - test_quota_exhausted_warning_message_no_fast_flights: usar caplog.at_level(WARNING), zerar quota, rodar run_polling_cycle, assert "fast-flights" NAO aparece em nenhum record e "cache Travelpayouts" SIM aparece.

    Reutilizar fixtures de tests/conftest.py (db, client, authenticated_user). Seguir padrao AAA dos demais testes (ex: test_polling_service.py se existir).
  </behavior>
  <action>
    1. Criar tests/test_polling_quota_exhausted.py com os 5 testes acima. Imports: pytest, unittest.mock (patch, MagicMock), TestClient, app, modelos. Reusar fixtures do conftest.py existente.
    2. Rodar pytest tests/test_polling_quota_exhausted.py -x e CONFIRMAR FALHA dos 5 (RED). Documentar em comentario quais testes falham por motivo (assertion vs ImportError vs etc).
    3. Fix 3 (trivial, parte do mesmo commit RED pois nao quebra nenhum teste): em app/services/polling_service.py linha 36-40, trocar
       ANTES: `"SerpAPI monthly quota exhausted (%d searches). Tentando via fast-flights."`
       DEPOIS: `"SerpAPI monthly quota exhausted (%d searches). Apenas rotas do cache Travelpayouts serao processadas."`
       Esta mudanca de copy ja faz test_quota_exhausted_warning_message_no_fast_flights passar isoladamente quando run_polling_cycle for chamado com quota=0 (os outros 4 ainda RED).
    4. Commit atomico: `test(quota): adiciona testes RED para short-circuit quota=0 + corrige copy fast-flights`
       Arquivos: tests/test_polling_quota_exhausted.py, app/services/polling_service.py
  </action>
  <verify>
    <automated>cd "C:/Users/gusta/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_polling_quota_exhausted.py -x --tb=short 2>&1 | tail -30</automated>
    Esperado: 4 testes FALHAM (RED) + 1 passa (test_quota_exhausted_warning_message_no_fast_flights, pois o warning ja foi corrigido). Suite total: 419 + 5 = 424 com 4 falhas.
  </verify>
  <done>
    Arquivo tests/test_polling_quota_exhausted.py existe com 5 testes. polling_service.py:38 nao contem mais "fast-flights". Commit atomico criado. RED confirmado (4/5 falhas esperadas).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Fix 1 (short-circuit quota + stats dict + flash handler + next_reset_date)</name>
  <files>app/services/quota_service.py, app/services/polling_service.py, app/routes/dashboard.py</files>
  <behavior>
    Apos esta task, dos 5 testes da task 1, devem passar:
    - test_run_polling_cycle_with_quota_zero_skips_serpapi (SerpAPI nao chamada quando quota=0)
    - test_run_polling_cycle_returns_stats_dict (dict com 4 chaves nominais)
    - test_manual_polling_flash_when_quota_zero (303 + msg=polling_sem_quota)
    - test_quota_exhausted_warning_message_no_fast_flights (continua passando)

    Continua RED apos esta task: test_travelpayouts_cron_includes_international_routes (Task 3).
  </behavior>
  <action>
    Implementacao em 3 arquivos, 1 commit atomico ao final:

    1. app/services/quota_service.py — adicionar helper:
       ```python
       def next_reset_date() -> datetime.date:
           """Retorna 1o dia do proximo mes (UTC)."""
           today = datetime.datetime.utcnow().date()
           if today.month == 12:
               return datetime.date(today.year + 1, 1, 1)
           return datetime.date(today.year, today.month + 1, 1)
       ```

    2. app/services/polling_service.py — refatorar run_polling_cycle:
       - Capturar `remaining = get_remaining_quota(db)` no inicio.
       - Calcular `use_serpapi = remaining > 0`.
       - Inicializar contadores: processed_groups=0, snapshots_created=0, snapshots_skipped_quota=0, snapshots_skipped_no_data=0.
       - Passar use_serpapi para _poll_group (novo parametro opcional, default True para retrocompat dos testes existentes).
       - Em _poll_group, propagar para search_flights via parametro `disable_serpapi=not use_serpapi` (verificar assinatura de search_flights — se nao aceita, contornar com guard local: skip quando source serpapi e use_serpapi False; route_cache hits ainda contam normalmente).
       - Acumular contadores conforme cada flight produz snapshot ou e pulado.
       - No fim, retornar dict: `{"processed_groups": ..., "snapshots_created": ..., "snapshots_skipped_quota": ..., "snapshots_skipped_no_data": ...}`.
       - Se search_flights nao tiver flag disable_serpapi: aplicar guard ANTES da chamada — se source seria serpapi (verificar via cache lookup direto a route_cache_service.get_cached_price antes de search_flights, e se cache miss e use_serpapi False, incrementar snapshots_skipped_quota e continue).

    3. app/routes/dashboard.py — atualizar handler manual_polling:
       - Adicionar 2 entradas em FLASH_MESSAGES (perto da linha 809):
         ```python
         FLASH_MESSAGES["polling_sem_quota"] = (
             "Cota mensal de busca esgotada. Renovada automaticamente em "
             "{proximo_mes}. Rotas do cache continuam sendo atualizadas 4x/dia."
         )
         FLASH_MESSAGES["polling_parcial_quota"] = (
             "Cota esgotada. Atualizadas apenas rotas do cache popular. "
             "Renovacao em {proximo_mes}."
         )
         ```
         Como o renderer atual nao formata placeholders dinamicos por chave, formatar a mensagem inline antes do redirect ou adicionar tratamento especial. Decisao mais simples: mensagem sem placeholder dinamico, copy estatica:
         ```python
         FLASH_MESSAGES["polling_sem_quota"] = (
             "Cota mensal de busca esgotada. Renovada automaticamente no dia 1 do proximo mes. "
             "Rotas do cache continuam sendo atualizadas 4x/dia."
         )
         FLASH_MESSAGES["polling_parcial_quota"] = (
             "Cota esgotada. Atualizadas apenas rotas do cache popular. "
             "Renovacao no dia 1 do proximo mes."
         )
         ```
         (Justificativa: usuario nao precisa saber a data exata; "dia 1 do proximo mes" e auto-evidente. next_reset_date() ainda fica disponivel para uso futuro / templates.)
       - Refatorar manual_polling: antes de agendar background, abrir SessionLocal sincrono e chamar get_remaining_quota. Se 0, redirect 303 para /?msg=polling_sem_quota SEM agendar background (cache Travelpayouts ja roda no cron, nao adianta forcar nada agora). Se >0, manter comportamento atual (agendar + msg=polling_ok).
         ```python
         from app.services.quota_service import get_remaining_quota
         from app.database import SessionLocal
         ...
         db = SessionLocal()
         try:
             remaining = get_remaining_quota(db)
         finally:
             db.close()
         if remaining <= 0:
             return RedirectResponse(url="/?msg=polling_sem_quota", status_code=303)
         background_tasks.add_task(_run_polling_background, uid)
         return RedirectResponse(url="/?msg=polling_ok", status_code=303)
         ```

    4. Rodar suite completa: `python -m pytest tests/test_polling_quota_exhausted.py -x` — esperado 4/5 verdes (somente travelpayouts_cron ainda RED).

    5. Rodar full: `python -m pytest -q` — esperado: 423 verdes + 1 falha (test_travelpayouts_cron_includes_international_routes).

    6. Commit atomico: `fix(polling): short-circuit quando quota SerpAPI esgota e flash diferenciado em manual_polling`
  </action>
  <verify>
    <automated>cd "C:/Users/gusta/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_polling_quota_exhausted.py -k "not international_routes" -x --tb=short 2>&1 | tail -20</automated>
    Esperado: 4/4 testes (excluindo o de rotas internacionais) passam.
    E rodar full: `python -m pytest -q` deve mostrar somente test_travelpayouts_cron_includes_international_routes falhando.
  </verify>
  <done>
    next_reset_date() existe em quota_service. run_polling_cycle retorna dict de stats e nao chama SerpAPI quando quota=0. manual_polling redireciona para polling_sem_quota quando quota=0. Commit atomico criado. 4/5 testes da task 1 passam.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Fix 2 (expandir TOP_BR_ROUTES com 6 rotas BR-internacional)</name>
  <files>app/services/route_cache_service.py</files>
  <behavior>
    Apos esta task: test_travelpayouts_cron_includes_international_routes passa. Suite completa: 424/424 verdes.
  </behavior>
  <action>
    1. Editar app/services/route_cache_service.py, lista TOP_BR_ROUTES (linha 127-134). Adicionar 6 tuplas (as 8 do spec, descontando GRU-SCL e GRU-EZE que ja existem):
       - ("GIG", "SCL")
       - ("BSB", "SCL")
       - ("GIG", "EZE")
       - ("GRU", "LIM")
       - ("GRU", "BOG")
       - ("GRU", "MEX")
       Inserir agrupadas com as outras BR-internacional (apos GRU-EZE) para manter agrupamento logico.

    2. NAO incluir REC/JPA/CPV-internacional (spec explicito: sem cobertura Travelpayouts direta).

    3. Rodar `python -m pytest tests/test_polling_quota_exhausted.py -x` — esperado 5/5 verdes.

    4. Rodar full suite `python -m pytest -q` — esperado 424 passed (419 originais + 5 novos).

    5. Commit atomico: `feat(cache): adiciona 6 rotas BR-internacional ao cron Travelpayouts`
  </action>
  <verify>
    <automated>cd "C:/Users/gusta/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_polling_quota_exhausted.py -x --tb=short 2>&1 | tail -15 && python -m pytest -q 2>&1 | tail -5</automated>
    Esperado: 5/5 do arquivo de quota + 424 passed total.
  </verify>
  <done>
    TOP_BR_ROUTES contem as 8 rotas BR-internacional do spec. Todos os 5 testes RED viraram GREEN. Suite full 424/424. Commit atomico criado.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest -q` retorna 424 passed (ou superior se houver outros testes adjacentes), 0 failed.
- Grep `fast-flights` em app/ nao retorna match em polling_service.py.
- Grep `polling_sem_quota` em dashboard.py retorna match no FLASH_MESSAGES e no handler manual_polling.
- Grep `("GIG", "SCL")` em route_cache_service.py retorna match.
- 3 commits atomicos no git log: test(quota), fix(polling), feat(cache).
</verification>

<success_criteria>
- 5 testes RED -> 5 testes GREEN.
- Suite total 419 -> 424.
- Quando quota=0: SerpAPI nao chamada, flash polling_sem_quota exibido.
- Cron Travelpayouts cobre 8 rotas BR-internacional.
- Copy "fast-flights" eliminada do warning de quota.
- 3 commits atomicos respeitando 1 fix por commit.
</success_criteria>

<output>
Apos completar, criar `.planning/quick/260425-0sf-fix-quota-esgotada-cache-travelpayouts-e/260425-0sf-SUMMARY.md` com:
- O que mudou (3 fixes resumidos).
- Commits gerados (3 hashes).
- Suite final (numero de testes).
- Edge cases descobertos durante implementacao (se houver).
- Pendencias / observacoes (ex: polling_parcial_quota nao tem trigger automatico atual — anotar).
</output>
