---
phase: 03-signal-detection
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/services/signal_service.py
  - tests/test_signal_service.py
autonomous: false
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-03
  - SIGN-04
  - SIGN-05

must_haves:
  truths:
    - "Todos os testes passam apos refatoracao"
    - "Nenhuma funcao tem complexidade ciclomatica acima de 5"
    - "Nenhuma duplicacao de logica no signal_service.py"
    - "Cada funcao tem responsabilidade unica"
    - "Nenhum print/debug esquecido"
    - "App roda de ponta a ponta e sinais sao detectados corretamente"
  artifacts:
    - path: "app/services/signal_service.py"
      provides: "Codigo refatorado com qualidade verificada"
      min_lines: 120
    - path: "tests/test_signal_service.py"
      provides: "Testes limpos e bem organizados"
      min_lines: 150
  key_links:
    - from: "app/services/signal_service.py"
      to: "app/services/polling_service.py"
      via: "detect_signals chamado apos save_flight_snapshot"
      pattern: "detect_signals"
---

<objective>
Fase REFACTOR do TDD: revisar todo o codigo produzido nos Plans 01 e 02, aplicar checklist de qualidade do CLAUDE.md (Secao 4), e verificar que a aplicacao funciona de ponta a ponta. Checkpoint humano para validar que tudo roda no ambiente real.

Purpose: Garantir qualidade do codigo de deteccao de sinais antes de avancar para Phase 4. Detectar problemas de integracao que testes automatizados podem nao pegar.
Output: Codigo refatorado, checklist de revisao completo, RESUMO DA ENTREGA, confirmacao humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-signal-detection/03-01-SUMMARY.md
@.planning/phases/03-signal-detection/03-02-SUMMARY.md
@CLAUDE.md

@app/services/signal_service.py
@app/services/polling_service.py
@app/models.py
@tests/test_signal_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatoracao e checklist de qualidade (REFACTOR)</name>
  <files>app/services/signal_service.py, tests/test_signal_service.py</files>
  <read_first>
    CLAUDE.md (checklist de revisao obrigatorio na secao FASE 4)
    app/services/signal_service.py (verificar duplicacao, SRP, complexidade)
    tests/test_signal_service.py (verificar organizacao, helpers)
    app/services/polling_service.py (verificar integracao)
  </read_first>
  <action>
    Aplicar o checklist de revisao do CLAUDE.md (Fase 4) ao codigo da Phase 3:

    1. Rodar python -m pytest tests/ -v para confirmar baseline GREEN

    2. Verificar complexidade ciclomatica (max 5 por funcao):
       - Contar IFs, elifs, fors, whiles em cada funcao de signal_service.py
       - Se alguma funcao excede 5, extrair logica para subfuncoes
       - _check_balde_fechando e _check_balde_reaberto sao candidatas por iterar sobre classes com condicoes

    3. Verificar DRY (sem duplicacao):
       - Se _check_balde_fechando e _check_balde_reaberto compartilham logica de _booking_classes_to_dict, garantir que usam a mesma funcao helper
       - Se ha pattern repetido de criacao de DetectedSignal, considerar _create_signal helper

    4. Verificar SRP (responsabilidade unica):
       - detect_signals deve apenas orquestrar (chamar detectores + dedup + persist)
       - Cada _check_* deve apenas detectar (retornar signal ou None)
       - _is_duplicate deve apenas consultar (sem side effects)

    5. Verificar nomes:
       - Funcoes, variaveis, parametros devem ser auto-explicativos
       - Sem abreviacoes obscuras

    6. Verificar tratamento de erros:
       - detect_signals deve tratar excecoes de detectores individuais sem crashar
       - previous=None tratado gracefully em balde detectors

    7. Remover qualquer print() ou logging de debug esquecido
       (logger.info para sinais detectados no polling_service.py eh intencional, manter)

    8. Rodar python -m pytest tests/ -v novamente
       TODOS os testes devem continuar passando apos refatoracao.
       Se algum teste falhar, DESFAZER ultima mudanca.

    9. Verificar testes:
       - Helpers _make_snapshot e _make_route_group estao DRY
       - Testes usam nomes descritivos
       - Nenhum teste depende de ordem de execucao

    10. Produzir RESUMO DA ENTREGA conforme template CLAUDE.md Secao 4.2:
        Feature: Signal Detection Phase 3
        Testes: X passando / 0 falhando
        Cobertura: X%
        Arquivos modificados: [lista]
        Decisoes tomadas: [lista]
        Pendencias / proximos passos: [lista]
  </action>
  <verify>
    <automated>cd "/c/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>Codigo refatorado, checklist de qualidade aplicado, todos os testes passam, RESUMO DA ENTREGA produzido conforme CLAUDE.md Secao 4.2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana da deteccao de sinais</name>
  <what-built>
    Sistema completo de deteccao de sinais para Phase 3:
    - DetectedSignal model no banco com indice de deduplicacao
    - 4 detectores: BALDE_FECHANDO, BALDE_REABERTO, PRECO_ABAIXO_HISTORICO, JANELA_OTIMA
    - Deduplicacao de 12h para evitar re-alertas
    - Integracao automatica com polling_service (detecta sinais apos cada snapshot)
    - 18+ testes passando
    - Codigo refatorado conforme checklist CLAUDE.md
  </what-built>
  <how-to-verify>
    1. No terminal, ativar venv e rodar: python -m pytest tests/ -v
       -> deve mostrar todos os testes passando (incluindo test_signal_service.py)
    2. Verificar que nenhuma funcao em signal_service.py tem mais de 5 niveis de IF aninhado
    3. Rodar: python main.py
    4. Abrir http://localhost:8000/docs no navegador
    5. Verificar que a aplicacao inicia sem erros
    6. Confirmar que a tabela detected_signals existe no banco:
       python -c "from app.database import engine; from sqlalchemy import inspect; print([t for t in inspect(engine).get_table_names() if 'signal' in t])"
    7. Revisar o RESUMO DA ENTREGA produzido na Task 1
  </how-to-verify>
  <verify>
    <automated>cd "/c/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>Usuario aprovou a deteccao de sinais funcionando. Phase 3 pronta para transicao para Phase 4.</done>
  <resume-signal>Digite "aprovado" se tudo funciona, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- python -m pytest tests/ -v --tb=short (todos passam)
- Nenhuma funcao com complexidade ciclomatica > 5
- Sem print/debug esquecido
- RESUMO DA ENTREGA conforme CLAUDE.md Secao 4.2
- Usuario aprovou execucao de ponta a ponta
</verification>

<success_criteria>
- Codigo refatorado sem duplicacao, com SRP e complexidade controlada
- Todos os testes passam apos refatoracao
- RESUMO DA ENTREGA produzido
- Usuario confirmou que app funciona de ponta a ponta
- Phase 3 pronta para transicao para Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-detection/03-03-SUMMARY.md`
</output>
