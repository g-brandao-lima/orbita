---
phase: 33-public-route-index-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - app/config.py
  - app/services/affiliate_links.py
  - app/services/public_share_card_service.py
  - app/routes/public.py
  - app/templates/public/route.html
  - tests/test_affiliate_links.py
  - tests/test_public_sitemap.py
  - tests/test_public_og_image.py
autonomous: true
requirements:
  - SEO-02
  - SEO-03
  - SEO-05
must_haves:
  truths:
    - "GET /sitemap.xml retorna XML valido listando rotas com >=10 snapshots"
    - "GET /rotas/GRU-LIS/og-image.png retorna PNG 1200x630 com preco atual"
    - "HTML da pagina publica contem og:image e twitter:image apontando para a og-image"
    - "Botao 'Comprar' na pagina publica tem href de affiliate Travelpayouts com marker configuravel"
    - "build_affiliate_url retorna URL no formato aviasales com marker=<marker>"
    - "Sitemap e og-image servem 100% do banco, sem chamada externa"
  artifacts:
    - path: app/services/affiliate_links.py
      provides: "build_aviasales_url(origin, dest, departure, return_, marker) + DEFAULT_TRIP_OFFSET_DAYS"
    - path: app/services/public_share_card_service.py
      provides: "build_public_og_card(origin, dest, current_price, median_180d) -> bytes"
    - path: app/config.py
      provides: "Settings.travelpayouts_marker (default '714304')"
    - path: app/routes/public.py
      provides: "Endpoints /sitemap.xml e /rotas/{O}-{D}/og-image.png; afiliate_url injetado no template"
  key_links:
    - from: app/routes/public.py
      to: app/services/affiliate_links.py
      via: "build_aviasales_url chamado no handler da pagina e injetado no template como affiliate_url"
      pattern: "build_aviasales_url"
    - from: app/templates/public/route.html
      to: app/routes/public.py
      via: "template usa affiliate_url para href do botao Comprar e og_image no meta OG"
      pattern: "affiliate_url|og:image"
    - from: app/routes/public.py (sitemap)
      to: app/services/public_route_service.has_enough_data
      via: "filtra rotas por threshold antes de listar"
      pattern: "has_enough_data|MIN_SNAPSHOTS_FOR_INDEX"
---

<objective>
Completar o canal SEO + monetizacao: (a) sitemap.xml dinamico listando rotas com snapshots suficientes (SEO-02); (b) imagem Open Graph dinamica servida por endpoint PNG (SEO-03); (c) affiliate link Travelpayouts no botao Comprar (SEO-05).

Purpose: Indexacao no Google (sitemap), compartilhamento rico em redes sociais (OG image) e monetizacao passiva (affiliate).
Output: Sitemap acessivel, og-image rendering do preco atual, pagina publica com botao Comprar afiliado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-public-route-index-seo/33-01-PLAN.md
@.planning/phases/32-cache-layer-travelpayouts/32-SUMMARY.md
@app/services/route_cache_service.py
@app/services/share_card_service.py
@app/services/public_route_service.py
@app/routes/public.py
@app/config.py
@app/templates/public/route.html

<interfaces>
From app/services/public_route_service.py (criado em 33-01):
```python
MIN_SNAPSHOTS_FOR_INDEX = 10
def has_enough_data(db: Session, origin: str, destination: str) -> bool
def get_route_stats(db: Session, origin: str, destination: str) -> dict | None
```

From app/services/route_cache_service.py:
```python
TOP_BR_ROUTES: list[tuple[str, str]]  # 28 rotas seed
```

From app/config.py:
```python
class Settings(BaseSettings):
    app_base_url: str = "http://localhost:8000"
    travelpayouts_token: str = ""
    # 33-02 adiciona: travelpayouts_marker: str = "714304"
```

From app/services/share_card_service.py (Phase 30 existente, referencia de estilo Pillow):
```python
def build_price_card(group, snapshot, historical_ctx, passengers) -> bytes  # PNG
# Constantes CARD_W, CARD_H, BG_TOP, ACCENT, GREEN, RED, MUTED, TEXT
```

Formato affiliate Aviasales (documentado em RESEARCH/context):
  https://www.aviasales.com/search/{ORIG}{DDMM}{DEST}{DDMM}1?marker={MARKER}
  - {DDMM} = data no formato DDMM (2 digitos dia + 2 digitos mes)
  - trecho final "1" = 1 adulto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: affiliate_links.py + settings.travelpayouts_marker + injecao no template</name>
  <files>
    app/config.py
    app/services/affiliate_links.py
    app/routes/public.py
    app/templates/public/route.html
    tests/test_affiliate_links.py
  </files>
  <behavior>
    # tests/test_affiliate_links.py
    - `build_aviasales_url("GRU", "LIS", date(2026, 6, 15), date(2026, 6, 30), "714304")` retorna string contendo `GRU1506LIS3006` e `marker=714304`
    - URL comeca com `https://www.aviasales.com/search/`
    - Se `marker` vazio/None, URL ainda e gerada mas SEM `marker=` (fallback seguro, nao quebra botao)
    - `default_trip_dates()` retorna tupla (departure, return_) com departure ~60 dias a frente e return = departure + 10 dias (constante)
    - Codigos minusculos sao normalizados para maiuscula no path
    - Test de integracao: GET /rotas/GRU-LIS contem `href="https://www.aviasales.com/search/GRU` no HTML
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_affiliate_links.py` com os casos acima + um teste de integracao via TestClient que monkeypatch `settings.travelpayouts_marker = "714304"` e verifica que o HTML de `/rotas/GRU-LIS` contem o link de afiliado. Rodar e confirmar falha.

    Passo 2 (GREEN) — adicionar em `app/config.py`:
    ```python
    travelpayouts_marker: str = "714304"  # ID afiliado do user no dashboard Travelpayouts
    ```
    (manter compatibilidade: env var `TRAVELPAYOUTS_MARKER` sobrescreve)

    Passo 3 (GREEN) — criar `app/services/affiliate_links.py`:
    ```python
    """Construtor de links de afiliado (monetizacao SEO-05).

    Aviasales e o motor de busca da Travelpayouts. O marker identifica o afiliado
    (usuario do programa) e atribui a comissao. Formato oficial:
      https://www.aviasales.com/search/{ORIG}{DDMM}{DEST}{DDMM}{PAX}?marker={MARKER}

    Nota: Aviasales pode exigir approval adicional no dashboard Travelpayouts
    para comissoes efetivas; o link funciona mesmo sem approval (redireciona a busca).
    """
    import datetime

    DEFAULT_TRIP_OFFSET_DAYS = 60   # viagem daqui a 60 dias
    DEFAULT_STAY_DAYS = 10          # retorno 10 dias depois

    def default_trip_dates(today: datetime.date | None = None) -> tuple[datetime.date, datetime.date]:
        today = today or datetime.date.today()
        departure = today + datetime.timedelta(days=DEFAULT_TRIP_OFFSET_DAYS)
        return_ = departure + datetime.timedelta(days=DEFAULT_STAY_DAYS)
        return departure, return_

    def _ddmm(d: datetime.date) -> str:
        return f"{d.day:02d}{d.month:02d}"

    def build_aviasales_url(
        origin: str,
        destination: str,
        departure: datetime.date,
        return_: datetime.date,
        marker: str | None = None,
        passengers: int = 1,
    ) -> str:
        origin = origin.upper()
        destination = destination.upper()
        path = f"{origin}{_ddmm(departure)}{destination}{_ddmm(return_)}{max(1, passengers)}"
        url = f"https://www.aviasales.com/search/{path}"
        if marker:
            url += f"?marker={marker}"
        return url
    ```

    Passo 4 (GREEN) — em `app/routes/public.py`, no handler `public_route_page`:
    - Importar `from app.services.affiliate_links import build_aviasales_url, default_trip_dates`
    - Antes do `TemplateResponse`, calcular:
      ```python
      dep, ret = default_trip_dates()
      affiliate_url = build_aviasales_url(origin, destination, dep, ret, settings.travelpayouts_marker)
      ```
    - Passar `affiliate_url` no context do template.

    Passo 5 (GREEN) — em `app/templates/public/route.html`, no bloco content (depois do CTA "Monitore essa rota"), adicionar:
    ```jinja
    <div class="public-cta" style="padding-top: 0;">
      <a href="{{ affiliate_url }}" target="_blank" rel="nofollow sponsored noopener" class="btn-cta" style="background: linear-gradient(135deg,#16a34a,#22c55e);">
        Comprar agora
      </a>
      <p style="color:#64748b; font-size:12px; margin-top:12px;">Redireciona para Aviasales (parceiro). Podemos receber comissao sem custo extra para voce.</p>
    </div>
    ```

    Passo 6 — rodar `pytest tests/test_affiliate_links.py tests/test_public_route.py -x`. Verde.
  </action>
  <verify>
    <automated>pytest tests/test_affiliate_links.py tests/test_public_route.py -x</automated>
  </verify>
  <done>build_aviasales_url gera URL correta com marker configuravel; pagina publica renderiza botao Comprar com link de afiliado usando settings.travelpayouts_marker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sitemap.xml dinamico filtrando rotas por threshold</name>
  <files>
    app/routes/public.py
    tests/test_public_sitemap.py
  </files>
  <behavior>
    # tests/test_public_sitemap.py
    - Seed: GRU-LIS com 15 FlightSnapshot (>=10), GRU-GIG com 5 FlightSnapshot (<10), CGH-SDU com 12 FlightSnapshot
    - `GET /sitemap.xml` -> status 200, Content-Type `application/xml` (ou text/xml)
    - XML valido: parse via `xml.etree.ElementTree` sem erro
    - Contem `<loc>http://localhost:8000/rotas/GRU-LIS</loc>`
    - Contem `<loc>http://localhost:8000/rotas/CGH-SDU</loc>`
    - NAO contem `GRU-GIG` (abaixo do threshold)
    - Cada `<url>` tem `<lastmod>` (ISO date) e `<priority>`
    - Header `Cache-Control: public, max-age=3600` (cache agressivo; rotas mudam lentamente)
    - Namespace correto: `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"`
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_public_sitemap.py` com TestClient + seed + assercoes acima. Confirmar falha.

    Passo 2 (GREEN) — adicionar em `app/routes/public.py`:
    ```python
    from fastapi.responses import Response
    from sqlalchemy import func
    from app.models import FlightSnapshot
    from app.services.public_route_service import MIN_SNAPSHOTS_FOR_INDEX

    @router.get("/sitemap.xml")
    def sitemap_xml(db: Session = Depends(get_db)):
        # Rotas com >= MIN_SNAPSHOTS_FOR_INDEX no historico
        rows = (
            db.query(
                FlightSnapshot.origin,
                FlightSnapshot.destination,
                func.count(FlightSnapshot.id).label("cnt"),
                func.max(FlightSnapshot.collected_at).label("last"),
            )
            .group_by(FlightSnapshot.origin, FlightSnapshot.destination)
            .having(func.count(FlightSnapshot.id) >= MIN_SNAPSHOTS_FOR_INDEX)
            .all()
        )
        base = settings.app_base_url.rstrip("/")
        items = []
        for origin, destination, _cnt, last in rows:
            lastmod = last.date().isoformat() if last else ""
            items.append(
                f"  <url>\n"
                f"    <loc>{base}/rotas/{origin}-{destination}</loc>\n"
                f"    <lastmod>{lastmod}</lastmod>\n"
                f"    <changefreq>daily</changefreq>\n"
                f"    <priority>0.7</priority>\n"
                f"  </url>"
            )
        body = (
            '<?xml version="1.0" encoding="UTF-8"?>\n'
            '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'
            + "\n".join(items)
            + "\n</urlset>\n"
        )
        return Response(
            content=body,
            media_type="application/xml",
            headers={"Cache-Control": "public, max-age=3600"},
        )
    ```

    Passo 3 — rodar `pytest tests/test_public_sitemap.py -x`. Verde.
  </action>
  <verify>
    <automated>pytest tests/test_public_sitemap.py -x</automated>
  </verify>
  <done>GET /sitemap.xml retorna XML valido listando apenas rotas acima do threshold, parseavel sem erro, com lastmod e priority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Open Graph image dinamica /rotas/{O}-{D}/og-image.png</name>
  <files>
    app/services/public_share_card_service.py
    app/routes/public.py
    tests/test_public_og_image.py
  </files>
  <behavior>
    # tests/test_public_og_image.py
    - Seed com FlightSnapshot GRU-LIS (>=10 snapshots) + RouteCache
    - `GET /rotas/GRU-LIS/og-image.png` -> status 200, Content-Type `image/png`
    - Body comeca com magic bytes PNG (`\x89PNG\r\n\x1a\n`)
    - Dimensoes 1200x630 (validar via `PIL.Image.open(BytesIO(body)).size == (1200, 630)`)
    - Header `Cache-Control: public, max-age=21600`
    - `GET /rotas/XXX-YYY/og-image.png` (sem dados) -> 404
    - `build_public_og_card(origin="GRU", dest="LIS", current_price=2345.0, median_180d=2500.0, origin_city="Sao Paulo", dest_city="Lisboa")` retorna bytes >0 comecando com magic PNG
    - NAO chama SerpAPI nem Travelpayouts
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_public_og_image.py`. Confirmar falha.

    Passo 2 (GREEN) — criar `app/services/public_share_card_service.py`:
    ```python
    """Open Graph image 1200x630 para pagina publica de rota (SEO-03).

    Gera PNG em memoria usando Pillow. Reusa paleta de share_card_service.
    """
    from io import BytesIO
    from PIL import Image, ImageDraw

    from app.services.share_card_service import _load_font, _gradient_background
    from app.services.dashboard_service import format_price_brl

    OG_W, OG_H = 1200, 630
    BG = (11, 14, 20)
    ACCENT = (14, 165, 233)
    GREEN = (34, 197, 94)
    RED = (239, 68, 68)
    TEXT = (226, 232, 240)
    MUTED = (148, 163, 184)

    def build_public_og_card(
        origin: str,
        dest: str,
        current_price: float | None,
        median_180d: float | None,
        origin_city: str,
        dest_city: str,
    ) -> bytes:
        img = Image.new("RGB", (OG_W, OG_H), BG)
        _gradient_background(img)
        draw = ImageDraw.Draw(img)

        f_brand = _load_font(32, bold=True)
        f_route = _load_font(64, bold=True)
        f_cities = _load_font(28)
        f_price = _load_font(140, bold=True)
        f_sub = _load_font(26)

        draw.text((60, 50), "FLIGHT MONITOR", font=f_brand, fill=ACCENT)
        draw.text((60, 140), f"{origin}  \u2192  {dest}", font=f_route, fill=TEXT)
        draw.text((60, 220), f"{origin_city} para {dest_city}", font=f_cities, fill=MUTED)

        # Preco central
        if current_price:
            price_text = format_price_brl(current_price)
            color = GREEN
            if median_180d and current_price > median_180d * 1.05:
                color = RED
            draw.text((60, 300), price_text, font=f_price, fill=color)
            sub = "preco de referencia"
            if median_180d:
                delta_pct = round((current_price - median_180d) / median_180d * 100)
                sign = "+" if delta_pct >= 0 else ""
                sub += f"   \u00b7   {sign}{delta_pct}% vs mediana 180d"
            draw.text((64, 470), sub, font=f_sub, fill=MUTED)
        else:
            draw.text((60, 340), "Monitore este preco", font=f_cities, fill=TEXT)

        draw.text((60, OG_H - 60), "flight-monitor.onrender.com", font=f_sub, fill=MUTED)

        buf = BytesIO()
        img.save(buf, format="PNG", optimize=True)
        return buf.getvalue()
    ```

    Passo 3 (GREEN) — adicionar handler em `app/routes/public.py`:
    ```python
    from fastapi.responses import Response as FastAPIResponse
    from app.services.public_share_card_service import build_public_og_card

    @router.get("/rotas/{pair}/og-image.png")
    def public_route_og_image(
        pair: str,
        db: Session = Depends(get_db),
    ):
        m = IATA_PAIR.match(pair)
        if not m:
            raise HTTPException(status_code=404, detail="Rota invalida")
        origin, destination = m.group(1).upper(), m.group(2).upper()
        stats = get_route_stats(db, origin, destination)
        if stats is None:
            raise HTTPException(status_code=404, detail="Rota sem dados")
        png_bytes = build_public_og_card(
            origin=origin,
            dest=destination,
            current_price=stats["current_price"],
            median_180d=stats["median_180d"],
            origin_city=stats["origin_city"],
            dest_city=stats["destination_city"],
        )
        return FastAPIResponse(
            content=png_bytes,
            media_type="image/png",
            headers={"Cache-Control": "public, max-age=21600"},
        )
    ```

    IMPORTANTE: ordem das rotas no FastAPI — registrar `/rotas/{pair}/og-image.png` ANTES de `/rotas/{pair}` no arquivo? FastAPI resolve por ordem de declaracao; como ambas tem o mesmo path prefix mas a mais especifica tem sufixo fixo `/og-image.png`, elas nao colidem (path param `{pair}` nao consome a barra). Mesmo assim, declarar o endpoint de imagem logo apos o registro do router por clareza.

    Passo 4 — rodar `pytest tests/test_public_og_image.py tests/test_public_route.py tests/test_public_sitemap.py tests/test_affiliate_links.py -x`. Verde. Depois `pytest -x` full suite.
  </action>
  <verify>
    <automated>pytest tests/test_public_og_image.py tests/test_public_sitemap.py tests/test_affiliate_links.py tests/test_public_route.py -x</automated>
  </verify>
  <done>OG image PNG 1200x630 renderiza corretamente com preco, delta vs mediana, cidades. Meta og:image e twitter:image do template apontam para endpoint funcional. Sitemap + OG + affiliate cobrem SEO-02/03/05.</done>
</task>

</tasks>

<verification>
- `pytest -x` suite completa verde (testes do plan 01 + novos do plan 02)
- Validar manualmente: `curl -I http://localhost:8000/rotas/GRU-LIS/og-image.png` retorna `image/png` + `Cache-Control`
- Validar XML: `curl http://localhost:8000/sitemap.xml | python -c "import sys, xml.etree.ElementTree as ET; ET.fromstring(sys.stdin.read())"` nao levanta erro
- Validar meta OG: buscar `og:image` no HTML de `/rotas/GRU-LIS`, URL deve resolver em PNG valido
</verification>

<success_criteria>
1. SEO-02 atendido: /sitemap.xml listando apenas rotas com >= MIN_SNAPSHOTS_FOR_INDEX
2. SEO-03 atendido: meta OG + Twitter Card com imagem dinamica 1200x630
3. SEO-05 atendido: botao "Comprar" com href de afiliado Aviasales/Travelpayouts (marker configuravel)
4. Suite pytest verde, sem regressao
5. Zero chamada externa por pageview confirmada (testes usam monkeypatch de rede)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/33-public-route-index-seo/33-02-SUMMARY.md` listando:
- Endpoints novos (/sitemap.xml, /rotas/{O}-{D}/og-image.png)
- settings.travelpayouts_marker + nota para o user validar marker=714304 em producao
- Observacao sobre aprovacao Aviasales no dashboard Travelpayouts (pode ser pendente)
- Numero total de testes adicionados
</output>
