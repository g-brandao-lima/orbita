---
phase: 33-public-route-index-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/iata_cities.py
  - app/services/public_route_service.py
  - app/routes/public.py
  - app/templates/public/route.html
  - main.py
  - tests/test_public_route.py
  - tests/test_public_route_service.py
  - tests/test_iata_cities.py
autonomous: true
requirements:
  - SEO-01
  - SEO-04
must_haves:
  truths:
    - "GET /rotas/GRU-LIS retorna 200 com HTML contendo preco atual, historico 180d, melhores meses e CTA"
    - "GET /rotas/ZZZ-YYY (sem dados) retorna 404"
    - "GET /robots.txt retorna text/plain permitindo /rotas/* e referenciando /sitemap.xml"
    - "Pagina publica nao dispara SerpAPI nem Travelpayouts HTTP (servida 100% do banco local)"
    - "HTML contem tag rel=canonical apontando para a propria URL absoluta"
    - "Response header Cache-Control: public, max-age=21600"
  artifacts:
    - path: app/services/iata_cities.py
      provides: "IATA -> nome de cidade + fallback"
      contains: "IATA_CITIES"
    - path: app/services/public_route_service.py
      provides: "get_route_stats(db, origin, dest) e has_enough_data(db, origin, dest)"
    - path: app/routes/public.py
      provides: "Rotas publicas: /rotas/{ORIG}-{DEST} e /robots.txt"
    - path: app/templates/public/route.html
      provides: "Template SSR da pagina publica"
  key_links:
    - from: app/routes/public.py
      to: app/services/public_route_service.py
      via: "get_route_stats consultado no handler"
      pattern: "get_route_stats"
    - from: main.py
      to: app/routes/public.py
      via: "app.include_router(public_router)"
      pattern: "public_router"
    - from: app/services/public_route_service.py
      to: models.RouteCache + models.FlightSnapshot
      via: "SQLAlchemy queries (NAO chamar SerpAPI nem Travelpayouts)"
      pattern: "RouteCache|FlightSnapshot"
---

<objective>
Criar o core da pagina publica SEO: handler `/rotas/{ORIG}-{DEST}` renderizado 100% do banco local (RouteCache + FlightSnapshot), com mapeamento IATA -> cidade, service de agregacao e `robots.txt`. Cumpre SEO-01 (pagina publica com preco atual, historico 180d, melhores meses, CTA) e SEO-04 (robots.txt + rel=canonical).

Purpose: Abrir canal de aquisicao organica sem consumir cota de APIs externas.
Output: Pagina publica funcional em `/rotas/GRU-LIS` servida do cache acumulado pela Phase 32, sem login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-cache-layer-travelpayouts/32-SUMMARY.md
@app/models.py
@app/services/route_cache_service.py
@app/services/dashboard_service.py
@app/routes/dashboard.py
@app/templates/base.html
@main.py

<interfaces>
<!-- Contratos do banco e services que o executor vai usar -->

From app/models.py:
```python
class RouteCache(Base):
    __tablename__ = "route_cache"
    origin: Mapped[str]          # String(3)
    destination: Mapped[str]     # String(3)
    departure_date: Mapped[date]
    return_date: Mapped[date | None]
    min_price: Mapped[float]
    currency: Mapped[str]        # default "BRL"
    cached_at: Mapped[datetime]
    source: Mapped[str]

class FlightSnapshot(Base):
    __tablename__ = "flight_snapshots"
    origin: Mapped[str]
    destination: Mapped[str]
    departure_date: Mapped[date]
    return_date: Mapped[date]
    price: Mapped[float]
    currency: Mapped[str]
    collected_at: Mapped[datetime]
```

From app/services/dashboard_service.py:
```python
def format_price_brl(price: float) -> str  # "R$ 2.345,00"
def format_date_br(d: date | None) -> str  # "dd/mm/aaaa"
```

From app/config.py:
```python
settings.app_base_url: str  # ex: "https://flight-monitor.onrender.com"
```

From main.py include pattern (linha 58-68):
```python
app.include_router(public_router)  # sem prefix, rota raiz e /rotas/*
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IATA cities mapping + public_route_service (RED + GREEN)</name>
  <files>
    app/services/iata_cities.py
    app/services/public_route_service.py
    tests/test_iata_cities.py
    tests/test_public_route_service.py
  </files>
  <behavior>
    # tests/test_iata_cities.py
    - `iata_to_city("GRU")` retorna "Sao Paulo"
    - `iata_to_city("LIS")` retorna "Lisboa"
    - `iata_to_city("ZZZ")` (nao mapeado) retorna "ZZZ" (fallback IATA cru)
    - Dict `IATA_CITIES` tem pelo menos 30 entradas cobrindo as rotas de `TOP_BR_ROUTES`

    # tests/test_public_route_service.py (usar fixture `db` e seed com FlightSnapshot + RouteCache)
    - `get_route_stats(db, "GRU", "LIS")` retorna dict com chaves:
      `current_price` (float), `currency` (str), `cached_at` (datetime | None),
      `median_180d` (float | None), `snapshot_count` (int),
      `monthly_series` (list[dict{month_label, avg_price, snapshot_count}]),
      `best_months` (list[dict{month_label, avg_price}] ordenado ascendente, top 3),
      `origin_city` (str), `destination_city` (str)
    - Quando nao ha dados (`snapshot_count == 0` e sem RouteCache), retorna `None`
    - Quando ha RouteCache recente mas 0 FlightSnapshot, retorna dict com current_price do cache e historico vazio (nao retorna None)
    - `has_enough_data(db, "GRU", "LIS")` retorna True se snapshot_count >= 10 (constante MIN_SNAPSHOTS_FOR_INDEX = 10)
    - `has_enough_data` retorna False para rota com < 10 snapshots
    - **NENHUMA das funcoes chama SerpAPI ou Travelpayouts HTTP** (teste valida via monkeypatch que `flight_search.search_flights_ex` e `TravelpayoutsClient.fetch_calendar` NAO sao invocados)
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_iata_cities.py` e `tests/test_public_route_service.py` com os casos acima. Rodar `pytest tests/test_iata_cities.py tests/test_public_route_service.py -x` e confirmar FALHA (arquivos de implementacao ainda nao existem).

    Passo 2 (GREEN) — criar `app/services/iata_cities.py`:
    ```python
    """Mapeamento IATA -> nome de cidade para titulos SEO.

    Fallback: se IATA nao estiver no dict, retorna o proprio codigo (ex: "YYY").
    """
    IATA_CITIES: dict[str, str] = {
        # BR domesticas (cobrir todas as rotas de TOP_BR_ROUTES)
        "GRU": "Sao Paulo", "CGH": "Sao Paulo", "VCP": "Campinas",
        "GIG": "Rio de Janeiro", "SDU": "Rio de Janeiro",
        "BSB": "Brasilia", "CNF": "Belo Horizonte", "SSA": "Salvador",
        "REC": "Recife", "FOR": "Fortaleza", "POA": "Porto Alegre",
        "CWB": "Curitiba", "MAO": "Manaus", "NAT": "Natal", "BEL": "Belem",
        "MCZ": "Maceio", "FLN": "Florianopolis", "VIX": "Vitoria",
        "AJU": "Aracaju", "THE": "Teresina", "SLZ": "Sao Luis",
        "CGB": "Cuiaba", "CGR": "Campo Grande", "GYN": "Goiania",
        # Internacionais principais
        "LIS": "Lisboa", "MAD": "Madri", "BCN": "Barcelona",
        "CDG": "Paris", "LHR": "Londres", "FCO": "Roma", "MXP": "Milao",
        "FRA": "Frankfurt", "MUC": "Munique", "AMS": "Amsterda",
        "ZRH": "Zurique", "VIE": "Viena", "BRU": "Bruxelas",
        "MIA": "Miami", "JFK": "Nova York", "LAX": "Los Angeles",
        "MCO": "Orlando", "IAH": "Houston", "ORD": "Chicago",
        "YYZ": "Toronto", "MEX": "Cidade do Mexico", "CUN": "Cancun",
        "EZE": "Buenos Aires", "SCL": "Santiago", "LIM": "Lima",
        "BOG": "Bogota", "UIO": "Quito", "MVD": "Montevideu",
        "ASU": "Assuncao", "PTY": "Cidade do Panama",
        "DXB": "Dubai", "DOH": "Doha", "IST": "Istambul",
        "NRT": "Toquio", "HND": "Toquio", "ICN": "Seul",
    }

    def iata_to_city(code: str) -> str:
        return IATA_CITIES.get(code.upper(), code.upper())
    ```

    Passo 3 (GREEN) — criar `app/services/public_route_service.py`:
    ```python
    """Agregacao somente-banco para pagina publica SEO.

    IMPORTANTE: nao chamar SerpAPI nem Travelpayouts. Tudo vem do banco local
    (RouteCache, FlightSnapshot). Phase 33 exige zero chamada externa por pageview.
    """
    import datetime
    import statistics
    from collections import defaultdict

    from sqlalchemy.orm import Session

    from app.models import FlightSnapshot, RouteCache
    from app.services.iata_cities import iata_to_city

    MIN_SNAPSHOTS_FOR_INDEX = 10
    HISTORY_WINDOW_DAYS = 180
    MONTH_LABELS_PT = [
        "Jan", "Fev", "Mar", "Abr", "Mai", "Jun",
        "Jul", "Ago", "Set", "Out", "Nov", "Dez",
    ]

    def _month_label(d: datetime.date) -> str:
        return f"{MONTH_LABELS_PT[d.month - 1]}/{d.year}"

    def has_enough_data(db: Session, origin: str, destination: str) -> bool:
        count = (
            db.query(FlightSnapshot)
            .filter(
                FlightSnapshot.origin == origin,
                FlightSnapshot.destination == destination,
            )
            .count()
        )
        return count >= MIN_SNAPSHOTS_FOR_INDEX

    def get_route_stats(db: Session, origin: str, destination: str) -> dict | None:
        origin = origin.upper()
        destination = destination.upper()
        cutoff = datetime.datetime.utcnow() - datetime.timedelta(days=HISTORY_WINDOW_DAYS)

        snaps = (
            db.query(FlightSnapshot)
            .filter(
                FlightSnapshot.origin == origin,
                FlightSnapshot.destination == destination,
                FlightSnapshot.collected_at >= cutoff,
            )
            .all()
        )
        cache_row = (
            db.query(RouteCache)
            .filter(
                RouteCache.origin == origin,
                RouteCache.destination == destination,
            )
            .order_by(RouteCache.cached_at.desc())
            .first()
        )

        if not snaps and cache_row is None:
            return None

        # Preco atual: preferir menor RouteCache vigente; fallback snapshot mais recente
        current_price = None
        currency = "BRL"
        cached_at = None
        if cache_row is not None:
            current_price = cache_row.min_price
            currency = cache_row.currency
            cached_at = cache_row.cached_at
        elif snaps:
            latest = max(snaps, key=lambda s: s.collected_at)
            current_price = latest.price
            currency = latest.currency

        # Historico por mes (avg de FlightSnapshot)
        by_month: dict[str, list[float]] = defaultdict(list)
        for s in snaps:
            if s.price and s.price > 0:
                by_month[_month_label(s.collected_at.date())].append(s.price)
        monthly_series = [
            {"month_label": label, "avg_price": round(sum(p) / len(p), 2), "snapshot_count": len(p)}
            for label, p in sorted(by_month.items(), key=lambda kv: kv[0])
        ]

        # Mediana 180d
        all_prices = [s.price for s in snaps if s.price and s.price > 0]
        median_180d = round(statistics.median(all_prices), 2) if all_prices else None

        # Melhores meses (top 3 com menor avg)
        best_months = sorted(monthly_series, key=lambda m: m["avg_price"])[:3]

        return {
            "origin": origin,
            "destination": destination,
            "origin_city": iata_to_city(origin),
            "destination_city": iata_to_city(destination),
            "current_price": current_price,
            "currency": currency,
            "cached_at": cached_at,
            "median_180d": median_180d,
            "snapshot_count": len(snaps),
            "monthly_series": monthly_series,
            "best_months": best_months,
        }
    ```

    Passo 4 — rodar `pytest tests/test_iata_cities.py tests/test_public_route_service.py -x` e confirmar VERDE.
  </action>
  <verify>
    <automated>pytest tests/test_iata_cities.py tests/test_public_route_service.py -x</automated>
  </verify>
  <done>Testes verdes. `get_route_stats` funciona com cache e com snapshots, retorna None quando nao ha nenhum dado, e foi provado por monkeypatch que NAO invoca SerpAPI ou Travelpayouts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route handler /rotas/{ORIG}-{DEST} + /robots.txt + template</name>
  <files>
    app/routes/public.py
    app/templates/public/route.html
    main.py
    tests/test_public_route.py
  </files>
  <behavior>
    # tests/test_public_route.py (usar FastAPI TestClient + DB fixture)
    - Seed: FlightSnapshot GRU-LIS com 12 snapshots em datas variadas nos ultimos 90d + 1 RouteCache recente
    - `GET /rotas/GRU-LIS` -> status 200, Content-Type text/html
    - HTML contem: "GRU", "LIS", "Sao Paulo", "Lisboa", preco formatado "R$ ", CTA com texto "Monitore essa rota" e link "/auth/login", header `Cache-Control: public, max-age=21600`
    - HTML contem `<link rel="canonical" href="http://localhost:8000/rotas/GRU-LIS"` (usa settings.app_base_url)
    - HTML contem `<meta name="description"` com preco e cidades
    - HTML contem `<title>Passagens Sao Paulo para Lisboa` (titulo semantico)
    - `GET /rotas/XXX-YYY` (rota sem dados) -> status 404 com pagina de erro amigavel
    - `GET /rotas/invalid-format` (origem/destino fora do padrao 3 letras) -> status 404
    - `GET /rotas/gru-lis` (minusculas) aceito (normalizado para upper)
    - `GET /robots.txt` -> status 200, Content-Type text/plain, body contem "User-agent: *", "Allow: /rotas/", "Sitemap: http://localhost:8000/sitemap.xml"
    - `GET /rotas/GRU-LIS` NAO executa SerpAPI/Travelpayouts (monkeypatch bloqueia rede, teste passa mesmo sem TRAVELPAYOUTS_TOKEN)
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_public_route.py` com TestClient, seed de FlightSnapshot + RouteCache, e todos os casos acima. Rodar `pytest tests/test_public_route.py -x` e confirmar falha (handler ainda nao existe).

    Passo 2 (GREEN) — criar `app/routes/public.py`:
    ```python
    """Rotas publicas SEO (Phase 33).

    NAO requer autenticacao. Serve 100% do banco local (sem chamadas externas).
    """
    import re
    from pathlib import Path

    from fastapi import APIRouter, Depends, HTTPException, Request
    from fastapi.responses import HTMLResponse, PlainTextResponse
    from sqlalchemy.orm import Session

    from app.config import settings
    from app.database import get_db
    from app.services.dashboard_service import format_price_brl, format_date_br
    from app.services.public_route_service import get_route_stats
    from app.templates_config import get_templates

    router = APIRouter(tags=["public"])
    _TEMPLATES_DIR = Path(__file__).resolve().parent.parent / "templates"
    templates = get_templates(str(_TEMPLATES_DIR))

    IATA_PAIR = re.compile(r"^([A-Za-z]{3})-([A-Za-z]{3})$")
    PUBLIC_CACHE_SECONDS = 21600  # 6h, alinhado com TTL do route_cache

    @router.get("/rotas/{pair}", response_class=HTMLResponse)
    def public_route_page(
        pair: str,
        request: Request,
        db: Session = Depends(get_db),
    ):
        m = IATA_PAIR.match(pair)
        if not m:
            raise HTTPException(status_code=404, detail="Rota nao encontrada")
        origin, destination = m.group(1).upper(), m.group(2).upper()

        stats = get_route_stats(db, origin, destination)
        if stats is None:
            raise HTTPException(status_code=404, detail="Rota sem dados suficientes")

        canonical = f"{settings.app_base_url.rstrip('/')}/rotas/{origin}-{destination}"
        og_image = f"{settings.app_base_url.rstrip('/')}/rotas/{origin}-{destination}/og-image.png"
        response = templates.TemplateResponse(
            request=request,
            name="public/route.html",
            context={
                "stats": stats,
                "canonical": canonical,
                "og_image": og_image,
                "format_price_brl": format_price_brl,
                "format_date_br": format_date_br,
                "user": None,  # pagina publica
            },
        )
        response.headers["Cache-Control"] = f"public, max-age={PUBLIC_CACHE_SECONDS}"
        return response

    @router.get("/robots.txt", response_class=PlainTextResponse)
    def robots_txt():
        base = settings.app_base_url.rstrip("/")
        body = (
            "User-agent: *\n"
            "Allow: /rotas/\n"
            "Disallow: /admin/\n"
            "Disallow: /auth/\n"
            "Disallow: /groups/\n"
            "Disallow: /api/\n"
            f"Sitemap: {base}/sitemap.xml\n"
        )
        return PlainTextResponse(body, headers={"Cache-Control": "public, max-age=86400"})
    ```

    Passo 3 (GREEN) — criar `app/templates/public/route.html` extendendo `base.html`:
    ```jinja
    {% extends "base.html" %}
    {% set s = stats %}
    {% block title %}Passagens {{ s.origin_city }} para {{ s.destination_city }} — Flight Monitor{% endblock %}
    {% block head %}
      <link rel="canonical" href="{{ canonical }}">
      <meta name="description" content="Passagens {{ s.origin_city }} ({{ s.origin }}) para {{ s.destination_city }} ({{ s.destination }}): preco atual {{ format_price_brl(s.current_price) if s.current_price else 'consulte' }}{% if s.median_180d %}, mediana 180d {{ format_price_brl(s.median_180d) }}{% endif %}. Monitore e receba alertas quando o preco cair.">
      <!-- Placeholders OG/Twitter; serao preenchidos em 33-02 -->
      <meta property="og:type" content="website">
      <meta property="og:url" content="{{ canonical }}">
      <meta property="og:title" content="Passagens {{ s.origin_city }} para {{ s.destination_city }}">
      <meta property="og:image" content="{{ og_image }}">
      <meta name="twitter:card" content="summary_large_image">
      <meta name="twitter:image" content="{{ og_image }}">
      <style>
        .public-hero { text-align:center; padding: 48px 0 32px; }
        .public-hero h1 { font-size: 32px; font-weight: 700; color: #e2e8f0; margin-bottom: 8px; }
        .public-hero .sub { color:#94a3b8; font-size: 15px; }
        .public-price { font-size: 56px; font-weight: 700; color: #22c55e; font-family: ui-monospace, monospace; margin: 24px 0 8px; }
        .public-price-sub { color: #94a3b8; font-size: 13px; }
        .public-card { background:#111827; border:1px solid #1e293b; border-radius:12px; padding:24px; margin-bottom:16px; }
        .public-card h2 { font-size: 18px; color:#e2e8f0; margin-bottom: 12px; font-weight: 600; }
        .public-months { display:grid; grid-template-columns:repeat(auto-fit, minmax(140px, 1fr)); gap:12px; }
        .public-month { background:#0b0e14; border:1px solid #1e293b; border-radius:8px; padding:12px; text-align:center; }
        .public-month .label { color:#94a3b8; font-size:12px; }
        .public-month .price { color:#e2e8f0; font-size:18px; font-weight:700; font-family: ui-monospace, monospace; }
        .public-cta { text-align:center; padding: 32px 0; }
        .public-cta .btn-cta { display:inline-block; background: linear-gradient(135deg,#0ea5e9,#3b82f6); color:#fff; font-weight:700; padding: 14px 28px; border-radius:10px; text-decoration:none; }
        .public-cta .btn-cta:hover { transform: translateY(-1px); }
      </style>
    {% endblock %}
    {% block content %}
      <section class="public-hero">
        <h1>Passagens {{ s.origin_city }} para {{ s.destination_city }}</h1>
        <div class="sub">{{ s.origin }} &rarr; {{ s.destination }}</div>
        {% if s.current_price %}
          <div class="public-price">{{ format_price_brl(s.current_price) }}</div>
          <div class="public-price-sub">
            preco de referencia
            {% if s.median_180d %} &middot; mediana 180d {{ format_price_brl(s.median_180d) }}{% endif %}
            {% if s.cached_at %} &middot; atualizado {{ format_date_br(s.cached_at.date()) }}{% endif %}
          </div>
        {% endif %}
      </section>

      {% if s.best_months %}
      <div class="public-card">
        <h2>Melhores meses historicos</h2>
        <div class="public-months">
          {% for m in s.best_months %}
            <div class="public-month">
              <div class="label">{{ m.month_label }}</div>
              <div class="price">{{ format_price_brl(m.avg_price) }}</div>
            </div>
          {% endfor %}
        </div>
      </div>
      {% endif %}

      {% if s.monthly_series %}
      <div class="public-card">
        <h2>Historico 180 dias</h2>
        <div class="public-months">
          {% for m in s.monthly_series %}
            <div class="public-month">
              <div class="label">{{ m.month_label }}</div>
              <div class="price">{{ format_price_brl(m.avg_price) }}</div>
            </div>
          {% endfor %}
        </div>
      </div>
      {% endif %}

      <section class="public-cta">
        <a href="/auth/login" class="btn-cta">Monitore essa rota</a>
        <p style="color:#64748b; font-size:13px; margin-top:12px;">Grátis. Avisamos por email quando o preco cair.</p>
      </section>
    {% endblock %}
    ```

    Passo 4 — registrar router em `main.py`. Adicionar apos `from app.routes.dashboard import router as dashboard_router`:
    ```python
    from app.routes.public import router as public_router
    ```
    E apos `app.include_router(dashboard_router)`:
    ```python
    app.include_router(public_router)
    ```

    Passo 5 — rodar `pytest tests/test_public_route.py -x` e confirmar VERDE. Rodar a suite completa `pytest -x` e confirmar 0 regressao.
  </action>
  <verify>
    <automated>pytest tests/test_public_route.py tests/test_public_route_service.py tests/test_iata_cities.py -x</automated>
  </verify>
  <done>GET /rotas/GRU-LIS retorna 200 com HTML publico completo (titulo, canonical, meta description, preco, historico, melhores meses, CTA). GET /robots.txt retorna text/plain com Allow /rotas/ e referencia de sitemap. Rota invalida retorna 404. Suite pytest verde.</done>
</task>

</tasks>

<verification>
- `pytest -x` continua verde (334+ testes)
- `curl -s http://localhost:8000/rotas/GRU-LIS` retorna 200 com HTML
- `curl -s http://localhost:8000/robots.txt` retorna text/plain valido
- `curl -I http://localhost:8000/rotas/GRU-LIS` mostra header `Cache-Control: public, max-age=21600`
- Nenhuma chamada externa (SerpAPI / Travelpayouts) em logs por pageview
</verification>

<success_criteria>
1. SEO-01 parcialmente atendido: pagina publica existe com preco, historico, melhores meses, CTA
2. SEO-04 atendido: robots.txt + rel=canonical em cada rota
3. Todos os novos testes passam (>=8 testes adicionais)
4. Zero regressao na suite existente
5. Pagina serve 100% do banco local (verificado via monkeypatch de rede nos testes)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/33-public-route-index-seo/33-01-SUMMARY.md` listando:
- Arquivos criados (iata_cities.py, public_route_service.py, public.py, route.html)
- Rotas publicadas (/rotas/{ORIG}-{DEST}, /robots.txt)
- Testes adicionados (count)
- Observacoes para Plan 02 (pontos de extensao para og-image e sitemap)
</output>
