---
phase: 33-public-route-index-seo
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - app/services/public_route_service.py
  - app/routes/dashboard.py
  - app/templates/dashboard/index.html
  - app/templates/landing.html
  - tests/test_public_route_discovery.py
autonomous: true
requirements:
  - SEO-01
must_haves:
  truths:
    - "Landing page mostra secao 'Rotas populares' com ate 5 links para /rotas/{ORIG}-{DEST} somente de rotas com dados suficientes"
    - "Dashboard logado mostra link 'Ver pagina publica' em cada card de grupo cuja rota mais barata tenha >=10 snapshots"
    - "Links internos sao crawlaveis (tags <a href> estaticas, nao JavaScript)"
    - "get_top_public_routes(db, limit=5) retorna rotas ordenadas por numero de snapshots desc filtradas por MIN_SNAPSHOTS_FOR_INDEX"
  artifacts:
    - path: app/services/public_route_service.py
      provides: "get_top_public_routes(db, limit=5) -> list[dict]"
    - path: app/templates/landing.html
      provides: "Secao 'Rotas populares' com links internos"
    - path: app/templates/dashboard/index.html
      provides: "Link 'Ver pagina publica' por card"
  key_links:
    - from: app/templates/landing.html
      to: "/rotas/{origin}-{destination}"
      via: "Jinja for loop sobre popular_public_routes"
      pattern: "/rotas/"
    - from: app/templates/dashboard/index.html
      to: "/rotas/{origin}-{destination}"
      via: "link condicional renderizado se route.has_public_page e True"
      pattern: "/rotas/"
    - from: app/routes/dashboard.py (dashboard_index + landing handler)
      to: app/services/public_route_service.get_top_public_routes + has_enough_data
      via: "context injection para os templates"
      pattern: "get_top_public_routes|has_enough_data"
---

<objective>
Amplificar descoberta e indexacao criando links internos das paginas autenticadas (dashboard) e da landing publica para as paginas `/rotas/{ORIG}-{DEST}`. Internal linking e um sinal forte para SEO e ajuda o Google a encontrar e ranquear o novo indice publico.

Purpose: Aumentar a profundidade de indexacao do site; novos visitantes organicos da landing descobrem as paginas de rota; usuarios logados podem compartilhar o link publico da propria rota.
Output: Secao "Rotas populares" na landing + link discreto por card no dashboard apontando para a pagina publica correspondente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-public-route-index-seo/33-01-PLAN.md
@app/services/public_route_service.py
@app/services/dashboard_service.py
@app/routes/dashboard.py
@app/templates/landing.html
@app/templates/dashboard/index.html

<interfaces>
From app/services/public_route_service.py (Plan 01):
```python
MIN_SNAPSHOTS_FOR_INDEX = 10
def has_enough_data(db, origin, destination) -> bool
def get_route_stats(db, origin, destination) -> dict | None
# Plan 03 adiciona:
def get_top_public_routes(db, limit=5) -> list[dict]
```

From app/routes/dashboard.py:
- `dashboard_index` handler renderiza landing.html quando user=None e dashboard/index.html quando user existe
- `groups` no context do dashboard e lista de dicts com chave `cheapest_snapshot` (FlightSnapshot com origin/destination)

From app/services/dashboard_service.get_groups_with_summary:
- Cada item tem `cheapest_snapshot.origin` e `cheapest_snapshot.destination` quando ha dados
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_top_public_routes + injecao em landing e dashboard</name>
  <files>
    app/services/public_route_service.py
    app/routes/dashboard.py
    app/templates/landing.html
    app/templates/dashboard/index.html
    tests/test_public_route_discovery.py
  </files>
  <behavior>
    # tests/test_public_route_discovery.py
    - Seed: 3 rotas com volumes diferentes de FlightSnapshot (GRU-LIS=50, CGH-SDU=30, GRU-GIG=15, GRU-FOR=5)
    - `get_top_public_routes(db, limit=5)` retorna lista com 3 elementos (GRU-FOR filtrada por < 10)
    - Ordem: GRU-LIS primeiro (mais snapshots), depois CGH-SDU, depois GRU-GIG
    - Cada item e dict com `origin`, `destination`, `origin_city`, `destination_city`, `snapshot_count`
    - `get_top_public_routes(db, limit=2)` retorna exatamente 2 itens
    - Retorna [] quando banco esta vazio

    # Integracao landing (user=None)
    - `GET /` sem login, com banco seedado, retorna HTML contendo secao "Rotas populares"
    - HTML contem pelo menos 1 `href="/rotas/GRU-LIS"` (a com mais snapshots)
    - HTML NAO contem `/rotas/GRU-FOR` (abaixo do threshold)

    # Integracao dashboard (user logado)
    - Seed: grupo do user com cheapest_snapshot GRU-LIS (rota com >=10 snapshots)
    - `GET /` logado -> HTML do card contem `href="/rotas/GRU-LIS"` com texto "Ver pagina publica"
    - Se grupo nao tem cheapest_snapshot, link nao aparece
    - Se rota do grupo tem < 10 snapshots, link nao aparece
  </behavior>
  <action>
    Passo 1 (RED) — criar `tests/test_public_route_discovery.py` com seed de FlightSnapshot e assercoes acima. Rodar, confirmar falha.

    Passo 2 (GREEN) — adicionar em `app/services/public_route_service.py`:
    ```python
    from sqlalchemy import func

    def get_top_public_routes(db: Session, limit: int = 5) -> list[dict]:
        """Rotas com >= MIN_SNAPSHOTS_FOR_INDEX, ordenadas por volume desc.

        Usado na landing publica e no dashboard logado para criar links internos
        que ajudam indexacao SEO das paginas /rotas/{ORIG}-{DEST}.
        """
        rows = (
            db.query(
                FlightSnapshot.origin,
                FlightSnapshot.destination,
                func.count(FlightSnapshot.id).label("cnt"),
            )
            .group_by(FlightSnapshot.origin, FlightSnapshot.destination)
            .having(func.count(FlightSnapshot.id) >= MIN_SNAPSHOTS_FOR_INDEX)
            .order_by(func.count(FlightSnapshot.id).desc())
            .limit(limit)
            .all()
        )
        return [
            {
                "origin": origin,
                "destination": destination,
                "origin_city": iata_to_city(origin),
                "destination_city": iata_to_city(destination),
                "snapshot_count": cnt,
            }
            for origin, destination, cnt in rows
        ]
    ```

    Passo 3 (GREEN) — em `app/routes/dashboard.py` `dashboard_index`:
    - Importar `from app.services.public_route_service import get_top_public_routes, has_enough_data`
    - No branch `if user is None`: calcular `popular_public_routes = get_top_public_routes(db, limit=5)` e passar no context da landing.
    - No branch autenticado: para cada item de `groups`, adicionar chave `public_route_slug` setada para `f"{origin}-{destination}"` se `cheapest_snapshot` existe E `has_enough_data(db, origin, destination)` for True, senao None. (Fazer esse enriquecimento apos `get_groups_with_summary`.)

    Passo 4 (GREEN) — em `app/templates/landing.html`, antes da secao `<!-- CTA final -->`, adicionar:
    ```jinja
    {% if popular_public_routes %}
    <section class="landing-section">
      <h2 class="landing-section-heading">Rotas populares</h2>
      <div class="landing-grid">
        {% for r in popular_public_routes[:3] %}
          <a href="/rotas/{{ r.origin }}-{{ r.destination }}" class="landing-card" style="text-decoration:none; display:block;">
            <div class="step-title">{{ r.origin_city }} &rarr; {{ r.destination_city }}</div>
            <div class="step-desc">{{ r.origin }} &rarr; {{ r.destination }} &middot; ver historico e melhores meses</div>
          </a>
        {% endfor %}
      </div>
    </section>
    {% endif %}
    ```
    (Usar no maximo 3 cards em linha para manter o grid atual. Se `popular_public_routes` tem 5 itens, exibir 3 primeiros.)

    Passo 5 (GREEN) — em `app/templates/dashboard/index.html`, localizar o rodape de cada card de grupo (onde aparecem links como "Meus Alertas" ou botoes de acao). Adicionar, condicionalmente:
    ```jinja
    {% if item.public_route_slug %}
      <a href="/rotas/{{ item.public_route_slug }}" class="btn btn-ghost btn-sm" title="Pagina publica desta rota">
        Ver pagina publica
      </a>
    {% endif %}
    ```
    (Localizacao exata: dentro do footer/acoes do card. Se o template usa componente reutilizavel, adicionar la. Se nao souber, usar `grep -n "cheapest_snapshot\|card-footer\|group-actions" app/templates/dashboard/index.html` para achar o ponto de insercao.)

    Passo 6 — rodar `pytest tests/test_public_route_discovery.py -x`. Verde. Depois full suite `pytest -x`.
  </action>
  <verify>
    <automated>pytest tests/test_public_route_discovery.py tests/test_public_route.py -x</automated>
  </verify>
  <done>Landing (deslogado) mostra secao "Rotas populares" com ate 3 links para paginas publicas, apenas de rotas com >=10 snapshots. Dashboard logado mostra link "Ver pagina publica" em cards cuja rota mais barata tem dados suficientes. Testes verdes.</done>
</task>

</tasks>

<verification>
- `pytest -x` verde
- `curl -s http://localhost:8000/` (deslogado) contem `href="/rotas/` em secao Rotas populares quando banco tem dados
- Acessar dashboard logado -> cards com rota >= 10 snapshots mostram link "Ver pagina publica"
- Google Search Console (futuramente): verificar que sitemap + internal linking estao sendo rastreados
</verification>

<success_criteria>
1. SEO-01 amplificado: pagina publica e descoberta via landing e via dashboard
2. Internal linking aumenta sinal de indexacao para o Google
3. Zero regressao na suite pytest
4. Links aparecem apenas para rotas com dados (evita paginas publicas 404 sendo linkadas)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/33-public-route-index-seo/33-03-SUMMARY.md` listando:
- Funcao adicionada (get_top_public_routes)
- Modificacoes nos templates (landing + dashboard/index)
- Recomendacao: apos primeiro ciclo de polling Travelpayouts + alguns pollings SerpAPI, visitar `/` e confirmar visual da secao "Rotas populares"
- Nota para monitorar primeiro organico via logs de acesso
</output>
