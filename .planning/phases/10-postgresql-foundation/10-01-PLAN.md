---
phase: 10-postgresql-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/database.py
  - requirements.txt
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/001_baseline.py
  - main.py
autonomous: true
requirements: [DB-01, DB-02]
user_setup:
  - service: neon.tech
    why: "PostgreSQL hosting persistente (free tier, sem expiracao)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon.tech Dashboard -> Connection Details -> Connection string (Direct, nao Pooled). Formato: postgresql+psycopg://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require"
    dashboard_config:
      - task: "Criar conta no Neon.tech e criar database"
        location: "https://neon.tech -> Sign Up -> Create Project -> copiar connection string"

must_haves:
  truths:
    - "database.py cria engine com connect_args condicional (check_same_thread so para SQLite)"
    - "database.py adiciona pool_pre_ping=True e pool_recycle=300 quando URL e PostgreSQL"
    - "Alembic upgrade head cria todas as 4 tabelas (route_groups, flight_snapshots, booking_class_snapshots, detected_signals) a partir de banco vazio"
    - "main.py nao chama Base.metadata.create_all() (Alembic gerencia o schema)"
  artifacts:
    - path: "app/database.py"
      provides: "Engine com connect_args condicional por dialeto"
      contains: "if settings.database_url.startswith"
    - path: "alembic.ini"
      provides: "Configuracao Alembic com script_location e sqlalchemy.url vazio"
      contains: "script_location = alembic"
    - path: "alembic/env.py"
      provides: "env.py que le DATABASE_URL do ambiente e importa Base.metadata"
      contains: "target_metadata"
    - path: "alembic/versions/001_baseline.py"
      provides: "Migration baseline com as 4 tabelas existentes"
      contains: "route_groups"
    - path: "requirements.txt"
      provides: "Dependencias psycopg[binary] e alembic adicionadas"
      contains: "psycopg"
  key_links:
    - from: "alembic/env.py"
      to: "app/database.py"
      via: "import Base metadata"
      pattern: "from app\\.database import Base"
    - from: "alembic/env.py"
      to: "os.getenv DATABASE_URL"
      via: "get_url function"
      pattern: "os\\.getenv.*DATABASE_URL"
---

<objective>
Tornar database.py agnostico ao dialeto (SQLite vs PostgreSQL), instalar dependencias, inicializar Alembic com baseline migration e remover create_all() do main.py.

Purpose: Preparar a infraestrutura para que a aplicacao rode com PostgreSQL em producao e SQLite nos testes, com migrations gerenciadas pelo Alembic.
Output: database.py condicional, Alembic inicializado com migration baseline das 4 tabelas, main.py sem create_all(), requirements.txt atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-postgresql-foundation/10-RESEARCH.md

@app/database.py
@app/config.py
@app/models.py
@main.py
@requirements.txt
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: database.py condicional + dependencias</name>
  <files>app/database.py, requirements.txt</files>
  <read_first>
    - app/database.py (estado atual com check_same_thread hardcoded)
    - app/config.py (ver settings.database_url)
    - requirements.txt (ver dependencias atuais)
  </read_first>
  <action>
1. Editar `app/database.py` para substituir o codigo atual por:

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, DeclarativeBase
from app.config import settings

connect_args = {}
engine_kwargs = {}

if settings.database_url.startswith("sqlite"):
    connect_args["check_same_thread"] = False
else:
    engine_kwargs["pool_pre_ping"] = True
    engine_kwargs["pool_recycle"] = 300

engine = create_engine(
    settings.database_url,
    connect_args=connect_args,
    **engine_kwargs,
)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


class Base(DeclarativeBase):
    pass


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

A logica: se a URL comeca com "sqlite", aplica check_same_thread=False. Caso contrario (PostgreSQL), aplica pool_pre_ping=True e pool_recycle=300 para lidar com cold starts do Neon.tech.

2. Editar `requirements.txt` para adicionar ao final:
```
psycopg[binary]==3.3.3
alembic==1.18.4
```

3. Instalar as dependencias no venv:
```bash
pip install "psycopg[binary]==3.3.3" alembic==1.18.4
```

Per DB-01: connect_args condicional garante que check_same_thread so se aplica ao SQLite, e pool_pre_ping/pool_recycle se aplicam ao PostgreSQL.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/database.py contem `if settings.database_url.startswith("sqlite"):`
    - app/database.py contem `engine_kwargs["pool_pre_ping"] = True`
    - app/database.py contem `engine_kwargs["pool_recycle"] = 300`
    - app/database.py NAO contem `connect_args={"check_same_thread": False}` hardcoded (a linha antiga)
    - requirements.txt contem `psycopg[binary]==3.3.3`
    - requirements.txt contem `alembic==1.18.4`
    - `python -m pytest tests/ -x -q` passa (todos os 188+ testes)
  </acceptance_criteria>
  <done>database.py usa connect_args condicional por dialeto, dependencias instaladas e declaradas, todos os testes passam</done>
</task>

<task type="auto">
  <name>Task 2: Alembic init + baseline migration + remover create_all</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/001_baseline.py, main.py</files>
  <read_first>
    - main.py (ver lifespan com Base.metadata.create_all)
    - app/models.py (ver os 4 models: RouteGroup, FlightSnapshot, BookingClassSnapshot, DetectedSignal)
    - app/database.py (ver Base class apos Task 1)
  </read_first>
  <action>
1. Inicializar Alembic no diretorio raiz do projeto:
```bash
alembic init alembic
```

2. Editar `alembic.ini` para deixar sqlalchemy.url vazio (sera lido do ambiente via env.py):
```ini
[alembic]
script_location = alembic
sqlalchemy.url =
```
Manter o restante do alembic.ini gerado (loggers etc).

3. Substituir `alembic/env.py` pelo conteudo abaixo. O env.py deve importar Base de app.database e todos os models de app.models (side-effect import para registrar metadata), e ler DATABASE_URL do ambiente:

```python
import os
from logging.config import fileConfig

from sqlalchemy import engine_from_config, pool, create_engine
from alembic import context

# Importar Base e models para que target_metadata tenha todas as tabelas
from app.database import Base
import app.models  # noqa: F401 — side-effect: registra models no Base.metadata

config = context.config

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata


def get_url():
    return os.getenv("DATABASE_URL", "sqlite:///./flight_monitor.db")


def run_migrations_offline():
    url = get_url()
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    url = get_url()
    connectable = create_engine(url)
    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=target_metadata,
        )
        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

4. Gerar a migration baseline com autogenerate:
```bash
alembic revision --autogenerate -m "baseline: 4 tables from v1.2"
```

Verificar que a migration gerada contem as 4 tabelas: route_groups, flight_snapshots, booking_class_snapshots, detected_signals, e o indice ix_signal_dedup.

Se o autogenerate nao funcionar corretamente (ex: DATABASE_URL aponta para SQLite com tabelas existentes), criar manualmente o arquivo de migration em `alembic/versions/` com nome `001_baseline_4_tables_from_v1_2.py` contendo os op.create_table() para as 4 tabelas.

5. Editar `main.py` para remover a linha `Base.metadata.create_all(bind=engine)` do lifespan. O lifespan fica:

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # Schema gerenciado pelo Alembic (alembic upgrade head)
    from app.scheduler import init_scheduler, shutdown_scheduler
    init_scheduler()
    yield
    shutdown_scheduler()
```

Tambem remover o import de `Base, engine` do topo do main.py (ja que create_all foi removido). Se `engine` nao for usado em nenhum outro lugar do main.py, remover o import. Se `Base` nao for usado, remover tambem. Manter apenas imports necessarios.

6. Verificar que alembic upgrade head funciona com SQLite local:
```bash
DATABASE_URL=sqlite:///./test_alembic.db alembic upgrade head
```
Verificar que as 4 tabelas foram criadas, depois limpar:
```bash
rm -f test_alembic.db
```

Per DB-02: Alembic gerencia o schema completo via migrations.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - alembic.ini existe e contem `script_location = alembic`
    - alembic.ini contem `sqlalchemy.url =` (vazio, sem credentials)
    - alembic/env.py contem `from app.database import Base`
    - alembic/env.py contem `import app.models`
    - alembic/env.py contem `target_metadata = Base.metadata`
    - alembic/env.py contem `def get_url():` com `os.getenv("DATABASE_URL"`
    - alembic/versions/ contem pelo menos 1 arquivo .py de migration
    - O arquivo de migration contem `op.create_table('route_groups'`
    - O arquivo de migration contem `op.create_table('flight_snapshots'`
    - O arquivo de migration contem `op.create_table('booking_class_snapshots'`
    - O arquivo de migration contem `op.create_table('detected_signals'`
    - main.py NAO contem `Base.metadata.create_all`
    - main.py NAO contem `from app.database import Base, engine` (ou equivalente que so servia para create_all)
    - `python -m pytest tests/ -x -q` passa (todos os 188+ testes)
  </acceptance_criteria>
  <done>Alembic inicializado com env.py lendo DATABASE_URL do ambiente, migration baseline contem as 4 tabelas, main.py nao usa create_all, todos os testes passam</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` retorna 188+ testes passando, 0 falhando
- `alembic/env.py` importa Base e models corretamente
- `DATABASE_URL=sqlite:///./verify.db alembic upgrade head && python -c "from sqlalchemy import create_engine, inspect; e=create_engine('sqlite:///./verify.db'); print(inspect(e).get_table_names())" && rm verify.db` lista as 4 tabelas
</verification>

<success_criteria>
- database.py detecta dialeto e aplica connect_args/engine_kwargs corretos
- Alembic upgrade head cria schema completo a partir de banco vazio
- main.py delegou controle de schema para Alembic (sem create_all)
- Todos os 188+ testes existentes passam sem alteracao
- requirements.txt inclui psycopg[binary] e alembic
</success_criteria>

<output>
After completion, create `.planning/phases/10-postgresql-foundation/10-01-SUMMARY.md`
</output>
