---
phase: 10-postgresql-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - render.yaml
  - .env.example
autonomous: false
requirements: [DB-01, DB-03]

must_haves:
  truths:
    - "render.yaml usa DATABASE_URL sync:false (configurada no dashboard do Render com connection string do Neon.tech)"
    - "render.yaml roda alembic upgrade head no buildCommand antes do gunicorn"
    - ".env.example documenta DATABASE_URL com exemplo de formato PostgreSQL"
    - "Todos os 188+ testes existentes passam com SQLite in-memory sem nenhuma alteracao nos testes"
  artifacts:
    - path: "render.yaml"
      provides: "Build command com alembic upgrade head + DATABASE_URL como env var sync:false"
      contains: "alembic upgrade head"
    - path: ".env.example"
      provides: "Template de configuracao com DATABASE_URL documentado"
      contains: "DATABASE_URL"
  key_links:
    - from: "render.yaml buildCommand"
      to: "alembic/env.py"
      via: "alembic upgrade head le DATABASE_URL do ambiente"
      pattern: "alembic upgrade head"
    - from: "render.yaml envVars DATABASE_URL"
      to: "app/config.py settings.database_url"
      via: "pydantic-settings le env var DATABASE_URL"
      pattern: "DATABASE_URL"
---

<objective>
Atualizar configuracao de deploy (render.yaml) para rodar migrations no build e usar PostgreSQL via env var, atualizar .env.example, e validar que a suite completa de testes continua passando.

Purpose: Completar a configuracao de producao para que o proximo deploy no Render use PostgreSQL (Neon.tech) com migrations automaticas, e confirmar que nenhum teste quebrou.
Output: render.yaml pronto para deploy com PostgreSQL, .env.example atualizado, suite de regressao verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-postgresql-foundation/10-RESEARCH.md
@.planning/phases/10-postgresql-foundation/10-01-SUMMARY.md

@render.yaml
@.env.example
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: render.yaml + .env.example para PostgreSQL</name>
  <files>render.yaml, .env.example</files>
  <read_first>
    - render.yaml (estado atual com buildCommand sem alembic e DATABASE_URL com valor sqlite)
    - .env.example (estado atual sem exemplo PostgreSQL)
  </read_first>
  <action>
1. Editar `render.yaml` para:
   - Mudar `buildCommand` de `pip install -r requirements.txt` para `pip install -r requirements.txt && alembic upgrade head`
   - Mudar a env var `DATABASE_URL` de `value: sqlite:///./flight_monitor.db` para `sync: false` (sera configurada manualmente no dashboard do Render com a connection string do Neon.tech)

O render.yaml final deve ficar:
```yaml
services:
  - type: web
    name: flight-monitor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && alembic upgrade head
    startCommand: gunicorn main:app -w 1 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.12"
      - key: DATABASE_URL
        sync: false
      - key: SERPAPI_API_KEY
        sync: false
      - key: GMAIL_SENDER
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: GMAIL_RECIPIENT
        sync: false
```

2. Editar `.env.example` para documentar o formato da DATABASE_URL:
```
# SQLite (desenvolvimento local):
DATABASE_URL=sqlite:///./flight_monitor.db

# PostgreSQL (producao via Neon.tech):
# DATABASE_URL=postgresql+psycopg://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

SERPAPI_API_KEY=
GMAIL_SENDER=
GMAIL_APP_PASSWORD=
GMAIL_RECIPIENT=
```

Per DB-01: render.yaml configura deploy com PostgreSQL. Per DB-03: nenhuma alteracao nos testes.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - render.yaml contem `buildCommand: pip install -r requirements.txt && alembic upgrade head`
    - render.yaml contem `key: DATABASE_URL` seguido de `sync: false` (sem `value:` hardcoded)
    - render.yaml NAO contem `value: sqlite:///./flight_monitor.db`
    - .env.example contem `DATABASE_URL=sqlite:///./flight_monitor.db`
    - .env.example contem `postgresql+psycopg://` como exemplo comentado
    - `python -m pytest tests/ -v` passa com 188+ testes, 0 falhas
  </acceptance_criteria>
  <done>render.yaml pronto para deploy com alembic upgrade head no build e DATABASE_URL como env var secreta, .env.example documenta ambos os formatos, todos os testes passam</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint de validacao da fase</name>
  <files>N/A</files>
  <action>
Apresentar ao usuario o resultado completo da fase para validacao manual.

Infraestrutura construida:
1. database.py com connect_args condicional (SQLite vs PostgreSQL)
2. Alembic inicializado com migration baseline das 4 tabelas
3. main.py sem create_all() (Alembic gerencia schema)
4. render.yaml com alembic upgrade head no buildCommand
5. requirements.txt com psycopg[binary] e alembic
6. .env.example com exemplos de DATABASE_URL

Passos para o usuario verificar:
1. Rodar `python -m pytest tests/ -v` e confirmar que todos os 188+ testes passam
2. Rodar `DATABASE_URL=sqlite:///./verify.db alembic upgrade head` e confirmar saida sem erros
3. Verificar tabelas criadas: `python -c "from sqlalchemy import create_engine, inspect; e=create_engine('sqlite:///./verify.db'); print(sorted(inspect(e).get_table_names()))"`
   Esperado: ['alembic_version', 'booking_class_snapshots', 'detected_signals', 'flight_snapshots', 'route_groups']
4. Limpar: `rm verify.db`
5. Revisar render.yaml: buildCommand inclui `alembic upgrade head`, DATABASE_URL e sync:false
6. (Opcional) Se tiver conta Neon.tech: setar DATABASE_URL com connection string do Neon e rodar `alembic upgrade head`

Aguardar resposta: "aprovado" ou descricao de problemas.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
  </verify>
  <done>Usuario aprovou a fase: todos os testes passam, alembic upgrade head funciona, render.yaml configurado para deploy com PostgreSQL</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v` retorna 188+ testes passando, 0 falhando (DB-03)
- `DATABASE_URL=sqlite:///./verify.db alembic upgrade head` cria schema completo (DB-02)
- render.yaml configurado para deploy com PostgreSQL (DB-01)
</verification>

<success_criteria>
- render.yaml roda alembic upgrade head no build e usa DATABASE_URL como env var secreta
- .env.example documenta formato SQLite (dev) e PostgreSQL (producao)
- Suite completa de regressao (188+ testes) passa sem nenhuma alteracao nos testes
- Infraestrutura pronta para deploy com PostgreSQL no Neon.tech
</success_criteria>

<output>
After completion, create `.planning/phases/10-postgresql-foundation/10-02-SUMMARY.md`
</output>
