---
phase: 09-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/templates/dashboard/detail.html
  - app/templates/dashboard/create.html
  - app/templates/dashboard/edit.html
autonomous: false
requirements: [VIS-01, VIS-06]

must_haves:
  truths:
    - "Detail page usa chart color #0ea5e9 e link Voltar com mesma cor"
    - "Formularios create/edit usam botao verde #22c55e consistente com o resto"
    - "Todas as paginas secundarias seguem a mesma paleta visual do index"
  artifacts:
    - path: "app/templates/dashboard/detail.html"
      provides: "Pagina de detalhe com cores do UI-SPEC"
      contains: "#0ea5e9"
    - path: "app/templates/dashboard/create.html"
      provides: "Formulario criar com botao verde atualizado"
      contains: "#22c55e"
    - path: "app/templates/dashboard/edit.html"
      provides: "Formulario editar com botao verde atualizado"
      contains: "#22c55e"
  key_links:
    - from: "app/templates/dashboard/detail.html"
      to: "app/templates/base.html"
      via: "Jinja2 extends"
      pattern: "extends.*base.html"
---

<objective>
Aplicar o UI-SPEC nas paginas secundarias (detail, create, edit) e fazer verificacao visual humana do dashboard completo.

Purpose: Garantir consistencia visual em todas as paginas e validar o resultado final com o usuario.
Output: Todas as paginas do dashboard seguindo o UI-SPEC + aprovacao visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-polish/09-CONTEXT.md
@.planning/phases/08-dashboard-redesign/08-UI-SPEC.md
@.planning/phases/09-visual-polish/09-01-SUMMARY.md
@app/templates/dashboard/detail.html
@app/templates/dashboard/create.html
@app/templates/dashboard/edit.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar detail.html, create.html e edit.html conforme UI-SPEC</name>
  <files>app/templates/dashboard/detail.html, app/templates/dashboard/create.html, app/templates/dashboard/edit.html</files>
  <read_first>
    - .planning/phases/08-dashboard-redesign/08-UI-SPEC.md (FONTE DE VERDADE - Detail Page Contract, Color table)
    - app/templates/dashboard/detail.html
    - app/templates/dashboard/create.html
    - app/templates/dashboard/edit.html
  </read_first>
  <action>
**detail.html (per D-01, UI-SPEC Detail Page Contract):**
1. Link "Voltar" color: trocar `#3b82f6` por `#0ea5e9` (UI-SPEC Accent Primary)
2. Chart.js borderColor: trocar `'#3b82f6'` por `'#0ea5e9'` (UI-SPEC Detail Page Contract)
3. Chart.js backgroundColor: trocar `'rgba(59, 130, 246, 0.1)'` por `'rgba(14, 165, 233, 0.1)'` (UI-SPEC Detail Page Contract)
4. h1 style: adicionar `font-size: 1.25rem; font-weight: 700` (20px Heading per UI-SPEC Typography)
5. NAO alterar Chart.js config, labels, datasets structure - apenas valores de cor

**create.html (per D-01):**
1. Botao submit background: trocar `#059669` por `#22c55e` (UI-SPEC Accent Success)
2. Botao submit font-weight: trocar 600 por 700 (UI-SPEC Button text)
3. Botao submit: adicionar `min-height: 40px` para consistencia com CTA do index
4. h1 style: adicionar `font-size: 1.25rem; font-weight: 700` se nao tiver (20px Heading)

**edit.html (per D-01):**
1. Botao submit background: trocar `#059669` por `#22c55e` (UI-SPEC Accent Success)
2. Botao submit font-weight: trocar 600 por 700
3. Botao submit: adicionar `min-height: 40px`
4. h1 style: adicionar `font-size: 1.25rem; font-weight: 700` se nao tiver

NAO alterar JavaScript (airport search, Chart.js logic). NAO alterar estrutura HTML. Apenas valores CSS inline e no bloco style.
  </action>
  <verify>
    <automated>grep -c "#0ea5e9" app/templates/dashboard/detail.html && grep -c "rgba(14, 165, 233" app/templates/dashboard/detail.html && grep -c "#22c55e" app/templates/dashboard/create.html && grep -c "#22c55e" app/templates/dashboard/edit.html && grep -c "#3b82f6" app/templates/dashboard/detail.html | head -1 && grep -c "#059669" app/templates/dashboard/create.html | head -1 && grep -c "#059669" app/templates/dashboard/edit.html | head -1 && python -m pytest tests/ -x --timeout=30 -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "#0ea5e9" detail.html retorna matches (link Voltar + chart line)
    - grep "rgba(14, 165, 233" detail.html retorna match (chart fill)
    - grep "#3b82f6" detail.html retorna 0 matches (cor antiga removida)
    - grep "#22c55e" create.html retorna match (botao submit)
    - grep "#059669" create.html retorna 0 matches (cor antiga removida)
    - grep "#22c55e" edit.html retorna match (botao submit)
    - grep "#059669" edit.html retorna 0 matches (cor antiga removida)
    - Todos os testes existentes passam
  </acceptance_criteria>
  <done>detail.html usa #0ea5e9 no chart e link Voltar, create.html e edit.html usam #22c55e nos botoes. Nenhuma cor antiga permanece.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual do dashboard completo</name>
  <what-built>
    Visual polish completo do dashboard conforme UI-SPEC aprovado:
    - Paleta de cores padronizada (#f8fafc fundo, sky blue accent, verde/amarelo/vermelho semanticos)
    - Precos em monospace 28px bold como elemento focal dos cards
    - Cards com hover suave, footer com separador visivel, inativos com opacidade 0.6
    - Summary bar escura com metricas em destaque
    - Estado vazio com SVG de aviao e CTA verde
    - Paginas secundarias (detail, create, edit) com cores consistentes
  </what-built>
  <how-to-verify>
    1. Iniciar servidor: `python main.py` (ou `uvicorn app.main:app --reload`)
    2. Acessar http://localhost:8000/
    3. Verificar na pagina principal:
       - Fundo da pagina e cinza muito claro (#f8fafc), nao branco puro
       - Summary bar e escura (#1e293b) com numeros em branco 20px bold e labels em cinza
       - Cards tem borda esquerda colorida (verde/amarelo/vermelho por classificacao)
       - Preco em cada card e grande (28px), monospace, com cor correspondente
       - Ao passar mouse no card, sombra aumenta suavemente em 0.2s
       - Footer do card tem linha separadora visivel
       - Se houver grupo inativo, aparece com transparencia reduzida
    4. Se nao houver grupos, verificar estado vazio:
       - Icone de aviao SVG centralizado
       - Texto "Nenhum grupo cadastrado. Comece monitorando sua primeira rota!" em 14px
       - Botao verde "Criar primeiro grupo" com boa area de clique
    5. Clicar em um grupo para ver detail page:
       - Link "Voltar" em azul sky (#0ea5e9)
       - Grafico com linha azul sky (nao azul escuro)
    6. Clicar em "Novo Grupo" ou "Editar":
       - Botao submit e verde (#22c55e), consistente com o CTA
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. grep -r "#059669" app/templates/ deve retornar 0 matches (cor antiga eliminada de TODOS os templates)
2. grep -r "#3b82f6" app/templates/ deve retornar 0 matches (azul antigo eliminado)
3. grep -r "#d97706" app/templates/ deve retornar 0 matches (amarelo antigo eliminado)
4. grep -r "#dc2626" app/templates/ deve retornar 0 matches (vermelho antigo eliminado)
5. python -m pytest tests/ -x --timeout=30 deve passar 100%
</verification>

<success_criteria>
- Todas as 5 paginas do dashboard (index, detail, create, edit, error) seguem a paleta do UI-SPEC
- Cores antigas (#059669, #3b82f6, #d97706, #dc2626) completamente eliminadas dos templates
- Chart.js usa sky blue #0ea5e9 como cor principal
- Botoes de acao usam verde #22c55e consistente
- Usuario aprovou visualmente o resultado final
- Todos os testes continuam passando (per D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-polish/09-02-SUMMARY.md`
</output>
