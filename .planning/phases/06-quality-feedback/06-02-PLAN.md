---
phase: 06-quality-feedback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routes/dashboard.py
  - app/templates/base.html
  - app/templates/dashboard/index.html
  - app/templates/error.html
  - main.py
  - tests/test_dashboard_feedback.py
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "Apos criar grupo, usuario ve mensagem 'Grupo criado com sucesso!' na tela"
    - "Apos editar grupo, usuario ve mensagem 'Grupo atualizado com sucesso!' na tela"
    - "Apos ativar/desativar grupo, usuario ve mensagem correspondente na tela"
    - "Mensagem de feedback desaparece apos 5 segundos com animacao fade-out"
    - "Quando ocorre erro 500, usuario ve pagina amigavel com botao 'Voltar ao inicio'"
    - "Pagina de erro nao expoe stack trace ou detalhes tecnicos"
  artifacts:
    - path: "app/templates/error.html"
      provides: "Template de pagina de erro amigavel"
      contains: "Voltar ao inicio"
    - path: "app/templates/base.html"
      provides: "Banner de feedback com CSS fade-out"
      contains: "flash-message"
    - path: "main.py"
      provides: "Exception handler global registrado no FastAPI"
      contains: "exception_handler"
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/templates/dashboard/index.html"
      via: "Query param ?msg= no redirect, template renderiza mensagem"
      pattern: "msg="
    - from: "main.py"
      to: "app/templates/error.html"
      via: "Exception handler global renderiza error.html"
      pattern: "exception_handler"
---

<objective>
Implementar feedback visual para o usuario: flash messages apos acoes de CRUD de grupos (UX-01) e pagina de erro amigavel para erros inesperados (UX-02).

Purpose: Usuario nunca fica sem feedback apos uma acao e nunca ve "Internal Server Error" generico.
Output: Flash messages funcionando em todos os redirects de formulario + exception handler global com template de erro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quality-feedback/06-CONTEXT.md

@app/routes/dashboard.py
@app/templates/base.html
@app/templates/dashboard/index.html
@main.py
@tests/conftest.py

<interfaces>
From app/routes/dashboard.py:
```python
# Redirects atuais (sem ?msg=):
# create_group_form -> RedirectResponse(url="/", status_code=303)
# edit_group_form -> RedirectResponse(url="/", status_code=303)
# toggle_group -> RedirectResponse(url="/", status_code=303)

@router.get("/", response_class=HTMLResponse)
def dashboard_index(request: Request, db: Session = Depends(get_db)):
    groups = get_groups_with_summary(db)
    return templates.TemplateResponse(
        request=request,
        name="dashboard/index.html",
        context={"groups": groups, "format_price_brl": format_price_brl},
    )
```

From main.py:
```python
app = FastAPI(title="Flight Monitor", lifespan=lifespan)
# Sem exception handlers registrados
```

From app/templates/base.html:
```html
<div class="container">
    {% block content %}{% endblock %}
</div>
<!-- Sem bloco de mensagens de feedback -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flash messages via query param nos redirects</name>
  <files>app/routes/dashboard.py, app/templates/base.html, app/templates/dashboard/index.html</files>
  <read_first>
    - app/routes/dashboard.py
    - app/templates/base.html
    - app/templates/dashboard/index.html
    - tests/conftest.py
  </read_first>
  <action>
    Per D-03, D-04 e D-05:

    1. Em app/routes/dashboard.py, alterar os 3 redirects para incluir query param ?msg=:
       - create_group_form: RedirectResponse(url="/?msg=grupo_criado", status_code=303)
       - edit_group_form: RedirectResponse(url="/?msg=grupo_atualizado", status_code=303)
       - toggle_group: RedirectResponse(url=f"/?msg=grupo_{'ativado' if not group.is_active else 'desativado'}", status_code=303)
         ATENCAO: o toggle inverte is_active ANTES do redirect, entao verificar o estado APOS a inversao: se group.is_active (ja invertido) -> "ativado", senao -> "desativado".

    2. Em app/routes/dashboard.py, no endpoint dashboard_index, extrair query param msg da request:
       - Adicionar parametro msg: str | None = None ao dashboard_index
       - Criar dicionario FLASH_MESSAGES com mapeamento: {"grupo_criado": "Grupo criado com sucesso!", "grupo_atualizado": "Grupo atualizado com sucesso!", "grupo_ativado": "Grupo ativado com sucesso!", "grupo_desativado": "Grupo desativado com sucesso!"}
       - Passar flash_message=FLASH_MESSAGES.get(msg) no context do template

    3. Em app/templates/base.html, adicionar bloco de mensagem ANTES do block content, dentro de .container:
       ```html
       {% if flash_message %}
       <div class="flash-message" style="background: #059669; color: white; padding: 0.75rem 1.5rem; border-radius: 6px; margin-bottom: 1rem; font-weight: 500; animation: fadeOut 5s forwards;">
           {{ flash_message }}
       </div>
       {% endif %}
       ```
       Adicionar no bloco style do head:
       ```css
       @keyframes fadeOut {
           0%, 80% { opacity: 1; }
           100% { opacity: 0; display: none; }
       }
       .flash-message { animation: fadeOut 5s forwards; }
       ```

    4. Criar tests/test_dashboard_feedback.py com testes:
       - test_create_group_redirects_with_msg: POST /groups/create com dados validos retorna 303 com Location contendo "?msg=grupo_criado"
       - test_edit_group_redirects_with_msg: POST /groups/{id}/edit retorna 303 com Location contendo "?msg=grupo_atualizado"
       - test_toggle_group_redirects_with_msg: POST /groups/{id}/toggle retorna 303 com Location contendo "?msg=grupo_"
       - test_dashboard_shows_flash_message: GET /?msg=grupo_criado retorna HTML contendo "Grupo criado com sucesso!"
       - test_dashboard_no_flash_without_param: GET / sem param nao contem "flash-message"
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_dashboard_feedback.py -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "msg=grupo_criado" app/routes/dashboard.py
    - grep -q "msg=grupo_atualizado" app/routes/dashboard.py
    - grep -q "msg=grupo_" app/routes/dashboard.py (para toggle)
    - grep -q "flash_message" app/templates/base.html
    - grep -q "fadeOut" app/templates/base.html
    - grep -q "FLASH_MESSAGES" app/routes/dashboard.py
    - python -m pytest tests/test_dashboard_feedback.py retorna 0 exit code
  </acceptance_criteria>
  <done>
    - Criar grupo redireciona com ?msg=grupo_criado e mensagem aparece na tela
    - Editar grupo redireciona com ?msg=grupo_atualizado e mensagem aparece na tela
    - Toggle grupo redireciona com ?msg=grupo_ativado ou ?msg=grupo_desativado
    - Mensagem verde com fade-out CSS de 5 segundos
    - 5 testes passando
  </done>
</task>

<task type="auto">
  <name>Task 2: Pagina de erro amigavel e exception handler global</name>
  <files>app/templates/error.html, main.py, tests/test_dashboard_feedback.py</files>
  <read_first>
    - main.py
    - app/templates/base.html
    - app/routes/dashboard.py
    - tests/conftest.py
  </read_first>
  <action>
    Per D-06, D-07 e D-08:

    1. Criar app/templates/error.html que herda de base.html:
       ```html
       {% extends "base.html" %}
       {% block title %}Erro - Flight Monitor{% endblock %}
       {% block content %}
       <div style="text-align: center; padding: 3rem 1rem;">
           <h1 style="font-size: 3rem; color: #94a3b8; margin-bottom: 1rem;">Ops!</h1>
           <p style="font-size: 1.25rem; color: #64748b; margin-bottom: 0.5rem;">{{ message }}</p>
           <p style="color: #94a3b8; margin-bottom: 2rem;">{{ detail }}</p>
           <a href="/" style="display: inline-block; background: #1e293b; color: white; padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; font-weight: 500;">Voltar ao inicio</a>
       </div>
       {% endblock %}
       ```
       Mensagem default para 500: "Algo deu errado no servidor." / Detail: "Tente novamente em alguns instantes."
       Mensagem para 404: "Pagina nao encontrada." / Detail: "O endereco que voce acessou nao existe."
       NAO expor exception, stack trace ou detalhes tecnicos. Per D-06.

    2. Em main.py, registrar exception handlers APOS a criacao do app e APOS include_router:
       - Importar Request, Jinja2Templates de fastapi
       - Criar templates = Jinja2Templates(directory="app/templates")
       - Registrar @app.exception_handler(HTTPException) que renderiza error.html com status_code e mensagem customizada para 404 e mensagem generica para outros codigos. Per D-07.
       - Registrar @app.exception_handler(Exception) que renderiza error.html com status 500 e mensagem "Algo deu errado no servidor." Per D-07.
       - Ambos devem logar o erro real via logger.error para debugging.

    3. Atualizar as respostas 404 inline em dashboard.py (dashboard_detail, edit_group_page, edit_group_form, toggle_group) para usar raise HTTPException(status_code=404, detail="Grupo nao encontrado") ao inves de retornar HTMLResponse direto. Isso permite que o exception handler global capture e renderize error.html. Per D-08.

    4. Adicionar testes em tests/test_dashboard_feedback.py:
       - test_404_shows_friendly_error: GET /groups/99999 retorna 404 com HTML contendo "Pagina nao encontrada" e "Voltar ao inicio"
       - test_error_page_no_stack_trace: GET /groups/99999 retorna HTML que NAO contem "Traceback" nem "Error"
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_dashboard_feedback.py -v && python -m pytest tests/test_dashboard.py -v</automated>
  </verify>
  <acceptance_criteria>
    - test -f app/templates/error.html
    - grep -q "Voltar ao inicio" app/templates/error.html
    - grep -q "exception_handler" main.py
    - grep -q "HTTPException" main.py
    - grep -q "error.html" main.py
    - grep -q "raise HTTPException" app/routes/dashboard.py
    - python -m pytest tests/test_dashboard_feedback.py retorna 0 exit code
    - python -m pytest tests/test_dashboard.py retorna 0 exit code (testes existentes nao quebram)
  </acceptance_criteria>
  <done>
    - Template error.html existe com mensagem amigavel e botao "Voltar ao inicio"
    - Exception handler global captura erros 500 e renderiza error.html
    - 404 em dashboard usa HTTPException e mostra pagina amigavel
    - Pagina de erro nao expoe detalhes tecnicos
    - Testes novos passando, testes existentes nao quebram
  </done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_dashboard_feedback.py -v (todos passam)
- python -m pytest tests/test_dashboard.py -v (testes existentes nao quebram)
- GET / sem ?msg= nao mostra flash message
- GET /?msg=grupo_criado mostra "Grupo criado com sucesso!"
- GET /groups/99999 mostra pagina de erro amigavel, nao "Internal Server Error"
</verification>

<success_criteria>
1. Todas as acoes de CRUD de grupo (criar, editar, toggle) mostram mensagem de confirmacao verde que desaparece em 5 segundos.
2. Erros inesperados mostram pagina amigavel com botao de voltar, sem expor detalhes tecnicos.
</success_criteria>

<output>
After completion, create `.planning/phases/06-quality-feedback/06-02-SUMMARY.md`
</output>
