---
phase: 06-quality-feedback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/services/snapshot_service.py
  - app/services/polling_service.py
  - tests/test_snapshot_dedup.py
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - "Polling nunca salva o mesmo voo duas vezes no mesmo ciclo de coleta"
    - "Snapshots com rota+data+preco+airline identicos dentro de 1 hora sao ignorados"
    - "Snapshots diferentes (preco diferente, airline diferente) sao salvos normalmente"
  artifacts:
    - path: "app/services/snapshot_service.py"
      provides: "Funcao is_duplicate_snapshot que verifica duplicidade antes de salvar"
      contains: "is_duplicate_snapshot"
    - path: "tests/test_snapshot_dedup.py"
      provides: "Testes de deduplicacao de snapshots"
      contains: "test_duplicate_snapshot_not_saved"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/snapshot_service.py"
      via: "Chamada a is_duplicate_snapshot antes de save_flight_snapshot"
      pattern: "is_duplicate_snapshot"
---

<objective>
Corrigir bug FIX-01: polling salva snapshots duplicados quando o mesmo voo aparece mais de uma vez no resultado da API dentro do mesmo ciclo de coleta.

Purpose: Evitar dados duplicados no banco que distorcem historico de precos e sinais de compra.
Output: Funcao de deduplicacao testada (TDD) integrada ao fluxo de polling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quality-feedback/06-CONTEXT.md

@app/services/snapshot_service.py
@app/services/polling_service.py
@app/models.py
@tests/test_polling_service.py
@tests/conftest.py

<interfaces>
From app/models.py:
```python
class FlightSnapshot(Base):
    __tablename__ = "flight_snapshots"
    id: Mapped[int]
    route_group_id: Mapped[int]
    origin: Mapped[str]          # String(3)
    destination: Mapped[str]     # String(3)
    departure_date: Mapped[datetime.date]
    return_date: Mapped[datetime.date]
    price: Mapped[float]
    currency: Mapped[str]        # String(3)
    airline: Mapped[str]         # String(2)
    collected_at: Mapped[datetime.datetime]  # server_default=func.now()
```

From app/services/snapshot_service.py:
```python
def save_flight_snapshot(db: Session, data: dict) -> FlightSnapshot:
```

From app/services/polling_service.py:
```python
def _process_flight(db, group, origin, destination, dep_date, ret_date, flight, insights):
    # Chama save_flight_snapshot sem verificacao de duplicidade
    snapshot = save_flight_snapshot(db, snapshot_data)
```
</interfaces>
</context>

<feature>
  <name>Deduplicacao de snapshots no polling</name>
  <files>app/services/snapshot_service.py, tests/test_snapshot_dedup.py, app/services/polling_service.py</files>
  <behavior>
    - is_duplicate_snapshot(db, route_group_id, origin, destination, departure_date, return_date, price, airline) retorna True se ja existe snapshot com esses campos e collected_at dentro de 1 hora
    - is_duplicate_snapshot retorna False se nenhum snapshot existe com esses campos
    - is_duplicate_snapshot retorna False se snapshot existe mas collected_at foi ha mais de 1 hora (ciclo diferente)
    - is_duplicate_snapshot retorna False se preco diferente (mesmo voo, preco mudou)
    - is_duplicate_snapshot retorna False se airline diferente
    - _process_flight nao chama save_flight_snapshot quando is_duplicate_snapshot retorna True
    - _process_flight chama save_flight_snapshot normalmente quando is_duplicate_snapshot retorna False
  </behavior>
  <implementation>
    1. Criar funcao is_duplicate_snapshot em snapshot_service.py que faz query no banco: SELECT 1 FROM flight_snapshots WHERE route_group_id=X AND origin=Y AND destination=Z AND departure_date=D1 AND return_date=D2 AND price=P AND airline=A AND collected_at >= (now - 1 hora). Per D-01 e D-02.
    2. Em polling_service.py, na funcao _process_flight, antes de chamar save_flight_snapshot, chamar is_duplicate_snapshot. Se retornar True, logar "Duplicate snapshot skipped" e fazer return sem salvar. Per D-01.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD deduplicacao de snapshots</name>
  <files>app/services/snapshot_service.py, tests/test_snapshot_dedup.py, app/services/polling_service.py</files>
  <read_first>
    - app/services/snapshot_service.py
    - app/services/polling_service.py
    - app/models.py
    - tests/conftest.py
    - tests/test_polling_service.py
  </read_first>
  <behavior>
    - Test 1: is_duplicate_snapshot retorna True quando snapshot identico existe com collected_at recente (< 1 hora)
    - Test 2: is_duplicate_snapshot retorna False quando nenhum snapshot existe
    - Test 3: is_duplicate_snapshot retorna False quando snapshot identico existe mas collected_at > 1 hora atras
    - Test 4: is_duplicate_snapshot retorna False quando preco e diferente (mesmo voo, preco mudou)
    - Test 5: is_duplicate_snapshot retorna False quando airline e diferente
    - Test 6: _process_flight pula save quando duplicata detectada
    - Test 7: _process_flight salva normalmente quando nao e duplicata
  </behavior>
  <action>
    RED: Criar tests/test_snapshot_dedup.py com os 7 testes acima. Usar o fixture db do conftest.py. Para testes 1-5, criar FlightSnapshot manualmente no banco e chamar is_duplicate_snapshot. Para testes 6-7, usar patch em is_duplicate_snapshot e verificar se save_flight_snapshot e chamado. Rodar pytest, confirmar todos falhando.

    GREEN:
    1. Em app/services/snapshot_service.py, adicionar funcao is_duplicate_snapshot(db, route_group_id, origin, destination, departure_date, return_date, price, airline) que faz query: db.query(FlightSnapshot).filter(...).filter(FlightSnapshot.collected_at >= datetime.utcnow() - timedelta(hours=1)).first() is not None. Per D-01 e D-02.
    2. Em app/services/polling_service.py, na funcao _process_flight, importar is_duplicate_snapshot. Antes da linha "snapshot = save_flight_snapshot(db, snapshot_data)", adicionar check: if is_duplicate_snapshot(db, group.id, origin, destination, dep_date, ret_date, price, airline): logger.debug("Duplicate snapshot skipped..."); return. Per D-01.
    3. Rodar pytest, confirmar todos passando.

    REFACTOR: Verificar se ha duplicacao de logica, nomes claros, sem codigo morto.
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_snapshot_dedup.py -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "is_duplicate_snapshot" app/services/snapshot_service.py
    - grep -q "is_duplicate_snapshot" app/services/polling_service.py
    - grep -q "test_duplicate_snapshot" tests/test_snapshot_dedup.py
    - python -m pytest tests/test_snapshot_dedup.py retorna 0 exit code
    - python -m pytest tests/test_polling_service.py retorna 0 exit code (testes existentes nao quebram)
  </acceptance_criteria>
  <done>
    - is_duplicate_snapshot existe e impede snapshots duplicados dentro de 1 hora
    - _process_flight chama is_duplicate_snapshot antes de salvar
    - 7 testes novos passando, testes existentes do polling nao quebram
  </done>
</task>

</tasks>

<verification>
- pytest tests/test_snapshot_dedup.py -v (todos passam)
- pytest tests/test_polling_service.py -v (testes existentes nao quebram)
- grep "is_duplicate_snapshot" app/services/polling_service.py (funcao esta sendo chamada)
</verification>

<success_criteria>
Polling nunca salva snapshot duplicado (mesma rota+data+preco+airline) dentro do mesmo ciclo de coleta. Testes comprovam comportamento.
</success_criteria>

<output>
After completion, create `.planning/phases/06-quality-feedback/06-01-SUMMARY.md`
</output>
