---
phase: 08-dashboard-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - app/templates/dashboard/detail.html
autonomous: false
requirements:
  - UI-05

must_haves:
  truths:
    - "Datas na pagina de detalhe do grupo aparecem no formato dd/mm/aaaa"
    - "Dashboard redesenhado e visualmente correto no navegador"
  artifacts:
    - path: "app/templates/dashboard/detail.html"
      provides: "Detail page with Brazilian date format"
      contains: "format_date_br"
  key_links:
    - from: "app/templates/dashboard/detail.html"
      to: "app/routes/dashboard.py"
      via: "format_date_br passed in template context"
      pattern: "format_date_br"
---

<objective>
Atualizar pagina de detalhe com datas em formato brasileiro e verificar visualmente o dashboard redesenhado.

Purpose: Completar UI-05 (datas dd/mm/aaaa em todas as paginas) e validar visualmente o redesign.
Output: Detail page com datas formatadas + confirmacao visual do dashboard.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-redesign/08-CONTEXT.md
@.planning/phases/08-dashboard-redesign/08-01-SUMMARY.md

@app/templates/dashboard/detail.html
@app/routes/dashboard.py
@app/services/dashboard_service.py

<interfaces>
From app/services/dashboard_service.py (after Plan 01):
```python
def format_date_br(d: datetime.date | None) -> str:
    # Returns d.strftime("%d/%m/%Y") or "" if None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update detail.html dates to dd/mm/aaaa format</name>
  <files>app/templates/dashboard/detail.html, app/routes/dashboard.py</files>
  <read_first>
    - app/templates/dashboard/detail.html
    - app/routes/dashboard.py
    - app/services/dashboard_service.py
  </read_first>
  <action>
    1. In app/routes/dashboard.py `dashboard_detail` function, add `format_date_br` to the template context (import it if not already imported from dashboard_service).

    2. In app/templates/dashboard/detail.html, find the line showing "Periodo:" which currently outputs raw dates:
       `{{ group.travel_start }} a {{ group.travel_end }}`
       Replace with:
       `{{ format_date_br(group.travel_start) }} a {{ format_date_br(group.travel_end) }}`

    3. Verify no other raw date outputs remain in detail.html. The chart labels already use dd/mm format from dashboard_service.get_price_history (strftime "%d/%m %Hh"), so those are fine.

    Per D-11: ALL dates in the dashboard must use dd/mm/aaaa format.
  </action>
  <verify>
    <automated>cd "C:\Users\GustavoBrandão\OneDrive\Área de Trabalho\projetos pessoais\flight-monitor" && python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('app/templates')); t = env.get_template('dashboard/detail.html'); print('Template compiles OK')" && grep -c "format_date_br" app/templates/dashboard/detail.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "format_date_br" app/templates/dashboard/detail.html
    - grep -q "format_date_br" app/routes/dashboard.py
  </acceptance_criteria>
  <done>
    - detail.html uses format_date_br for travel_start and travel_end
    - dashboard_detail route passes format_date_br in context
    - No raw date format remains in detail.html
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard redesign</name>
  <files>app/templates/dashboard/index.html, app/templates/dashboard/detail.html</files>
  <action>
    Human visually inspects the dashboard redesign in the browser. No code changes needed.
  </action>
  <what-built>
    Dashboard redesenhado com:
    - Area de resumo escura no topo (grupos ativos, menor preco, proximo polling)
    - Cards com borda colorida por classificacao (verde/amarelo/vermelho/cinza)
    - Conteudo do card: nome, preco grande, rota, companhia, datas dd/mm/aaaa, badge de sinal
    - Estado vazio com icone de aviao e botao verde
    - Datas em dd/mm/aaaa na pagina de detalhe
  </what-built>
  <how-to-verify>
    1. Iniciar a aplicacao: `python main.py`
    2. Abrir http://localhost:8000/ no navegador
    3. Verificar:
       a. Barra de resumo escura no topo com 3 metricas
       b. Cards com bordas coloridas a esquerda
       c. Preco em destaque grande dentro de cada card
       d. Datas no formato dd/mm/aaaa nos cards
       e. Badges de sinal nos cards (se houver sinais)
    4. Se nao houver grupos: verificar estado vazio com icone de aviao e botao "Criar primeiro grupo"
    5. Clicar em um grupo -> verificar datas dd/mm/aaaa na pagina de detalhe
    6. Redimensionar para mobile -> verificar que summary bar empilha e cards ficam em coluna unica
  </how-to-verify>
  <verify>
    <automated>cd "C:\Users\GustavoBrandão\OneDrive\Área de Trabalho\projetos pessoais\flight-monitor" && python -c "import app; print('App imports OK')"</automated>
  </verify>
  <done>User confirms dashboard redesign is visually correct and functional</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All dates across dashboard (index + detail) use dd/mm/aaaa format
2. Visual layout matches decisions D-01 through D-11
3. Application runs without errors
</verification>

<success_criteria>
- detail.html dates formatted as dd/mm/aaaa
- User visually confirms dashboard redesign matches expectations
- No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-redesign/08-02-SUMMARY.md`
</output>
