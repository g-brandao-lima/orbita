---
phase: 12-data-isolation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/services/alert_service.py
  - app/services/polling_service.py
  - app/routes/dashboard.py
  - app/templates/dashboard/alerts.html
  - tests/test_alert_service.py
  - tests/test_alerts_page.py
autonomous: true
requirements: [MULTI-02, MULTI-04]

must_haves:
  truths:
    - "Email de alerta e enviado para o email do Google do dono do grupo, nao para gmail_recipient fixo"
    - "Polling service carrega user.email via relationship ao enviar email"
    - "Usuario pode acessar pagina Meus Alertas e ver apenas sinais dos seus grupos"
    - "Pagina Meus Alertas lista sinais com data, rota, tipo, urgencia e preco"
  artifacts:
    - path: "app/services/alert_service.py"
      provides: "Email enviado para recipient dinamico (user email)"
      contains: "recipient"
    - path: "app/services/polling_service.py"
      provides: "Carrega user.email via eager load ou relationship"
      contains: "user.email"
    - path: "app/templates/dashboard/alerts.html"
      provides: "Pagina de historico de alertas do usuario"
      contains: "Meus Alertas"
    - path: "app/routes/dashboard.py"
      provides: "Rota GET /alerts para historico de sinais"
      contains: "def alerts_page"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/alert_service.py"
      via: "compose_consolidated_email recebe recipient email"
      pattern: "user.email|recipient"
    - from: "app/routes/dashboard.py"
      to: "app/models.py"
      via: "Query DetectedSignal filtrado por user_id via RouteGroup"
      pattern: "DetectedSignal.*user"
---

<objective>
Enviar alertas por email para o email do dono do grupo (nao mais para gmail_recipient fixo) e criar pagina "Meus Alertas" com historico de sinais.

Purpose: MULTI-02 garante que cada usuario recebe seus proprios alertas. MULTI-04 da visibilidade ao historico de sinais, criando engajamento mesmo quando nao ha sinais novos.
Output: alert_service atualizado, polling_service com user email, template e rota de Meus Alertas.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-data-isolation/12-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md

<interfaces>
From app/services/alert_service.py (current - uses settings.gmail_recipient):
```python
def compose_alert_email(signal, group) -> MIMEMultipart:
    msg["To"] = settings.gmail_recipient  # FIXO - precisa mudar para user email

def compose_consolidated_email(signals, snapshots, group) -> MIMEMultipart:
    msg["To"] = settings.gmail_recipient  # FIXO - precisa mudar para user email
```

From app/services/polling_service.py (current - no user context):
```python
def _poll_group(db, client, group):
    # group.user nao e carregado (sem relationship ainda no Plan 01 ou precisa eagerly load)
    if accumulated_signals and should_alert(group):
        msg = compose_consolidated_email(accumulated_signals, accumulated_snapshots, group)
        send_email(msg)  # envia para gmail_recipient fixo
```

From app/models.py (after Plan 01):
```python
class RouteGroup(Base):
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    user: Mapped["User"] = relationship("User", backref="route_groups")

class DetectedSignal(Base):
    route_group_id: Mapped[int] = mapped_column(ForeignKey("route_groups.id"))
    route_group: Mapped["RouteGroup"] = relationship("RouteGroup")
```

From app/templates/base.html:
```html
<!-- Header com nav links: Dashboard, Criar Grupo -->
<!-- Precisa adicionar link "Meus Alertas" -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alertas por email para o dono do grupo + pagina Meus Alertas</name>
  <files>app/services/alert_service.py, app/services/polling_service.py, app/routes/dashboard.py, app/templates/dashboard/alerts.html, app/templates/base.html, tests/test_alert_service.py, tests/test_alerts_page.py</files>
  <read_first>app/services/alert_service.py, app/services/polling_service.py, app/routes/dashboard.py, app/templates/base.html, tests/test_alert_service.py, app/models.py</read_first>
  <behavior>
    - Test 1: compose_consolidated_email com recipient_email="user@example.com" seta msg["To"] para user@example.com
    - Test 2: compose_alert_email com recipient_email seta msg["To"] corretamente
    - Test 3: GET /alerts retorna 200 com template alerts.html
    - Test 4: Pagina /alerts mostra apenas sinais dos grupos do usuario logado
    - Test 5: Pagina /alerts com sinais exibe tipo, urgencia, rota, preco e data
  </behavior>
  <action>
    **Parte A: Email para dono do grupo**

    1. Em app/services/alert_service.py:
       - `compose_alert_email(signal, group, recipient_email: str)`: adicionar parametro recipient_email
         - Mudar `msg["To"] = settings.gmail_recipient` para `msg["To"] = recipient_email`
       - `compose_consolidated_email(signals, snapshots, group, recipient_email: str)`: adicionar parametro recipient_email
         - Mudar `msg["To"] = settings.gmail_recipient` para `msg["To"] = recipient_email`

    2. Em app/services/polling_service.py:
       - Na funcao `run_polling_cycle`, ao buscar groups, usar joinedload para carregar user:
         ```python
         from sqlalchemy.orm import joinedload
         groups = db.query(RouteGroup).options(joinedload(RouteGroup.user)).filter(RouteGroup.is_active == True).all()
         ```
       - Em `_poll_group`, ao enviar email consolidado, passar `group.user.email` como recipient:
         ```python
         msg = compose_consolidated_email(accumulated_signals, accumulated_snapshots, group, recipient_email=group.user.email)
         ```
       - Adicionar guard: se `group.user` e None (dados legados sem user_id), usar `settings.gmail_recipient` como fallback

    **Parte B: Pagina Meus Alertas**

    3. Em app/routes/dashboard.py, adicionar rota:
       ```python
       @router.get("/alerts", response_class=HTMLResponse)
       def alerts_page(request: Request, db: Session = Depends(get_db), user: User | None = Depends(get_current_user)):
           signals = (
               db.query(DetectedSignal)
               .join(RouteGroup, DetectedSignal.route_group_id == RouteGroup.id)
               .filter(RouteGroup.user_id == user.id)
               .order_by(DetectedSignal.detected_at.desc())
               .limit(100)
               .all()
           )
           return templates.TemplateResponse(
               request=request,
               name="dashboard/alerts.html",
               context={"signals": signals, "user": user, "format_date_br": format_date_br, "format_price_brl": format_price_brl},
           )
       ```
       - Importar DetectedSignal no topo do arquivo

    4. Criar app/templates/dashboard/alerts.html:
       - Extends base.html
       - Titulo "Meus Alertas"
       - Tabela ou lista de cards com colunas: Data (dd/mm/aaaa HH:MM), Tipo de sinal, Urgencia (badge colorido), Rota (origem -> destino), Preco, Grupo
       - Estado vazio: "Nenhum alerta detectado ainda. Configure grupos de rotas para comecar a receber alertas."
       - Usar mesma paleta visual do dashboard (dark mode, cores semanticas)

    5. Em app/templates/base.html, adicionar link "Meus Alertas" no nav (ao lado de Dashboard e Criar Grupo), com href="/alerts" e icone de sino

    6. Criar tests/test_alerts_page.py com os testes 3, 4, 5 descritos em <behavior>
    7. Atualizar tests/test_alert_service.py com os testes 1, 2 descritos em <behavior>
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_alert_service.py tests/test_alerts_page.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "recipient_email" app/services/alert_service.py` retorna >= 4 linhas
    - `grep "gmail_recipient" app/services/alert_service.py` retorna 0 linhas (removido) OU aparece apenas como fallback
    - `grep "user.email" app/services/polling_service.py` retorna >= 1 linha
    - `grep "joinedload" app/services/polling_service.py` retorna >= 1 linha
    - `grep "def alerts_page" app/routes/dashboard.py` retorna 1 linha
    - Arquivo app/templates/dashboard/alerts.html existe e contem "Meus Alertas"
    - `grep "alerts" app/templates/base.html` retorna >= 1 linha (link no nav)
    - `python -m pytest -x` passa com todos os testes
  </acceptance_criteria>
  <done>Email de alerta enviado para o email do Google do dono do grupo. Pagina "Meus Alertas" exibe historico de sinais filtrado por usuario. Todos os testes passam.</done>
</task>

</tasks>

<verification>
- `python -m pytest -x` passa com todos os testes (existentes + novos)
- compose_consolidated_email e compose_alert_email recebem recipient_email ao inves de usar settings.gmail_recipient
- polling_service carrega user via joinedload e passa user.email para email functions
- GET /alerts retorna pagina com sinais do usuario logado
- Link "Meus Alertas" visivel no nav do base.html
</verification>

<success_criteria>
- Alertas por email enviados para user.email (nao gmail_recipient fixo)
- Pagina /alerts acessivel e mostra apenas sinais dos grupos do usuario logado
- Todos os testes passam sem regressao
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-isolation/12-02-SUMMARY.md`
</output>
