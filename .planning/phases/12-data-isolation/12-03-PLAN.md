---
phase: 12-data-isolation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/add_api_usage_table.py
  - app/services/quota_service.py
  - app/services/polling_service.py
  - app/services/dashboard_service.py
  - app/templates/dashboard/index.html
  - tests/test_quota_service.py
autonomous: true
requirements: [MULTI-03]

must_haves:
  truths:
    - "Dashboard exibe indicador de buscas SerpAPI restantes no mes"
    - "Cada chamada SerpAPI incrementa o contador global"
    - "Contador reseta no primeiro dia de cada mes"
    - "Indicador mostra X/250 buscas usadas este mes"
  artifacts:
    - path: "app/models.py"
      provides: "Modelo ApiUsage para contador global de SerpAPI"
      contains: "ApiUsage"
    - path: "app/services/quota_service.py"
      provides: "Funcoes get_monthly_usage e increment_usage"
      contains: "def get_monthly_usage"
    - path: "app/templates/dashboard/index.html"
      provides: "Badge com buscas restantes no summary bar"
      contains: "buscas"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/quota_service.py"
      via: "increment_usage chamado apos cada SerpAPI call"
      pattern: "increment_usage"
    - from: "app/routes/dashboard.py"
      to: "app/services/quota_service.py"
      via: "get_monthly_usage passado ao template"
      pattern: "get_monthly_usage"
---

<objective>
Criar contador global de uso SerpAPI e exibir indicador de buscas restantes no dashboard.

Purpose: Com 250 buscas/mes compartilhadas entre todos os usuarios, visibilidade do consumo e essencial para evitar surpresas. Todos os usuarios veem o mesmo contador global.
Output: Modelo ApiUsage, quota_service, integracao com polling e dashboard.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From app/services/polling_service.py:
```python
def _poll_group(db, client, group):
    for origin in origins:
        for destination in destinations:
            for dep_date, ret_date in date_pairs:
                flights, insights = client.search_flights_with_insights(...)
                # Aqui e onde cada chamada SerpAPI acontece
```

From app/services/dashboard_service.py:
```python
def get_dashboard_summary(db: Session) -> dict:
    return {
        "active_count": active_count,
        "cheapest_price": cheapest_price,
        "next_polling": next_polling,
        "last_collection": last_collection_str,
    }
```

From app/templates/dashboard/index.html (summary bar section):
```html
<!-- Summary bar com fundo #1e293b, metricas em 20px bold -->
<!-- Mostra: grupos ativos, menor preco, proximo polling -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modelo ApiUsage + quota_service com contador mensal</name>
  <files>app/models.py, alembic/versions/add_api_usage_table.py, app/services/quota_service.py, tests/test_quota_service.py</files>
  <read_first>app/models.py, app/services/polling_service.py, app/config.py</read_first>
  <behavior>
    - Test 1: get_monthly_usage retorna 0 quando nao ha registros no mes atual
    - Test 2: increment_usage cria registro e incrementa count para 1
    - Test 3: Multiplas chamadas increment_usage acumulam corretamente (3 chamadas = count 3)
    - Test 4: get_monthly_usage de mes anterior nao conta no mes atual
    - Test 5: get_remaining_quota retorna 250 - usage_count
  </behavior>
  <action>
    1. Em app/models.py, adicionar modelo ApiUsage:
       ```python
       class ApiUsage(Base):
           __tablename__ = "api_usage"
           id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
           year_month: Mapped[str] = mapped_column(String(7), unique=True, index=True)  # formato "2026-03"
           search_count: Mapped[int] = mapped_column(Integer, default=0)
           updated_at: Mapped[datetime.datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())
       ```

    2. Gerar migration Alembic: `python -m alembic revision --autogenerate -m "add api_usage table"`

    3. Criar app/services/quota_service.py:
       ```python
       MONTHLY_QUOTA = 250

       def get_current_year_month() -> str:
           return datetime.datetime.utcnow().strftime("%Y-%m")

       def get_monthly_usage(db: Session) -> int:
           ym = get_current_year_month()
           record = db.query(ApiUsage).filter(ApiUsage.year_month == ym).first()
           return record.search_count if record else 0

       def increment_usage(db: Session, count: int = 1) -> int:
           ym = get_current_year_month()
           record = db.query(ApiUsage).filter(ApiUsage.year_month == ym).first()
           if record is None:
               record = ApiUsage(year_month=ym, search_count=0)
               db.add(record)
           record.search_count += count
           db.commit()
           return record.search_count

       def get_remaining_quota(db: Session) -> int:
           used = get_monthly_usage(db)
           return max(0, MONTHLY_QUOTA - used)
       ```

    4. Criar tests/test_quota_service.py com os 5 testes em <behavior>

    5. Rodar: `python -m pytest tests/test_quota_service.py -x -v`
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_quota_service.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class ApiUsage" app/models.py` retorna 1 linha
    - `grep "year_month" app/models.py` retorna >= 1 linha
    - Arquivo app/services/quota_service.py existe
    - `grep "def get_monthly_usage" app/services/quota_service.py` retorna 1 linha
    - `grep "def increment_usage" app/services/quota_service.py` retorna 1 linha
    - `grep "def get_remaining_quota" app/services/quota_service.py` retorna 1 linha
    - `grep "MONTHLY_QUOTA = 250" app/services/quota_service.py` retorna 1 linha
    - `python -m pytest tests/test_quota_service.py -x` passa
  </acceptance_criteria>
  <done>Modelo ApiUsage existe, quota_service funciona com get/increment/remaining, 5 testes passando</done>
</task>

<task type="auto">
  <name>Task 2: Integrar quota no polling + indicador no dashboard</name>
  <files>app/services/polling_service.py, app/services/dashboard_service.py, app/routes/dashboard.py, app/templates/dashboard/index.html</files>
  <read_first>app/services/polling_service.py, app/services/dashboard_service.py, app/routes/dashboard.py, app/templates/dashboard/index.html, app/services/quota_service.py</read_first>
  <action>
    1. Em app/services/polling_service.py:
       - Importar `from app.services.quota_service import increment_usage, get_remaining_quota`
       - Em `_poll_group`, apos cada chamada SerpAPI bem-sucedida (`client.search_flights_with_insights`), chamar `increment_usage(db)`
       - Em `run_polling_cycle`, antes de iniciar o loop de groups, checar `get_remaining_quota(db)`. Se <= 0, logar warning e retornar sem fazer polling:
         ```python
         remaining = get_remaining_quota(db)
         if remaining <= 0:
             logger.warning(f"SerpAPI monthly quota exhausted ({MONTHLY_QUOTA} searches). Skipping polling cycle.")
             return
         ```

    2. Em app/services/dashboard_service.py, na funcao `get_dashboard_summary`:
       - Importar `from app.services.quota_service import get_monthly_usage, get_remaining_quota, MONTHLY_QUOTA`
       - Adicionar ao dict de retorno:
         ```python
         "api_usage": get_monthly_usage(db),
         "api_remaining": get_remaining_quota(db),
         "api_quota": MONTHLY_QUOTA,
         ```

    3. Em app/templates/dashboard/index.html, no summary bar (a barra escura no topo):
       - Adicionar um quarto bloco de metrica ao lado dos existentes (grupos ativos, menor preco, proximo polling):
         ```html
         <div class="summary-metric">
             <span class="metric-value">{{ summary.api_remaining }}</span>
             <span class="metric-label">buscas restantes</span>
         </div>
         ```
       - Se api_remaining <= 50, usar cor amarela (#f59e0b) no valor
       - Se api_remaining <= 10, usar cor vermelha (#ef4444) no valor

    4. Rodar todos os testes: `python -m pytest -x`
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "increment_usage" app/services/polling_service.py` retorna >= 1 linha
    - `grep "get_remaining_quota" app/services/polling_service.py` retorna >= 1 linha
    - `grep "api_remaining" app/services/dashboard_service.py` retorna >= 1 linha
    - `grep "api_quota" app/services/dashboard_service.py` retorna >= 1 linha
    - `grep "buscas restantes" app/templates/dashboard/index.html` retorna >= 1 linha
    - `python -m pytest -x` passa com todos os testes
  </acceptance_criteria>
  <done>Cada chamada SerpAPI incrementa contador. Dashboard mostra "X buscas restantes" com cores semanticas. Polling para quando quota esgotada. Todos os testes passam.</done>
</task>

</tasks>

<verification>
- `python -m pytest -x` passa com todos os testes
- Dashboard mostra indicador de buscas restantes no summary bar
- Polling incrementa contador a cada chamada SerpAPI
- Polling para quando quota mensal esgotada
</verification>

<success_criteria>
- Indicador "X buscas restantes" visivel no summary bar do dashboard
- Cada chamada SerpAPI incrementa o contador global
- Quando quota esgotada (250/mes), polling nao faz novas chamadas
- Todos os testes passam sem regressao
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-isolation/12-03-SUMMARY.md`
</output>
