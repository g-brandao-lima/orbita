---
phase: 12-data-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/services/dashboard_service.py
  - app/routes/dashboard.py
  - alembic/versions/add_user_id_to_route_groups.py
  - tests/test_data_isolation.py
  - tests/conftest.py
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "Usuario A nao consegue ver grupos do Usuario B em nenhuma rota"
    - "Criacao de grupo associa automaticamente ao usuario logado"
    - "Dashboard exibe apenas grupos do usuario logado"
    - "Detalhe, edicao e toggle de grupo rejeita acesso a grupos de outro usuario"
  artifacts:
    - path: "app/models.py"
      provides: "user_id FK on RouteGroup + relationship User<->RouteGroup"
      contains: "user_id"
    - path: "alembic/versions/add_user_id_to_route_groups.py"
      provides: "Migration adicionando user_id a route_groups"
      contains: "add_column"
    - path: "app/services/dashboard_service.py"
      provides: "Todas as queries filtradas por user_id"
      contains: "user_id"
    - path: "app/routes/dashboard.py"
      provides: "Todas as rotas passam user.id para services"
      contains: "user.id"
    - path: "tests/test_data_isolation.py"
      provides: "Teste de isolamento com 2 usuarios"
      contains: "test_user_a_cannot_see_user_b"
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/services/dashboard_service.py"
      via: "user.id passado como parametro"
      pattern: "get_groups_with_summary.*user"
    - from: "app/models.py"
      to: "alembic migration"
      via: "user_id FK on route_groups"
      pattern: "ForeignKey.*users.id"
---

<objective>
Adicionar user_id FK na tabela route_groups, filtrar todas as queries por usuario e garantir isolamento completo de dados entre usuarios.

Purpose: Sem esta mudanca, qualquer usuario logado ve todos os dados de todos os usuarios. Este e o requisito de seguranca mais critico do v2.0.
Output: Migration Alembic, model atualizado, services e routes filtrados por user_id, testes de isolamento.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-google-oauth/11-01-SUMMARY.md
@.planning/phases/11-google-oauth/11-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From app/models.py:
```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    google_id: Mapped[str] = mapped_column(String(255), unique=True, index=True)
    email: Mapped[str] = mapped_column(String(255), unique=True)
    name: Mapped[str] = mapped_column(String(255))
    picture_url: Mapped[str | None] = mapped_column(String(500), nullable=True)

class RouteGroup(Base):
    __tablename__ = "route_groups"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(100))
    # ... (sem user_id ainda)
```

From app/auth/dependencies.py:
```python
def get_current_user(request: Request, db: Session = Depends(get_db)) -> User | None:
    user_id = request.session.get("user_id")
    if not user_id:
        return None
    return db.get(User, user_id)
```

From app/services/dashboard_service.py:
```python
def get_groups_with_summary(db: Session) -> list[dict]:  # sem user_id
def get_price_history(db: Session, group_id: int, days: int = 14) -> dict:  # sem user_id
def get_dashboard_summary(db: Session) -> dict:  # sem user_id
def get_recent_activity(db: Session, limit: int = 8) -> list[dict]:  # sem user_id
```

From app/routes/dashboard.py:
```python
def dashboard_index(request, msg, db, user: User | None = Depends(get_current_user)):
    groups = get_groups_with_summary(db)  # sem filtro de user
    summary = get_dashboard_summary(db)   # sem filtro de user
```

From tests/conftest.py:
```python
@pytest.fixture(name="test_user")
def test_user_fixture(db):
    user = User(google_id="google-test-123", email="test@gmail.com", name="Test User")
    # ...

@pytest.fixture(name="client")
def client_fixture(db, test_user):
    # Client autenticado com session cookie do test_user
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: user_id FK no RouteGroup + migration Alembic + testes de isolamento</name>
  <files>app/models.py, alembic/versions/add_user_id_to_route_groups.py, tests/test_data_isolation.py, tests/conftest.py</files>
  <read_first>app/models.py, tests/conftest.py, app/auth/dependencies.py, alembic/versions/</read_first>
  <behavior>
    - Test 1: RouteGroup criado com user_id=1 nao aparece em query filtrada por user_id=2
    - Test 2: Dois usuarios com grupos proprios; query por user_id retorna apenas grupos do respectivo usuario
    - Test 3: Dashboard index para usuario A mostra apenas grupos de A (via client autenticado)
    - Test 4: Dashboard detail para grupo de outro usuario retorna 404
    - Test 5: Toggle de grupo de outro usuario retorna 404
    - Test 6: Criacao de grupo associa user_id do usuario logado
  </behavior>
  <action>
    1. Em app/models.py, adicionar ao RouteGroup:
       - `user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True, nullable=True)`
       - `user: Mapped["User"] = relationship("User", backref="route_groups")`
       - nullable=True temporariamente para nao quebrar dados existentes; migration preenchera valor default

    2. Gerar migration Alembic: `cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m alembic revision --autogenerate -m "add user_id to route_groups"`
       - Se autogenerate falhar, criar migration manual com:
         - op.add_column("route_groups", sa.Column("user_id", sa.Integer(), nullable=True))
         - op.create_index("ix_route_groups_user_id", "route_groups", ["user_id"])
         - op.create_foreign_key(None, "route_groups", "users", ["user_id"], ["id"])

    3. Em tests/conftest.py, adicionar fixture `second_user`:
       ```python
       @pytest.fixture(name="second_user")
       def second_user_fixture(db):
           user = User(google_id="google-test-456", email="other@gmail.com", name="Other User", picture_url=None)
           db.add(user)
           db.commit()
           db.refresh(user)
           return user
       ```

    4. Criar tests/test_data_isolation.py com os 6 testes descritos em <behavior>.
       - Para test 3/4/5/6, usar client fixture (autenticado como test_user).
       - Para verificar isolamento, criar grupos via db.add() com user_id explicitamente diferente.

    5. Rodar testes (devem FALHAR pois services/routes nao filtram ainda): `python -m pytest tests/test_data_isolation.py -x`
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_data_isolation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - app/models.py contem `user_id: Mapped[int]` com `ForeignKey("users.id")` no RouteGroup
    - app/models.py contem `user: Mapped["User"] = relationship("User"` no RouteGroup
    - Arquivo de migration existe em alembic/versions/ com "user_id" no nome ou conteudo
    - tests/test_data_isolation.py contem pelo menos 6 funcoes test_
    - tests/conftest.py contem fixture second_user
    - `grep -c "user_id" app/models.py` retorna >= 2
    - `grep -c "def test_" tests/test_data_isolation.py` retorna >= 6
  </acceptance_criteria>
  <done>RouteGroup tem user_id FK, migration Alembic gerada, 6+ testes de isolamento escritos e falhando (RED)</done>
</task>

<task type="auto">
  <name>Task 2: Filtrar services e routes por user_id (GREEN)</name>
  <files>app/services/dashboard_service.py, app/routes/dashboard.py</files>
  <read_first>app/services/dashboard_service.py, app/routes/dashboard.py, tests/test_data_isolation.py</read_first>
  <action>
    1. Em app/services/dashboard_service.py, adicionar parametro `user_id: int` a todas as funcoes que retornam dados de usuario:

       a) `get_groups_with_summary(db: Session, user_id: int)`:
          - Mudar linha 12: `groups = db.query(RouteGroup).filter(RouteGroup.user_id == user_id).all()`

       b) `get_dashboard_summary(db: Session, user_id: int)`:
          - Mudar contagem de ativos: adicionar `.where(RouteGroup.user_id == user_id)` na query active_count
          - Mudar query de cheapest_price: adicionar `.filter(RouteGroup.user_id == user_id)` na query active_groups

       c) `get_recent_activity(db: Session, user_id: int, limit: int = 8)`:
          - Filtrar signals: fazer join com RouteGroup e filtrar `RouteGroup.user_id == user_id`
          - Filtrar groups polling: adicionar `.filter(RouteGroup.user_id == user_id)` na query de groups

       d) `get_price_history(db: Session, group_id: int, user_id: int, days: int = 14)`:
          - Na query de group: adicionar `.filter(RouteGroup.user_id == user_id)` para garantir que usuario so ve historico dos proprios grupos

    2. Em app/routes/dashboard.py, atualizar TODAS as rotas para:

       a) `dashboard_index`: mudar para `get_groups_with_summary(db, user.id)`, `get_dashboard_summary(db, user.id)`, `get_recent_activity(db, user.id)`

       b) `create_group_form`: ao criar RouteGroup, adicionar `user_id=user.id` nos atributos

       c) `dashboard_detail`: apos buscar group, verificar `group.user_id != user.id` e retornar 404

       d) `edit_group_page` e `edit_group_form`: apos buscar group, verificar `group.user_id != user.id` e retornar 404

       e) `toggle_group`: adicionar `user: User | None = Depends(get_current_user)` e verificar ownership

       f) `_count_active_groups(db, user_id, exclude_id)`: adicionar parametro user_id e filtrar por ele

       g) `manual_polling`: manter sem filtro de usuario (polling roda para todos)

    3. Rodar TODOS os testes: `python -m pytest -x`
       - Os 6 testes de isolamento devem passar (GREEN)
       - Os 202 testes existentes devem continuar passando (precisa garantir que test_user.id e usado nos testes existentes que criam RouteGroups; verificar se conftest.py client_fixture cria RouteGroups com user_id)

    4. Se testes existentes quebrarem porque RouteGroups nao tem user_id:
       - Na fixture ou nos testes existentes, garantir que RouteGroups criados recebem `user_id=test_user.id`
       - Pode ser necessario atualizar helpers de criacao de RouteGroup nos testes
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user_id" app/services/dashboard_service.py` retorna >= 8
    - `grep "def get_groups_with_summary" app/services/dashboard_service.py` mostra parametro user_id
    - `grep "def get_dashboard_summary" app/services/dashboard_service.py` mostra parametro user_id
    - `grep "def get_recent_activity" app/services/dashboard_service.py` mostra parametro user_id
    - `grep "user.id" app/routes/dashboard.py` retorna >= 5 linhas
    - `grep "user_id=user.id" app/routes/dashboard.py` existe (na criacao de grupo)
    - Todos os testes passam (202 existentes + 6 novos)
  </acceptance_criteria>
  <done>Todas as queries filtradas por user_id. Isolamento completo verificado. Zero regressao nos 202 testes existentes.</done>
</task>

</tasks>

<verification>
- `python -m pytest -x` passa com 208+ testes (202 existentes + 6 novos de isolamento)
- `grep -r "user_id" app/services/dashboard_service.py` mostra filtro em todas as funcoes
- `grep -r "user.id" app/routes/dashboard.py` mostra passagem de user.id em todas as rotas
- RouteGroup no models.py tem coluna user_id com ForeignKey("users.id")
</verification>

<success_criteria>
- Usuario A nao consegue ver, editar ou toggle grupos do Usuario B (verificado por teste automatizado)
- Criacao de grupo associa user_id do usuario logado
- Dashboard, detalhe, edicao e toggle filtram por usuario
- Todos os 202+ testes passam sem regressao
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-isolation/12-01-SUMMARY.md`
</output>
