---
phase: 13-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/landing.html
  - app/routes/dashboard.py
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Visitante nao logado que acessa / ve landing page com hero, secoes e CTA"
    - "Visitante logado que acessa / ve o dashboard normalmente"
    - "Botao CTA aponta para /auth/login e inicia fluxo OAuth"
    - "Layout empilha em coluna unica em telas <=768px"
    - "Secao 'Por que somos diferentes' mostra 3 cards com diferenciais"
  artifacts:
    - path: "app/templates/landing.html"
      provides: "Landing page template com hero, como funciona, diferenciais, CTA final"
      contains: "Saiba quando comprar antes que o preco suba"
    - path: "app/routes/dashboard.py"
      provides: "Condicional landing vs dashboard na rota /"
      contains: "landing.html"
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/templates/landing.html"
      via: "templates.TemplateResponse condicional por user"
      pattern: "landing\\.html"
    - from: "app/templates/landing.html"
      to: "/auth/login"
      via: "href nos botoes CTA"
      pattern: "href=./auth/login."
---

<objective>
Criar a landing page publica do Flight Monitor e conectar a rota / para exibir landing (nao logado) ou dashboard (logado).

Purpose: Visitantes nao logados precisam entender o produto e ter um CTA claro para entrar com Google, em vez de ver um dashboard vazio.
Output: Template landing.html com 4 secoes + rota condicional em dashboard.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-landing-page/13-CONTEXT.md
@.planning/phases/13-landing-page/13-UI-SPEC.md

<interfaces>
<!-- Existing route handler that needs conditional logic -->

From app/routes/dashboard.py (line 103-128):
```python
@router.get("/", response_class=HTMLResponse)
def dashboard_index(
    request: Request,
    msg: str | None = None,
    db: Session = Depends(get_db),
    user: User | None = Depends(get_current_user),
):
    # user is None when not logged in, User object when logged in
    # flash_message already works via ?msg= query param
```

From app/templates/base.html:
- Header with glassmorphism, sticky, already has logado/nao logado conditional
- .container class: max-width 960px, margin 0 auto, padding 24px 24px 48px
- Flash messages already render inside .container
- Inter font already loaded (400/500/600/700)
- Dark mode: bg #0b0e14, text #e2e8f0
- .btn-primary: bg #3b82f6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar template landing.html com todas as secoes do UI-SPEC</name>
  <files>app/templates/landing.html</files>
  <read_first>
    - app/templates/base.html
    - .planning/phases/13-landing-page/13-UI-SPEC.md
    - .planning/phases/13-landing-page/13-CONTEXT.md
  </read_first>
  <action>
Criar app/templates/landing.html que herda de base.html ({% extends "base.html" %}).

O template usa {% block content %} para renderizar 4 secoes, conforme UI-SPEC e decisoes D-01 a D-06:

**Secao 1: Hero (per D-01, LAND-01)**
- Container centralizado, max-width 720px
- Icone SVG aviao 40x40 cor #0ea5e9 (usar o mesmo SVG do favicon em base.html, so mudar tamanho e cor)
- Headline: "Saiba quando comprar antes que o preco suba." em 40px/700/1.1 cor #e2e8f0 (28px mobile)
- Subtitulo: "Monitoramos voos 24h por dia e avisamos quando e a melhor hora de comprar sua passagem." em 16px/400/1.6 cor #94a3b8, max-width 520px centralizado
- CTA: link para /auth/login com texto "Comecar gratis", background linear-gradient(135deg, #0ea5e9, #3b82f6), cor branca 16px/700, padding 16px 32px, border-radius 10px, min-height 44px, box-shadow 0 2px 12px rgba(14,165,233,0.25), hover: bg #0284c7 shadow 0 4px 20px rgba(14,165,233,0.35), active: scale(0.97), transition all 0.2s ease
- Padding: 64px top (48px mobile), 64px bottom
- text-align center

**Secao 2: Como funciona (per D-01)**
- Heading "Como funciona" centralizado, 24px/700 cor #e2e8f0 (20px mobile)
- 3 step cards em CSS grid 3 colunas gap 24px (1 coluna gap 16px mobile)
- Cada card: bg #111827, border 1px solid #1e293b, border-radius 12px, padding 24px
- Numero em circulo 32x32 bg #0ea5e9, texto branco 13px/700, margin-bottom 16px
- Titulo step: 16px/700 #e2e8f0, margin-bottom 8px
- Descricao: 13px/400 #94a3b8 line-height 1.5
- Conteudo dos 3 passos conforme UI-SPEC:
  1. "Configure seus voos" / "Escolha origens, destinos e datas. O monitoramento comeca automaticamente."
  2. "Receba sinais inteligentes" / "Nosso sistema analisa precos e disponibilidade 24h por dia e detecta o momento ideal de compra."
  3. "Compre na hora certa" / "Receba alertas no seu email quando for a melhor hora de garantir sua passagem."

**Secao 3: Por que somos diferentes (per D-06, LAND-02)**
- Heading "Por que somos diferentes" centralizado, mesmo estilo da secao 2
- 3 feature cards em CSS grid 3 colunas gap 24px (1 coluna gap 16px mobile)
- Cada card: bg #111827, border 1px solid #1e293b, border-radius 12px, padding 24px
- Hover: border-color transiciona para #0ea5e9 em 0.2s ease
- Icone container: 48x48, bg rgba(14,165,233,0.1), border-radius 12px, icone 24x24 stroke #0ea5e9 stroke-width 1.5, margin-bottom 16px
- Titulo: 16px/700 #e2e8f0, margin-bottom 8px
- Descricao: 13px/400 #94a3b8 line-height 1.5
- Card 1: SVG trending-up (polyline points="23 6 13.5 15.5 8.5 10.5 1 18" + polyline points="17 6 23 6 23 12"), titulo "Sinais antes do preco subir", desc "Detectamos quando assentos baratos estao acabando, antes que o preco aumente. Voce compra no momento certo."
- Card 2: SVG refresh (path d="M21 12a9 9 0 1 1-9-9c2.52 0 4.93 1 6.74 2.74L21 8" + path d="M21 3v5h-5"), titulo "Monitoramento passivo 24h", desc "Configure uma vez e esqueca. O sistema verifica seus voos automaticamente e so te aciona quando vale a pena."
- Card 3: SVG mail (path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" + polyline points="22,6 12,13 2,6"), titulo "Alerta direto no seu email", desc "Sem apps extras. Quando detectamos uma oportunidade, voce recebe um email claro com tudo que precisa saber."

**Secao 4: CTA final (per D-01, LAND-03)**
- border-top 1px solid #1e293b, padding 64px vertical, text-align center
- Heading "Pronto para encontrar a passagem ideal?" em 24px/700 #e2e8f0 (20px mobile)
- Botao "Entrar com Google" com icone Google "G" SVG 18x18 inline a esquerda, gap 8px. Mesmos estilos do CTA hero. Link para /auth/login.
- Subtexto "Gratis. Sem cartao de credito." em 13px/400 #64748b, margin-top 16px

**CSS (per D-04, LAND-04):**
- Tudo dentro de {% block head %}<style>...</style>{% endblock %}
- html { scroll-behavior: smooth; }
- Secoes separadas por gap 64px desktop / 48px mobile
- Mobile breakpoint @media (max-width: 768px): headline 28px, section headings 20px, grid 1 coluna, hero padding-top 48px, card padding 24px 16px, CTA padding 16px 24px
- NAO adicionar padding extra no .container (ja vem do base.html)

**Tom (per D-05):** Casual e direto, sem termos tecnicos.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('app/templates/landing.html').read_text(encoding='utf-8'); assert 'extends' in t and 'base.html' in t; assert 'Saiba quando comprar' in t; assert 'Como funciona' in t; assert 'Por que somos diferentes' in t; assert 'Pronto para encontrar' in t; assert '/auth/login' in t; assert '768px' in t; assert '#0ea5e9' in t; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Template herda de base.html via {% extends "base.html" %}
    - Contem headline "Saiba quando comprar antes que o preco suba."
    - Contem secao "Como funciona" com 3 passos
    - Contem secao "Por que somos diferentes" com 3 cards e SVGs inline
    - Contem CTA final "Entrar com Google" com link /auth/login
    - CSS tem media query @media (max-width: 768px) com ajustes responsivos
    - Cores seguem UI-SPEC: #0b0e14 fundo, #0ea5e9 accent, #111827 cards
  </acceptance_criteria>
  <done>Template landing.html existe, herda de base.html, tem 4 secoes com copy/estilos/SVGs do UI-SPEC, e e responsivo com breakpoint 768px.</done>
</task>

<task type="auto">
  <name>Task 2: Condicionar rota / para landing (nao logado) vs dashboard (logado)</name>
  <files>app/routes/dashboard.py</files>
  <read_first>
    - app/routes/dashboard.py
    - app/templates/landing.html
  </read_first>
  <action>
Modificar a funcao dashboard_index em app/routes/dashboard.py (per D-03, LAND-01, LAND-03).

Adicionar condicional no inicio do handler: se user e None, renderizar landing.html e retornar imediatamente, passando apenas request, user (None) e flash_message no context. O resto do handler (queries de dashboard) so executa se user existe.

Codigo da condicional (inserir ANTES das queries de grupos):
```python
if user is None:
    flash_message = FLASH_MESSAGES.get(msg) if msg else None
    return templates.TemplateResponse(
        request=request,
        name="landing.html",
        context={"user": None, "flash_message": flash_message},
    )
```

O restante do handler (get_groups_with_summary, get_dashboard_summary, etc.) continua igual, executando apenas para usuarios logados.

NAO criar rota separada (per D-03). NAO alterar middleware ou auth.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; code=Path('app/routes/dashboard.py').read_text(encoding='utf-8'); assert 'landing.html' in code; assert 'if user is None' in code; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.py contem `if user is None:` antes das queries de dashboard
    - Quando user e None, retorna TemplateResponse com name="landing.html"
    - Quando user existe, continua com o fluxo atual do dashboard
    - Flash messages funcionam na landing (ex: ?msg=login_erro)
  </acceptance_criteria>
  <done>Rota / renderiza landing.html para visitantes nao logados e dashboard/index.html para logados. Flash messages funcionam em ambos os casos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual da landing page</name>
  <files>app/templates/landing.html, app/routes/dashboard.py</files>
  <action>
Verificacao humana da landing page. Nenhuma mudanca de codigo nesta task.
O executor deve iniciar o servidor (python main.py) e apresentar os passos de verificacao ao usuario.
  </action>
  <what-built>Landing page publica com hero, "Como funciona", "Por que somos diferentes" e CTA final. Rota / condiciona entre landing (nao logado) e dashboard (logado).</what-built>
  <how-to-verify>
    1. Iniciar servidor: python main.py
    2. Abrir navegador em modo anonimo (sem sessao) e acessar http://localhost:8000/
    3. Verificar: hero com headline "Saiba quando comprar antes que o preco suba." e botao "Comecar gratis"
    4. Scroll: secao "Como funciona" com 3 passos numerados
    5. Scroll: secao "Por que somos diferentes" com 3 cards e icones SVG
    6. Scroll: CTA final "Entrar com Google" com subtexto "Gratis. Sem cartao de credito."
    7. Clicar em "Comecar gratis" ou "Entrar com Google": deve ir para /auth/login
    8. Redimensionar janela para ~375px largura: verificar que cards empilham em coluna unica e headline diminui
    9. Fazer login via Google e acessar / novamente: deve ver o dashboard normalmente
  </how-to-verify>
  <verify>Aprovacao humana apos inspecao visual</verify>
  <done>Usuario aprovou a landing page visualmente em desktop e mobile.</done>
  <resume-signal>Type "approved" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- python -c "from pathlib import Path; t=Path('app/templates/landing.html').read_text(encoding='utf-8'); assert 'extends' in t; assert 'Saiba quando comprar' in t; print('landing.html OK')"
- python -c "from pathlib import Path; c=Path('app/routes/dashboard.py').read_text(encoding='utf-8'); assert 'landing.html' in c; print('dashboard.py OK')"
- pytest tests/ -x -q (todos os testes existentes continuam passando)
</verification>

<success_criteria>
- Visitante nao logado em / ve landing page com 4 secoes
- Visitante logado em / ve dashboard
- Ambos os CTAs apontam para /auth/login
- Layout responsivo funciona em mobile (<=768px)
- Testes existentes nao quebram
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-page/13-01-SUMMARY.md`
</output>
