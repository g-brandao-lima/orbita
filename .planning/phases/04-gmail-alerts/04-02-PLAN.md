---
phase: 04-gmail-alerts
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/routes/alerts.py
  - main.py
  - tests/test_alert_routes.py
autonomous: true
requirements: [ALRT-02]
must_haves:
  truths:
    - "GET /api/v1/alerts/silence/{token}?group_id=X com token valido seta silenced_until = now + 24h"
    - "GET /api/v1/alerts/silence/{token}?group_id=X com token invalido retorna 400"
    - "GET /api/v1/alerts/silence/{token}?group_id=999 com grupo inexistente retorna 404"
    - "Resposta de sucesso contem mensagem de confirmacao com nome do grupo"
  artifacts:
    - path: "app/routes/alerts.py"
      provides: "Silence endpoint GET /api/v1/alerts/silence/{token}"
      exports: ["router"]
    - path: "tests/test_alert_routes.py"
      provides: "Testes de integracao para o silence endpoint"
      min_lines: 40
    - path: "main.py"
      provides: "Router de alerts registrado em /api/v1"
      contains: "alerts_router"
  key_links:
    - from: "app/routes/alerts.py"
      to: "app/services/alert_service.py"
      via: "verify_silence_token"
      pattern: "verify_silence_token"
    - from: "app/routes/alerts.py"
      to: "app/models.py"
      via: "RouteGroup.silenced_until"
      pattern: "silenced_until"
    - from: "main.py"
      to: "app/routes/alerts.py"
      via: "include_router"
      pattern: "alerts_router"
---

<objective>
Criar endpoint GET /api/v1/alerts/silence/{token} que valida token HMAC e silencia alertas do grupo por 24h, usando TDD (RED-GREEN-REFACTOR). Registrar router no main.py.

Purpose: Este endpoint e o target do link de silenciar embutido nos emails (ALRT-02). O usuario clica no link do email e o grupo fica silenciado por 24h.
Output: alerts.py com endpoint funcional, testes passando, router registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gmail-alerts/04-RESEARCH.md

<interfaces>
<!-- De Plan 01 (wave 1 paralelo — pode nao estar pronto ainda) -->
<!-- Se Plan 01 ainda nao executou, o executor deve criar stubs minimos -->

From app/services/alert_service.py (criado em Plan 01):
```python
def verify_silence_token(token: str, group_id: int) -> bool:
    """Verifica se token HMAC e valido para o group_id."""

def generate_silence_token(group_id: int) -> str:
    """Gera token HMAC para silence link."""
```

From app/models.py (modificado em Plan 01):
```python
class RouteGroup(Base):
    # ... campos existentes ...
    silenced_until: Mapped[datetime.datetime | None] = mapped_column(DateTime, nullable=True, default=None)
```

From app/routes/route_groups.py (padrao de router existente):
```python
router = APIRouter(prefix="/route-groups", tags=["route-groups"])
DbDep = Annotated[Session, Depends(get_db)]
```

From main.py (padrao de registro de router):
```python
from app.routes.route_groups import router as route_groups_router
app.include_router(route_groups_router, prefix="/api/v1")
```

From tests/conftest.py:
```python
# Fixtures: db (Session), client (TestClient com dependency override)
```
</interfaces>

NOTA: Este plan roda em Wave 1 paralelo com Plan 01. Se alert_service.py ainda nao existe quando este plan executar, o executor deve:
1. Criar stubs minimos de verify_silence_token e generate_silence_token em alert_service.py
2. Garantir que silenced_until existe no modelo
Caso Plan 01 ja tenha executado, simplesmente importar as funcoes existentes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Escrever testes para silence endpoint (todos devem falhar)</name>
  <files>tests/test_alert_routes.py, app/routes/alerts.py</files>
  <read_first>
    - tests/conftest.py (fixtures db e client)
    - app/routes/route_groups.py (padrao de router, get_group_or_404)
    - main.py (como registrar router)
    - app/models.py (RouteGroup — verificar se silenced_until ja existe de Plan 01)
    - app/services/alert_service.py (verificar se verify_silence_token ja existe de Plan 01)
  </read_first>
  <behavior>
    - test_silence_endpoint_valid_token_sets_silenced_until: POST grupo, gerar token, GET /api/v1/alerts/silence/{token}?group_id=X retorna 200, grupo.silenced_until e ~24h no futuro
    - test_silence_endpoint_invalid_token_returns_400: GET /api/v1/alerts/silence/tokenfake?group_id=X retorna 400
    - test_silence_endpoint_nonexistent_group_returns_404: GET /api/v1/alerts/silence/{token_valido}?group_id=999 retorna 404
    - test_silence_endpoint_response_contains_group_name: resposta JSON contem nome do grupo silenciado
    - test_silence_endpoint_already_silenced_extends: grupo ja silenciado, clicar novamente reseta para +24h
  </behavior>
  <action>
    1. Verificar se Plan 01 ja executou (app/services/alert_service.py existe com funcoes reais). Se nao:
       - Garantir silenced_until existe no RouteGroup (se nao, adicionar)
       - Criar stub minimo em alert_service.py com generate_silence_token e verify_silence_token
    2. Criar app/routes/alerts.py como stub:
       ```python
       from fastapi import APIRouter
       router = APIRouter()

       @router.get("/alerts/silence/{token}")
       def silence_group(token: str, group_id: int):
           raise NotImplementedError
       ```
    3. Registrar router em main.py:
       ```python
       from app.routes.alerts import router as alerts_router
       app.include_router(alerts_router, prefix="/api/v1")
       ```
    4. Criar tests/test_alert_routes.py com os 5 testes listados em behavior. Cada teste:
       - Cria um RouteGroup via db.add (nao via API, para isolar)
       - Gera token via generate_silence_token(group.id)
       - Faz GET via client
       - Asserta status code e conteudo
    5. Executar `python -m pytest tests/test_alert_routes.py -x -q` — todos falham (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_alert_routes.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_alert_routes.py existe com >= 5 funcoes test_
    - app/routes/alerts.py existe com endpoint stub
    - main.py inclui alerts_router registrado em /api/v1
    - `python -m pytest tests/test_alert_routes.py --collect-only` lista >= 5 testes
    - Todos os testes falham (RED)
  </acceptance_criteria>
  <done>5+ testes escritos e falhando. Router stub criado e registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar silence endpoint para todos os testes passarem</name>
  <files>app/routes/alerts.py</files>
  <read_first>
    - tests/test_alert_routes.py (o que os testes esperam)
    - app/services/alert_service.py (verify_silence_token — importar)
    - app/models.py (RouteGroup com silenced_until)
    - app/routes/route_groups.py (padrao de endpoint: Depends(get_db), HTTPException)
  </read_first>
  <behavior>
    - Todos os 5+ testes de test_alert_routes.py passam
    - Suite completa nao tem regressoes
  </behavior>
  <action>
    Implementar app/routes/alerts.py:

    ```python
    from typing import Annotated
    from datetime import datetime, timedelta
    from fastapi import APIRouter, Depends, HTTPException, Query
    from sqlalchemy.orm import Session
    from app.database import get_db
    from app.models import RouteGroup
    from app.services.alert_service import verify_silence_token

    router = APIRouter()
    DbDep = Annotated[Session, Depends(get_db)]

    @router.get("/alerts/silence/{token}")
    def silence_group(token: str, group_id: int = Query(...), db: Session = Depends(get_db)):
        if not verify_silence_token(token, group_id):
            raise HTTPException(status_code=400, detail="Token invalido")

        group = db.get(RouteGroup, group_id)
        if not group:
            raise HTTPException(status_code=404, detail="Grupo nao encontrado")

        group.silenced_until = datetime.utcnow() + timedelta(hours=24)
        db.commit()
        return {"message": f"Alertas do grupo '{group.name}' silenciados por 24 horas"}
    ```

    Executar:
    1. `python -m pytest tests/test_alert_routes.py -v` — todos passam (GREEN)
    2. `python -m pytest tests/ -x -q` — suite completa verde
  </action>
  <verify>
    <automated>python -m pytest tests/test_alert_routes.py -v && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_alert_routes.py -v` mostra 5+ PASSED, 0 FAILED
    - `python -m pytest tests/ -x -q` mostra suite completa passando
    - GET /api/v1/alerts/silence/{token}?group_id=1 com token valido retorna 200
    - GET /api/v1/alerts/silence/fake?group_id=1 retorna 400
    - app/routes/alerts.py nao contem NotImplementedError
  </acceptance_criteria>
  <done>Silence endpoint implementado. Todos os testes passam. Zero regressoes.</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_alert_routes.py -v --tb=short
python -m pytest tests/ -x -q
grep "alerts_router" main.py                        # registrado
grep "silence_group" app/routes/alerts.py            # endpoint existe
</verification>

<success_criteria>
- GET /api/v1/alerts/silence/{token}?group_id=X funciona com token valido (200) e invalido (400)
- Router registrado em main.py
- 5+ testes passando em test_alert_routes.py
- Suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-alerts/04-02-SUMMARY.md`
</output>
