---
phase: 04-gmail-alerts
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - app/services/polling_service.py
  - tests/test_polling_service.py
autonomous: false
requirements: [ALRT-01, ALRT-02, ALRT-03]
must_haves:
  truths:
    - "Quando detect_signals retorna sinais, alert_service.compose_alert_email e send_email sao chamados para cada sinal"
    - "Grupos silenciados (silenced_until no futuro) nao recebem email"
    - "Falha no envio de email (SMTP error) nao interrompe o ciclo de polling"
    - "Log registra envio de alerta e falhas de email"
  artifacts:
    - path: "app/services/polling_service.py"
      provides: "Integracao com alert_service no ciclo de polling"
      contains: "should_alert"
    - path: "tests/test_polling_service.py"
      provides: "Testes de integracao do alerta no polling"
      contains: "test_polling_sends_alert"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/alert_service.py"
      via: "should_alert, compose_alert_email, send_email"
      pattern: "should_alert|compose_alert_email|send_email"
    - from: "app/services/polling_service.py"
      to: "app/models.py"
      via: "RouteGroup.silenced_until check"
      pattern: "should_alert.*group"
---

<objective>
Integrar alert_service no polling_service para que sinais detectados disparem emails automaticamente, e adicionar checkpoint humano para verificar o fluxo completo.

Purpose: Esta e a "cola" que conecta deteccao de sinais (Phase 3) com entrega de valor (email). Sem essa integracao, sinais sao detectados mas o usuario nao e notificado.
Output: polling_service.py envia emails ao detectar sinais. Fluxo verificado pelo humano.

ALRT-03 nota: O RESEARCH.md esclarece que a UI de dashboard e escopo da Phase 5. A cobertura de ALRT-03 nesta phase se limita a garantir que os dados necessarios (silenced_until, best_price via snapshots) estejam disponiveis para Phase 5 consumir — o que ja esta coberto pelos Plans 01 e 02 (silenced_until column e should_alert logic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gmail-alerts/04-RESEARCH.md
@.planning/phases/04-gmail-alerts/04-01-SUMMARY.md
@.planning/phases/04-gmail-alerts/04-02-SUMMARY.md

<interfaces>
From app/services/alert_service.py (criado em Plan 01):
```python
def compose_alert_email(signal: DetectedSignal, group: RouteGroup) -> MIMEMultipart:
    """Compoe email HTML com dados do sinal."""

def send_email(msg: MIMEMultipart) -> None:
    """Envia email via Gmail SMTP_SSL porta 465."""

def should_alert(group: RouteGroup) -> bool:
    """Retorna True se grupo pode receber alertas (nao silenciado)."""
```

From app/services/polling_service.py (ponto de integracao, linhas 127-135):
```python
# Em _process_offer, apos detect_signals:
detected = detect_signals(db, snapshot)
for signal in detected:
    logger.info(f"Signal detected: {signal.signal_type} ...")
# AQUI: adicionar chamada a should_alert + compose_alert_email + send_email
```

From app/models.py:
```python
class RouteGroup(Base):
    silenced_until: Mapped[datetime.datetime | None]  # adicionado em Plan 01
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar alert_service no polling_service + testes</name>
  <files>app/services/polling_service.py, tests/test_polling_service.py</files>
  <read_first>
    - app/services/polling_service.py (funcao _process_offer, linhas 83-135 — ponto de integracao)
    - app/services/alert_service.py (funcoes a importar: should_alert, compose_alert_email, send_email)
    - tests/test_polling_service.py (testes existentes — nao quebrar)
    - app/models.py (RouteGroup com silenced_until)
  </read_first>
  <behavior>
    - test_polling_sends_alert_on_signal: quando detect_signals retorna sinais e grupo nao silenciado, send_email e chamado
    - test_polling_skips_alert_silenced_group: quando grupo tem silenced_until no futuro, send_email NAO e chamado
    - test_polling_continues_on_smtp_failure: quando send_email levanta Exception, polling continua (nao crashou)
  </behavior>
  <action>
    1. Modificar app/services/polling_service.py na funcao _process_offer:
       - Adicionar imports no topo: `from app.services.alert_service import should_alert, compose_alert_email, send_email`
       - Apos o loop `for signal in detected:` (linha 129), dentro do bloco existente, adicionar:
         ```python
         try:
             if should_alert(group):
                 msg = compose_alert_email(signal, group)
                 send_email(msg)
                 logger.info(f"Alert email sent for {signal.signal_type}")
         except Exception as e:
             logger.error(f"Alert email failed: {e}")
         ```
       - O try/except garante que falha de SMTP nao interrompe o polling (ALRT-01 requisito de resiliencia)

    2. Adicionar 3 testes em tests/test_polling_service.py:
       - test_polling_sends_alert_on_signal: mock detect_signals para retornar 1 sinal, mock send_email, mock should_alert=True, verificar send_email chamado 1x
       - test_polling_skips_alert_silenced_group: mock should_alert=False, verificar send_email NAO chamado
       - test_polling_continues_on_smtp_failure: mock send_email levanta Exception, verificar que _process_offer nao levanta (retorna normalmente)

    3. Executar testes:
       - `python -m pytest tests/test_polling_service.py -v` — novos e existentes passam
       - `python -m pytest tests/ -x -q` — suite completa verde
  </action>
  <verify>
    <automated>python -m pytest tests/test_polling_service.py -v && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "should_alert" app/services/polling_service.py` retorna match
    - `grep "compose_alert_email" app/services/polling_service.py` retorna match
    - `grep "send_email" app/services/polling_service.py` retorna match
    - 3 novos testes passando em test_polling_service.py
    - Suite completa verde
    - Falha de SMTP isolada em try/except (nao propaga)
  </acceptance_criteria>
  <done>polling_service envia alertas apos detectar sinais. Grupos silenciados ignorados. Falhas de email isoladas. Testes passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar fluxo completo de email e silenciamento</name>
  <files>N/A</files>
  <action>
    Checkpoint de verificacao humana. Claude executa a suite de testes e apresenta os resultados ao usuario para aprovacao.
    1. Executar `python -m pytest tests/ -v --tb=short` e mostrar resultado
    2. Executar `grep -rn "print(" app/services/alert_service.py app/routes/alerts.py` para confirmar ausencia de debug prints
    3. Apresentar resumo ao usuario com o template what-built/how-to-verify abaixo
  </action>
  <what-built>
    Sistema completo de alertas: alert_service.py (composicao + envio + silenciamento), silence endpoint (GET /api/v1/alerts/silence/{token}), integracao no polling_service. Testes unitarios e de integracao passando.
  </what-built>
  <how-to-verify>
    1. Verificar suite de testes: `python -m pytest tests/ -v --tb=short`
       Esperado: todos passam, incluindo test_alert_service.py, test_alert_routes.py, test_polling_service.py

    2. (Opcional — requer .env configurado com Gmail App Password)
       Para testar envio real de email:
       a. Configurar .env com gmail_sender, gmail_app_password, gmail_recipient reais
       b. Configurar app_base_url (ex: http://localhost:8000)
       c. Iniciar app: `python main.py`
       d. Criar um route group via API
       e. Aguardar ciclo de polling ou acionar manualmente
       f. Verificar inbox do gmail_recipient

    3. Testar silence endpoint manualmente:
       a. Com app rodando: `curl "http://localhost:8000/api/v1/alerts/silence/TOKEN_AQUI?group_id=1"` (usar token gerado)
       b. Verificar resposta JSON com mensagem de confirmacao

    4. Verificar que codigo nao tem prints de debug:
       `grep -rn "print(" app/services/alert_service.py app/routes/alerts.py` — deve retornar vazio
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>Humano aprovou o fluxo completo de alertas via email e silenciamento.</done>
  <resume-signal>Digite "aprovado" para concluir Phase 4 ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
python -m pytest tests/ -v --tb=short
grep "should_alert" app/services/polling_service.py
grep "alerts_router" main.py
grep "silenced_until" app/models.py
grep "app_base_url" app/config.py
</verification>

<success_criteria>
- Suite completa verde (todos os testes de phases 1-4 passam)
- polling_service envia email ao detectar sinal (via should_alert + compose + send)
- Grupos silenciados nao recebem email
- Falha SMTP nao interrompe polling
- Silence endpoint funciona via GET com token HMAC
- Humano aprova o fluxo
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-alerts/04-03-SUMMARY.md`
</output>
