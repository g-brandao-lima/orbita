---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - .env.example
  - .gitignore
  - main.py
  - app/__init__.py
  - app/config.py
  - app/database.py
  - app/models.py
  - app/schemas.py
  - app/routes/__init__.py
  - app/routes/route_groups.py
  - app/services/__init__.py
  - app/services/route_group_service.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_app_startup.py
  - tests/test_config.py
  - tests/test_database.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Aplicacao inicia com python main.py e responde em localhost:8000"
    - "Arquivo .env controla todas as credenciais e configuracoes"
    - "Banco SQLite eh criado automaticamente com tabela route_groups na primeira execucao"
  artifacts:
    - path: "main.py"
      provides: "Entry point com lifespan event que cria tabelas"
      contains: "uvicorn.run"
    - path: "app/config.py"
      provides: "Settings carregadas via .env com Pydantic BaseSettings"
      contains: "class Settings"
    - path: "app/database.py"
      provides: "Engine SQLAlchemy, SessionLocal, Base, get_db dependency"
      contains: "def get_db"
    - path: "app/models.py"
      provides: "Modelo RouteGroup com JSON columns para origens/destinos"
      contains: "class RouteGroup"
    - path: "tests/conftest.py"
      provides: "Fixtures de teste: test client, in-memory SQLite, session override"
      contains: "client_fixture"
  key_links:
    - from: "main.py"
      to: "app/database.py"
      via: "lifespan cria tabelas com Base.metadata.create_all"
      pattern: "create_all"
    - from: "app/config.py"
      to: ".env"
      via: "Pydantic BaseSettings carrega env_file"
      pattern: "env_file"
    - from: "tests/conftest.py"
      to: "app/database.py"
      via: "dependency_overrides substitui get_db por sessao de teste"
      pattern: "dependency_overrides"
---

<objective>
Criar o esqueleto completo do projeto Flight Monitor: instalar dependencias, configurar infra (config, database, models, schemas), escrever testes falhando (RED) para INFRA-01/02/03, e implementar o minimo para os testes passarem (GREEN).

Purpose: Sem esta fundacao, nenhum endpoint de Route Groups pode existir. Este plano cria toda a infraestrutura testavel.
Output: Projeto funcional com app rodando, banco criando tabelas automaticamente, config via .env, e testes de infra passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup do projeto + dependencias + test infrastructure + testes RED para INFRA</name>
  <files>
    requirements.txt, pyproject.toml, .env.example, .gitignore,
    tests/__init__.py, tests/conftest.py,
    tests/test_app_startup.py, tests/test_config.py, tests/test_database.py
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (patterns de conftest.py, stack versions)
    .planning/phases/01-foundation/01-VALIDATION.md (wave 0 requirements)
    CLAUDE.md (TDD rules)
  </read_first>
  <behavior>
    - test_app_starts_and_responds: GET / retorna 200 (INFRA-01)
    - test_settings_loads_from_env: Settings carrega database_url, amadeus_client_id, amadeus_client_secret, telegram_bot_token, telegram_chat_id do .env (INFRA-02)
    - test_settings_has_defaults: Settings sem .env usa defaults validos (INFRA-02)
    - test_tables_created_on_startup: Tabela route_groups existe apos create_all (INFRA-03)
    - test_route_groups_table_has_required_columns: Colunas id, name, origins, destinations, duration_days, travel_start, travel_end, target_price, is_active existem (INFRA-03)
  </behavior>
  <action>
    1. Criar venv e instalar dependencias:
       python -m venv venv
       venv\Scripts\activate (Windows)
       pip install fastapi==0.135.2 uvicorn==0.42.0 sqlalchemy==2.0.48 pydantic==2.12.5 pydantic-settings==2.13.1 python-dotenv==1.2.2 pytest==9.0.2 httpx==0.28.1

    2. Criar requirements.txt com:
       fastapi==0.135.2
       uvicorn==0.42.0
       sqlalchemy==2.0.48
       pydantic==2.12.5
       pydantic-settings==2.13.1
       python-dotenv==1.2.2
       pytest==9.0.2
       httpx==0.28.1

    3. Criar pyproject.toml com secao [tool.pytest.ini_options]:
       testpaths = ["tests"]
       python_files = ["test_*.py"]

    4. Criar .env.example (sem segredos reais, committed ao git):
       DATABASE_URL=sqlite:///./flight_monitor.db
       AMADEUS_CLIENT_ID=
       AMADEUS_CLIENT_SECRET=
       TELEGRAM_BOT_TOKEN=
       TELEGRAM_CHAT_ID=

    5. Criar .gitignore incluindo: venv/, __pycache__/, *.db, .env, *.pyc

    6. Criar tests/__init__.py (vazio)

    7. Criar tests/conftest.py seguindo Pattern 6 do RESEARCH.md:
       - Import engine com "sqlite:///:memory:" (in-memory, nao arquivo)
       - TestingSessionLocal com check_same_thread=False
       - Fixture "db" que faz create_all antes e drop_all depois
       - Fixture "client" que faz dependency_overrides[get_db] e retorna TestClient(app)
       - IMPORTANTE: importar app de main (nao de app.main)
       - IMPORTANTE: importar Base e get_db de app.database

    8. Criar tests/test_app_startup.py (INFRA-01):
       - test_app_starts_and_responds: client.get("/") retorna status_code 200
       - test_app_has_openapi_docs: client.get("/docs") retorna 200

    9. Criar tests/test_config.py (INFRA-02):
       - test_settings_loads_defaults: instanciar Settings() sem .env, verificar database_url contém "sqlite"
       - test_settings_has_all_fields: verificar que Settings tem atributos amadeus_client_id, amadeus_client_secret, telegram_bot_token, telegram_chat_id

    10. Criar tests/test_database.py (INFRA-03):
        - test_tables_created_on_startup: usar inspector = inspect(engine) do conftest, verificar "route_groups" in inspector.get_table_names()
        - test_route_groups_table_columns: verificar colunas id, name, origins, destinations, duration_days, travel_start, travel_end, target_price, is_active existem via inspector.get_columns("route_groups")

    11. Rodar testes: python -m pytest tests/ -x -q
        TODOS DEVEM FALHAR (RED) porque nao existe main.py nem app/ ainda.
        Confirmar RED antes de prosseguir.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
    Testes devem FALHAR (RED) com ImportError ou ModuleNotFoundError.
  </verify>
  <acceptance_criteria>
    - Arquivo requirements.txt existe e contem "fastapi==0.135.2"
    - Arquivo pyproject.toml existe e contem 'testpaths = ["tests"]'
    - Arquivo .env.example existe e contem "AMADEUS_CLIENT_ID="
    - Arquivo .gitignore existe e contem ".env"
    - Arquivo tests/conftest.py existe e contem "dependency_overrides"
    - Arquivo tests/test_app_startup.py existe e contem "test_app_starts_and_responds"
    - Arquivo tests/test_config.py existe e contem "test_settings_loads_defaults"
    - Arquivo tests/test_database.py existe e contem "test_tables_created_on_startup"
    - python -m pytest tests/ retorna FAILED (RED phase confirmada)
  </acceptance_criteria>
  <done>Infraestrutura de teste completa, todos os testes existem e falham (RED confirmado)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar app skeleton para testes passarem (GREEN) - INFRA-01/02/03</name>
  <files>
    app/__init__.py, app/config.py, app/database.py, app/models.py,
    app/schemas.py, app/routes/__init__.py, app/services/__init__.py,
    app/services/route_group_service.py, app/routes/route_groups.py,
    main.py
  </files>
  <read_first>
    tests/conftest.py (entender como testes importam e usam fixtures)
    tests/test_app_startup.py (entender o que GET / espera)
    tests/test_config.py (entender quais campos Settings deve ter)
    tests/test_database.py (entender quais tabelas e colunas sao verificadas)
    .planning/phases/01-foundation/01-RESEARCH.md (patterns exatos de codigo)
  </read_first>
  <action>
    1. Criar app/__init__.py (vazio)

    2. Criar app/config.py (per INFRA-02, Pattern 2 do RESEARCH):
       from pydantic_settings import BaseSettings

       class Settings(BaseSettings):
           database_url: str = "sqlite:///./flight_monitor.db"
           amadeus_client_id: str = ""
           amadeus_client_secret: str = ""
           telegram_bot_token: str = ""
           telegram_chat_id: str = ""
           model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

       settings = Settings()

    3. Criar app/database.py (per INFRA-03, Pattern 1 do RESEARCH):
       from sqlalchemy import create_engine
       from sqlalchemy.orm import sessionmaker, DeclarativeBase, Session
       from app.config import settings

       engine = create_engine(
           settings.database_url,
           connect_args={"check_same_thread": False},
       )
       SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

       class Base(DeclarativeBase):
           pass

       def get_db():
           db = SessionLocal()
           try:
               yield db
           finally:
               db.close()

    4. Criar app/models.py (per INFRA-03, Pattern 3 do RESEARCH):
       import datetime
       from sqlalchemy import JSON, String, Integer, Float, Boolean, Date, DateTime
       from sqlalchemy.orm import Mapped, mapped_column
       from sqlalchemy.sql import func
       from app.database import Base

       class RouteGroup(Base):
           __tablename__ = "route_groups"
           id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
           name: Mapped[str] = mapped_column(String(100))
           origins: Mapped[list] = mapped_column(JSON)
           destinations: Mapped[list] = mapped_column(JSON)
           duration_days: Mapped[int] = mapped_column(Integer)
           travel_start: Mapped[datetime.date] = mapped_column(Date)
           travel_end: Mapped[datetime.date] = mapped_column(Date)
           target_price: Mapped[float | None] = mapped_column(Float, nullable=True)
           is_active: Mapped[bool] = mapped_column(Boolean, default=True)
           created_at: Mapped[datetime.datetime] = mapped_column(DateTime, server_default=func.now())
           updated_at: Mapped[datetime.datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())

    5. Criar app/schemas.py (Pattern 4 do RESEARCH):
       - RouteGroupCreate com field_validators para IATA codes (^[A-Z]{3}$), duration_days >= 1
       - RouteGroupUpdate com todos os campos Optional
       - RouteGroupResponse com model_config = {"from_attributes": True}

    6. Criar app/routes/__init__.py (vazio)
    7. Criar app/services/__init__.py (vazio)

    8. Criar app/services/route_group_service.py (Pattern 6 do RESEARCH):
       MAX_ACTIVE_GROUPS = 10
       def check_active_group_limit(db: Session, exclude_id: int | None = None) -> None:
           # conta grupos ativos, raise HTTPException(409) se >= 10

    9. Criar app/routes/route_groups.py (esqueleto minimo - Pattern CRUD do RESEARCH):
       - router = APIRouter(prefix="/route-groups", tags=["route-groups"])
       - POST / (create), GET / (list), GET /{id}, PATCH /{id}, DELETE /{id}
       - Implementar completamente seguindo patterns do RESEARCH

    10. Criar main.py (Pattern 5 do RESEARCH):
        import sys
        sys.stdout.reconfigure(encoding="utf-8")
        from contextlib import asynccontextmanager
        from fastapi import FastAPI
        from app.database import Base, engine

        @asynccontextmanager
        async def lifespan(app: FastAPI):
            Base.metadata.create_all(bind=engine)
            yield

        app = FastAPI(title="Flight Monitor", lifespan=lifespan)
        from app.routes.route_groups import router as route_groups_router
        app.include_router(route_groups_router, prefix="/api/v1")

        @app.get("/")
        def root():
            return {"status": "ok", "app": "Flight Monitor"}

        if __name__ == "__main__":
            import uvicorn
            uvicorn.run("main:app", host="127.0.0.1", port=8000, reload=True)

    11. Rodar testes: python -m pytest tests/ -v
        TODOS DEVEM PASSAR (GREEN).

    12. Se algum teste falhar, corrigir o CODIGO (nunca o teste).
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_startup.py tests/test_config.py tests/test_database.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo main.py existe e contem "Flight Monitor" e "uvicorn.run"
    - Arquivo app/config.py contem "class Settings" e "amadeus_client_id"
    - Arquivo app/database.py contem "def get_db" e "check_same_thread"
    - Arquivo app/models.py contem "class RouteGroup" e '__tablename__ = "route_groups"'
    - Arquivo app/schemas.py contem "class RouteGroupCreate" e "IATA_PATTERN"
    - Arquivo app/services/route_group_service.py contem "MAX_ACTIVE_GROUPS = 10"
    - python -m pytest tests/test_app_startup.py tests/test_config.py tests/test_database.py retorna todos PASSED
  </acceptance_criteria>
  <done>App inicia, Settings carrega .env, banco cria tabelas automaticamente, todos os testes de infra passam (GREEN)</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_app_startup.py tests/test_config.py tests/test_database.py -v --tb=short
Todos os testes devem passar. App deve responder em localhost:8000.
</verification>

<success_criteria>
- GET / retorna {"status": "ok", "app": "Flight Monitor"} (INFRA-01)
- Settings carrega valores do .env com defaults validos (INFRA-02)
- Tabela route_groups existe com todas as colunas corretas apos startup (INFRA-03)
- Todos os testes de infra passam (GREEN phase completa)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
