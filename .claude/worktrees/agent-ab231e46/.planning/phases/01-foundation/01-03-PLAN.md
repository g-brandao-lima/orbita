---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/test_route_groups.py
  - tests/test_app_startup.py
  - tests/test_config.py
  - tests/test_database.py
  - app/routes/route_groups.py
  - app/schemas.py
  - app/models.py
  - app/services/route_group_service.py
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
  - ROUTE-06

must_haves:
  truths:
    - "Todos os testes passam apos refatoracao"
    - "Nenhuma duplicacao de logica"
    - "Nomes auto-explicativos"
    - "Tratamento de erros explicito"
    - "App roda de ponta a ponta e responde a requisicoes reais"
  artifacts:
    - path: "tests/test_route_groups.py"
      provides: "Testes limpos e bem organizados"
    - path: "app/routes/route_groups.py"
      provides: "Endpoints CRUD limpos"
    - path: "app/services/route_group_service.py"
      provides: "Business logic isolada"
  key_links:
    - from: "main.py"
      to: "app/routes/route_groups.py"
      via: "include_router registra endpoints"
      pattern: "include_router"
---

<objective>
Fase REFACTOR do TDD: revisar todo o codigo produzido nos Plans 01 e 02, aplicar checklist de qualidade do CLAUDE.md, e verificar que a aplicacao funciona de ponta a ponta. Checkpoint humano para validar que tudo roda no ambiente real.

Purpose: Garantir qualidade do codigo antes de avancar para Phase 2. Detectar problemas de integracao que testes automatizados podem nao pegar.
Output: Codigo refatorado, checklist de revisao completo, confirmacao humana de que app funciona.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatoracao e checklist de qualidade (REFACTOR)</name>
  <files>
    app/routes/route_groups.py, app/services/route_group_service.py,
    app/schemas.py, app/models.py, app/config.py, app/database.py, main.py
  </files>
  <read_first>
    CLAUDE.md (checklist de revisao obrigatorio na secao FASE 4)
    app/routes/route_groups.py (verificar duplicacao, SRP)
    app/services/route_group_service.py (verificar SRP)
    app/schemas.py (verificar validadores)
    app/models.py (verificar model)
    main.py (verificar entry point)
    tests/test_route_groups.py (verificar cobertura)
  </read_first>
  <action>
    Aplicar o checklist de revisao do CLAUDE.md (Fase 4):

    1. Rodar python -m pytest tests/ -v para confirmar baseline GREEN

    2. Verificar DRY (no duplicacao):
       - Se ha logica de "buscar grupo por id e retornar 404" repetida nos endpoints GET/PATCH/DELETE, extrair para uma funcao helper:
         def get_group_or_404(group_id: int, db: Session) -> RouteGroup
       - Se ha validacao duplicada, consolidar

    3. Verificar SRP (responsabilidade unica):
       - Cada funcao de rota deve ter no maximo 10-15 linhas
       - Service layer deve conter business logic, routes devem ser thin controllers
       - Se algum endpoint esta fazendo validacao de negocio diretamente, mover para service

    4. Verificar nomes:
       - Funcoes, variaveis, parametros devem ser auto-explicativos
       - Sem abreviacoes obscuras

    5. Verificar tratamento de erros:
       - Todo endpoint que recebe ID deve retornar 404 se nao encontrar
       - Erros de validacao retornam 422 (automatico via Pydantic)
       - Limite de ativos retorna 409

    6. Remover qualquer console.log/print de debug

    7. Verificar complexidade: nenhuma funcao deve ter mais de 5 IFs aninhados

    8. Rodar python -m pytest tests/ -v novamente
       TODOS os testes devem continuar passando apos refatoracao.
       Se algum teste falhar, DESFAZER ultima mudanca.

    9. Produzir RESUMO DA ENTREGA conforme template CLAUDE.md:
       Feature: Foundation Phase 1
       Testes: X passando / 0 falhando
       Arquivos modificados: [lista]
       Decisoes tomadas: [lista]
       Pendencias: [lista]
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/ -v retorna TODOS os testes PASSED
    - Nenhuma funcao em app/routes/route_groups.py tem mais de 20 linhas
    - Nenhum print() ou console.log esquecido em arquivos app/
    - Se helper get_group_or_404 foi criado, eh usado em GET/{id}, PATCH/{id}, DELETE/{id}
  </acceptance_criteria>
  <done>Codigo refatorado, checklist aplicado, todos os testes passam</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana da aplicacao rodando</name>
  <files>main.py</files>
  <action>
    Checkpoint de verificacao humana. O executor deve iniciar o servidor com python main.py e apresentar as instrucoes de verificacao ao usuario. Aguardar confirmacao antes de prosseguir.
  </action>
  <what-built>
    Aplicacao Flight Monitor completa para Phase 1:
    - FastAPI rodando com uvicorn em localhost:8000
    - SQLite criado automaticamente com tabela route_groups
    - API CRUD completa em /api/v1/route-groups/
    - Validacao de IATA codes, duracao, limite de 10 ativos
    - Config via .env
    - Suite de testes passando
  </what-built>
  <how-to-verify>
    1. No terminal, ativar venv e rodar: python main.py
    2. Abrir http://localhost:8000 no navegador - deve retornar JSON {"status": "ok", "app": "Flight Monitor"}
    3. Abrir http://localhost:8000/docs - deve mostrar Swagger UI com os endpoints
    4. Testar no Swagger UI:
       a. POST /api/v1/route-groups/ com:
          {"name": "Europa Verao", "origins": ["GRU"], "destinations": ["LIS"], "duration_days": 14, "travel_start": "2026-07-01", "travel_end": "2026-08-31"}
          -> deve retornar 201
       b. GET /api/v1/route-groups/ -> deve listar o grupo criado
       c. PATCH /api/v1/route-groups/1 com {"is_active": false} -> deve retornar 200
       d. DELETE /api/v1/route-groups/1 -> deve retornar 204
    5. Verificar que arquivo flight_monitor.db foi criado na raiz do projeto
    6. No terminal, rodar: python -m pytest tests/ -v
       -> deve mostrar todos os testes passando
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Usuario confirma que GET / retorna {"status": "ok", "app": "Flight Monitor"}
    - Usuario confirma que Swagger UI carrega em /docs
    - Usuario confirma que POST/GET/PATCH/DELETE funcionam no Swagger
    - Usuario confirma que flight_monitor.db foi criado
    - python -m pytest tests/ -v retorna todos PASSED
  </acceptance_criteria>
  <done>Usuario aprovou a aplicacao rodando de ponta a ponta</done>
  <resume-signal>Digite "aprovado" se tudo funciona, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
python -m pytest tests/ -v --tb=short
Todos os testes passam. App funciona no Swagger UI.
</verification>

<success_criteria>
- Codigo refatorado sem duplicacao
- Todos os testes passam apos refatoracao
- Usuario confirmou que app funciona de ponta a ponta no navegador
- Phase 1 pronta para transicao para Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
