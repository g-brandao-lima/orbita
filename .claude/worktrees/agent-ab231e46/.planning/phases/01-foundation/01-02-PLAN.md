---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_route_groups.py
  - app/routes/route_groups.py
  - app/services/route_group_service.py
  - app/schemas.py
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
  - ROUTE-06

must_haves:
  truths:
    - "Usuario pode criar um Grupo de Rota com nome, origens, destinos, duracao e periodo via POST /api/v1/route-groups/"
    - "Usuario pode definir preco-alvo opcional ao criar ou editar grupo"
    - "Usuario pode ativar e desativar grupo via PATCH sem deletar"
    - "Usuario pode editar campos de um grupo existente via PATCH"
    - "Usuario pode deletar grupo via DELETE"
    - "Sistema rejeita criacao ou ativacao quando ja existem 10 grupos ativos"
  artifacts:
    - path: "tests/test_route_groups.py"
      provides: "Testes completos para todos os ROUTE-* requirements"
      contains: "test_create_route_group"
    - path: "app/routes/route_groups.py"
      provides: "Endpoints CRUD para Route Groups"
      exports: ["router"]
    - path: "app/services/route_group_service.py"
      provides: "Business logic: max 10 active groups constraint"
      contains: "check_active_group_limit"
  key_links:
    - from: "app/routes/route_groups.py"
      to: "app/services/route_group_service.py"
      via: "check_active_group_limit chamado antes de create/activate"
      pattern: "check_active_group_limit"
    - from: "app/routes/route_groups.py"
      to: "app/schemas.py"
      via: "Pydantic valida request body automaticamente"
      pattern: "RouteGroupCreate"
    - from: "app/routes/route_groups.py"
      to: "app/models.py"
      via: "SQLAlchemy persiste RouteGroup no banco"
      pattern: "RouteGroup"
---

<objective>
Implementar e testar todos os endpoints CRUD de Route Groups seguindo TDD (RED -> GREEN). Este plano cobre ROUTE-01 a ROUTE-06: criar, editar, ativar/desativar, deletar grupos, e o limite de 10 grupos ativos.

Purpose: Route Groups sao a entidade central do sistema. Sem eles, nao ha o que monitorar nas fases seguintes.
Output: API CRUD completa e testada para /api/v1/route-groups/ com todas as validacoes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Interfaces criadas pelo Plan 01 que este plano consome -->

From app/database.py:
```python
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

From app/models.py:
```python
class RouteGroup(Base):
    __tablename__ = "route_groups"
    id: Mapped[int]
    name: Mapped[str]
    origins: Mapped[list]  # JSON column, e.g. ["GRU", "CGH"]
    destinations: Mapped[list]  # JSON column, e.g. ["LIS", "OPO"]
    duration_days: Mapped[int]
    travel_start: Mapped[datetime.date]
    travel_end: Mapped[datetime.date]
    target_price: Mapped[float | None]
    is_active: Mapped[bool]  # default=True
    created_at: Mapped[datetime.datetime]
    updated_at: Mapped[datetime.datetime]
```

From app/schemas.py:
```python
class RouteGroupCreate(BaseModel):
    name: str
    origins: list[str]  # validated: ^[A-Z]{3}$, min 1
    destinations: list[str]  # validated: ^[A-Z]{3}$, min 1
    duration_days: int  # validated: >= 1
    travel_start: datetime.date
    travel_end: datetime.date
    target_price: float | None = None

class RouteGroupUpdate(BaseModel):
    # All fields Optional
    name: str | None = None
    origins: list[str] | None = None
    ...
    is_active: bool | None = None

class RouteGroupResponse(BaseModel):
    # All fields from model
    model_config = {"from_attributes": True}
```

From app/services/route_group_service.py:
```python
MAX_ACTIVE_GROUPS = 10
def check_active_group_limit(db: Session, exclude_id: int | None = None) -> None:
    # raises HTTPException(409) if >= 10 active groups
```

From tests/conftest.py:
```python
# Fixtures disponiveis: "db" (Session), "client" (TestClient)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever testes completos para Route Groups (RED)</name>
  <files>tests/test_route_groups.py</files>
  <read_first>
    tests/conftest.py (fixtures disponiveis: client, db)
    app/schemas.py (formato exato dos campos e validacoes)
    app/models.py (colunas do modelo)
    .planning/phases/01-foundation/01-RESEARCH.md (patterns de teste)
  </read_first>
  <behavior>
    ROUTE-01 - Criar Grupo de Rota:
    - test_create_route_group: POST /api/v1/route-groups/ com body valido retorna 201 e JSON com todos os campos
    - test_create_route_group_invalid_iata: POST com origem "XX" retorna 422
    - test_create_route_group_empty_origins: POST com origins=[] retorna 422
    - test_create_route_group_invalid_duration: POST com duration_days=0 retorna 422

    ROUTE-02 - Preco-alvo opcional:
    - test_create_with_target_price: POST com target_price=1500.0 retorna 201 e campo presente
    - test_create_without_target_price: POST sem target_price retorna 201 e target_price=null

    ROUTE-03 - Ativar/desativar:
    - test_deactivate_route_group: PATCH /{id} com {"is_active": false} retorna 200 e is_active=false
    - test_activate_route_group: criar grupo inativo, PATCH com {"is_active": true} retorna 200 e is_active=true
    - test_deactivate_does_not_delete: apos desativar, GET /{id} retorna 200 (grupo ainda existe)

    ROUTE-04 - Editar:
    - test_update_route_group_name: PATCH /{id} com {"name": "novo nome"} retorna 200 e name="novo nome"
    - test_update_route_group_origins: PATCH /{id} com {"origins": ["GIG"]} retorna 200 e origins=["GIG"]
    - test_update_nonexistent_group: PATCH /999 retorna 404
    - test_update_partial_fields: PATCH com apenas um campo nao altera os demais

    ROUTE-05 - Deletar:
    - test_delete_route_group: DELETE /{id} retorna 204
    - test_delete_nonexistent_group: DELETE /999 retorna 404
    - test_get_after_delete: apos DELETE, GET /{id} retorna 404

    ROUTE-06 - Limite 10 ativos:
    - test_max_active_groups_limit: criar 10 grupos ativos, tentar criar 11o retorna 409
    - test_max_active_groups_allows_inactive: criar 10 ativos + 1 inativo (com is_active=false no body) deve funcionar
    - test_activate_exceeds_limit: ter 10 ativos, tentar ativar um inativo retorna 409

    Extras:
    - test_list_route_groups: GET /api/v1/route-groups/ retorna lista
    - test_get_route_group_by_id: GET /api/v1/route-groups/{id} retorna grupo
    - test_get_nonexistent_group: GET /api/v1/route-groups/999 retorna 404
  </behavior>
  <action>
    1. Criar tests/test_route_groups.py com TODOS os testes listados acima.

    2. Usar o payload padrao para criacao:
       VALID_GROUP = {
           "name": "Europa Verao",
           "origins": ["GRU", "CGH"],
           "destinations": ["LIS", "OPO"],
           "duration_days": 14,
           "travel_start": "2026-07-01",
           "travel_end": "2026-08-31",
           "target_price": 3500.0
       }

    3. Para test_max_active_groups_limit, criar 10 grupos em um loop:
       for i in range(10):
           payload = {**VALID_GROUP, "name": f"Group {i}"}
           response = client.post("/api/v1/route-groups/", json=payload)
           assert response.status_code == 201

    4. Para test_create_without_target_price, enviar payload sem target_price e verificar que a resposta contem "target_price": None

    5. Para test_max_active_groups_allows_inactive, a RouteGroupCreate NAO tem campo is_active (grupos sao criados sempre ativos). A forma de testar eh: criar 10 ativos, desativar um via PATCH, criar outro (agora sao 10 novamente). OU: se o schema permitir, criar com is_active separadamente. Verificar implementacao do Plan 01.
       NOTA: RouteGroupCreate nao tem is_active. Entao o teste deve: criar 10 ativos, desativar 1 via PATCH, criar outro (volta a 10 ativos), e confirmar sucesso.

    6. Rodar testes: python -m pytest tests/test_route_groups.py -v
       Testes DEVEM FALHAR (RED) se os endpoints ainda nao estiverem implementados corretamente.
       Se os endpoints ja foram implementados no Plan 01, verificar que os testes PASSAM e considerar como GREEN direto.
  </action>
  <verify>
    <automated>python -m pytest tests/test_route_groups.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/test_route_groups.py existe
    - Contem "test_create_route_group" (ROUTE-01)
    - Contem "test_create_with_target_price" (ROUTE-02)
    - Contem "test_deactivate_route_group" (ROUTE-03)
    - Contem "test_update_route_group_name" (ROUTE-04)
    - Contem "test_delete_route_group" (ROUTE-05)
    - Contem "test_max_active_groups_limit" (ROUTE-06)
    - Minimo 18 funcoes de teste no arquivo
  </acceptance_criteria>
  <done>Testes completos cobrindo todos os ROUTE-01 a ROUTE-06, fase RED confirmada ou GREEN se Plan 01 ja implementou endpoints</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ajustar implementacao ate todos os testes de Route Groups passarem (GREEN)</name>
  <files>
    app/routes/route_groups.py,
    app/services/route_group_service.py,
    app/schemas.py
  </files>
  <read_first>
    tests/test_route_groups.py (entender exatamente o que cada teste espera)
    app/routes/route_groups.py (ver estado atual dos endpoints)
    app/services/route_group_service.py (ver estado atual do service)
    app/schemas.py (ver estado atual dos schemas)
  </read_first>
  <action>
    1. Ler TODOS os testes de test_route_groups.py para entender o contrato esperado.

    2. Verificar se os endpoints do Plan 01 ja fazem os testes passarem rodando:
       python -m pytest tests/test_route_groups.py -v
       Se todos passarem, esta task eh confirmacao do GREEN. Pular para step 6.

    3. Se houver testes falhando, ajustar app/routes/route_groups.py:
       - POST /route-groups/ deve chamar check_active_group_limit(db) antes de criar
       - POST deve retornar 201 (status_code=201)
       - PATCH /{group_id} deve chamar check_active_group_limit(db, exclude_id=group.id) quando is_active muda para True
       - DELETE /{group_id} deve retornar 204
       - GET /{group_id} e PATCH /{group_id} e DELETE /{group_id} devem retornar 404 se grupo nao existe
       - Usar model_dump(exclude_unset=True) no update para nao sobrescrever campos nao enviados

    4. Ajustar app/schemas.py se necessario:
       - RouteGroupCreate: @field_validator("origins", "destinations") deve rejeitar listas vazias e codigos fora do padrao ^[A-Z]{3}$
       - RouteGroupCreate: @field_validator("duration_days") deve rejeitar valores < 1
       - RouteGroupResponse: deve incluir todos os campos do modelo incluindo id, is_active, created_at, updated_at

    5. Ajustar app/services/route_group_service.py se necessario:
       - check_active_group_limit deve contar grupos onde is_active == True
       - Deve excluir o grupo sendo editado (exclude_id) da contagem
       - Deve levantar HTTPException(status_code=409, detail="Maximum of 10 active route groups reached.")

    6. Rodar suite completa: python -m pytest tests/ -v
       TODOS os testes (infra + route groups) devem passar.

    7. Se algum teste falhar, corrigir o CODIGO (nunca o teste), a menos que o teste tenha um bug obvio de digitacao.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/ -v retorna TODOS os testes PASSED
    - Zero testes falhando
    - app/routes/route_groups.py contem "status_code=201" no POST
    - app/routes/route_groups.py contem "status_code=204" no DELETE
    - app/routes/route_groups.py contem "check_active_group_limit"
    - app/services/route_group_service.py contem "MAX_ACTIVE_GROUPS = 10"
    - app/schemas.py contem "IATA_PATTERN"
  </acceptance_criteria>
  <done>Todos os testes passam (GREEN), API CRUD completa funciona para Route Groups com validacoes e limite de 10 ativos</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -v --tb=short
Todos os testes devem passar cobrindo INFRA-01/02/03 e ROUTE-01/02/03/04/05/06.
</verification>

<success_criteria>
- POST /api/v1/route-groups/ cria grupo e retorna 201 (ROUTE-01)
- target_price eh opcional e aceita null (ROUTE-02)
- PATCH com is_active=false desativa sem deletar (ROUTE-03)
- PATCH com campos parciais atualiza apenas esses campos (ROUTE-04)
- DELETE retorna 204 e grupo desaparece (ROUTE-05)
- 11o grupo ativo eh rejeitado com 409 (ROUTE-06)
- Validacao rejeita IATA invalidos e duracao < 1 com 422
- Todos os testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
